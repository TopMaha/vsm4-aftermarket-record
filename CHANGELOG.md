# CHANGELOG — VSM Aftermarket / KPI Record

## v3.66 — 2026-09-02 — แก้ D1 ชนเพดาน rows read/วัน (sync ล้มทั้งระบบ)  🚧 ยังไม่ deploy

**อาการที่ผู้ใช้เจอ** — เปิดแอปแล้วขึ้นแถบส้ม
`Sync ไม่สำเร็จ — ยังใช้ Local ได้ปกติ · สาเหตุ: D1_ERROR: Your account has exceeded D1's free tier daily row read limit.`
ยืนยันแล้วว่าไม่ใช่บั๊กของโค้ดเส้นทางใดเส้นทางหนึ่ง แต่เป็น **โควตา**: D1 free tier อ่านได้ 5,000,000 แถว/วัน
(ยิง query ตรงเข้า D1 จากเครื่องมือ admin ก็ได้ error เดียวกัน = ทั้งฐานข้อมูลถูกล็อกจนเที่ยงคืน UTC)

### ต้นเหตุ — เปิดแอป 1 ครั้ง = อ่าน D1 เกือบแสนแถว

ประมาณจากขนาดตารางที่บันทึกไว้ (kpi_records 15,617 แถวเมื่อ 7 ส.ค. โต ~165 แถว/วัน → ~20,000 แถวตอนนี้)

| คำขอ (ต่อการเปิดแอป 1 ครั้ง 1 เครื่อง) | แถวที่อ่าน | ทำไมถึงแพง |
|---|---:|---|
| `/api/kpi` ก้อนประวัติทั้งหมด | ~20,000 | บังคับ full scan ด้วย `+shift_date` (ตั้งใจ — index แพงกว่า) |
| `/api/records` ก้อนประวัติทั้งหมด | ~20,000 | เหมือนกัน |
| `/api/kpi/produced` | ~20,000 | `SELECT DISTINCT` ทั้งตาราง |
| `/api/init` → `getIds` | ~8,000 | `ORDER BY valve_no,lot,id_code` ไม่มี index ตรง → สแกน+sort ก่อนตัด LIMIT |
| `pullIds()` → `/api/ids` | ~8,000 | สแกน valve_ids ซ้ำอีกรอบ ทั้งที่ init ส่งมาครบแล้ว |
| 30 วัน + เดือนนี้ + plans | ~12,000 | ใช้ index — ยอมรับได้ |
| **รวม** | **~88,000** | **≈ 57 ครั้ง/วัน = โควตาหมด** |

โรงงานมีเครื่องหลายสิบเครื่อง + refresh + บอร์ด IOT → ทะลุตั้งแต่ยังไม่หมดกะแรก

**ตัวขยายที่มองข้ามมาตลอด**: โหมด delta (`?since=`) ที่ทำไว้เมื่อ 7 ส.ค. ครอบแค่ 2 ใน 7 คำขอข้างบน
และถูกล้างทิ้งทุกเที่ยงคืน เพราะ cache บังคับว่า `c.day === วันนี้` — ทั้งสอง URL ล็อก `to=<วันนี้>` ไว้
⇒ **ทุกเครื่องต้องลากประวัติทั้งชีวิตลงมาใหม่อย่างน้อยวันละครั้ง ไม่มีทางเลี่ยง**

### สิ่งที่แก้

**1) ตรึงขอบบนของ URL ก้อนใหญ่เป็น `to=2999-12-31` (`ALL_TO`)** — ฝั่งแอป ทั้งสคริปต์ prefetch หัวไฟล์และ `ensureAllJobsLoaded()`

* URL ไม่มีวันที่อยู่ในนั้นอีก → cache ใน IndexedDB ใช้ **ข้ามวัน** ได้ → ตัดการโหลดเต็มวันละครั้ง/เครื่อง (~40,000 แถว) ทิ้งทั้งก้อน
* ทุกเครื่องขอ URL เดียวกันเป๊ะตลอดกาล → edge cache มีโอกาสโดนจริง (เดิมเปลี่ยนคีย์ทุกเที่ยงคืน)
* ช่วงยังกว้างเกิน 400 วัน → Worker เลือกแผน full scan แบบเดิมเป๊ะ **ผลลัพธ์ไม่เปลี่ยน**
* ผลพลอยได้: แถวที่ลงวันที่ล่วงหน้าเลยวันนี้ถูกนับใน WIP ด้วย (เดิมตกขอบ `to` หายเงียบ)
* `ALLCACHE_VER` 1 → 2 บังคับทิ้ง cache รุ่นเก่าที่สร้างจาก URL แบบมีวันที่ (ไม่งั้นจะขาดแถวกลุ่มนั้นถาวร)
* ตัดด่าน `pf.today !== todayISO()` ใน `_pfTake()` ทิ้ง — ไม่มี URL ไหนผูกวันที่แล้ว ด่านนี้เหลือแต่โทษ (ทิ้งของที่ดึงมาแล้วไปยิงซ้ำ)

**2) `refreshProducedSet()` คิดจาก `_allKpi` ในเครื่อง ไม่ยิง `/api/kpi/produced`**

`_allKpi` คือทุกแถวของ `kpi_records` ที่อยู่ในเครื่องอยู่แล้ว (โหมด slim มี `valve_no`/`lot` ครบ) → ได้คำตอบเดียวกันเป๊ะ
ย้ายลำดับใน `startWipinSync()` ให้เรียก **หลัง** `ensureAllJobsLoaded()` (เดิมยิงคู่ขนาน = `_allKpi` ยังว่างเสมอ → ตกไปใช้ API ทุกครั้ง)
ยังไม่มีก้อนใหญ่ → ใช้ API เป็นทางสำรองเหมือนเดิม

**3) เลิกยิง `pullIds()` เมื่อ `/api/init` ส่ง ids มาครบแล้ว** — `getIds` ใช้ limit ปริยาย 5,000 → ได้น้อยกว่านั้น = ครบทั้งตาราง การยิงซ้ำคือสแกน valve_ids ทิ้งเปล่า ๆ

**4) index ที่หายไปของ `getIds`** — `idx_valve_ids_sort ON valve_ids(valve_no, lot, id_code)` ตรงกับ `ORDER BY` เป๊ะ → อ่านตาม index แล้วหยุดที่ LIMIT แทนสแกนทั้งตาราง+sort (`SCHEMA_VER` → `2026-09-02-idsort`)

**5) เปิด Workers Caching + ประกาศ Cache-Control ให้ชัดทุกคำตอบ**

* `wrangler.toml` เพิ่ม `[cache] enabled = true` — cache hit **คืนคำตอบโดยไม่รัน Worker เลย** (ไม่แตะ D1 สักแถว)
  และ **รวบคำขอที่ยิงพร้อมกัน (request collapsing)** → 20 เครื่องเปิดแอปพร้อมกันตอนเปลี่ยนกะ = สแกน D1 รอบเดียว
  (สองข้อนี้คือสิ่งที่ Cache API `caches.default` ทำให้ไม่ได้)
* `json()` ตั้งต้น `Cache-Control: no-store` — จำเป็น เพราะ Workers Caching เดินตาม RFC 9111
  ซึ่งอนุญาตให้ cache คำตอบ 200 ที่ไม่มี Cache-Control แบบเดา ๆ ได้ ⇒ ถ้าไม่ประกาศ หน้าประวัติ/Dashboard อาจได้ของค้าง
* `servedCached()` ผูก query เข้าไปใน cache key แล้ว (เดิมใช้ได้แต่ endpoint ที่ไม่มีพารามิเตอร์) และยังเป็นตาข่ายชั้นในระดับ colo

| endpoint | TTL | เหตุผล |
|---|---:|---|
| `/api/init` | 60 วิ | master data เปลี่ยนน้อย · ฝั่งแอป memoize 15 วิอยู่แล้ว |
| `/api/ids` | 300 วิ | ID ใหม่รอได้ — สแกนบาร์โค้ดใช้ `/api/ids/lookup` ซึ่งยิงตรงเข้า index ไม่ผ่าน cache |
| `/api/kpi/produced` | 300 วิ | เหลือเป็นทางสำรองแล้ว (เดิม 60) |
| `/api/kpi` · `/api/records` **เฉพาะช่วง > 400 วัน และไม่มี `since=`** | 60 วิ | คือก้อนประวัติทั้งหมดพอดี · ทุกเครื่อง URL เดียวกัน |

**ที่จงใจไม่ cache**: ช่วงแคบทุกกรณี (หน้าประวัติ · Dashboard · บอร์ด IOT) — ถูกอยู่แล้วเพราะใช้ index
และเป็นหน้าที่ผู้ใช้เพิ่งกดบันทึกแล้วต้องเห็นผลทันที · คำขอที่มี `since=` (คีย์ต่างกันทุกเครื่อง cache ไม่มีวันโดน) · **ทุก error**

### ผลที่คาดว่าจะได้

| | ก่อน | หลัง |
|---|---:|---:|
| เปิดแอปครั้งแรกของวัน (cache miss ทุกชั้น) | ~88,000 แถว | ~48,000 แถว |
| เปิดแอปรอบถัด ๆ ไป (มี cache ในเครื่อง) | ~53,000 แถว | ~12,000 แถว |
| เครื่องที่ 2–20 ที่เปิดในหน้าต่าง TTL เดียวกัน | เต็มราคาทุกเครื่อง | **~0 แถว** (cache hit ไม่รัน Worker) |

### ⚠️ ยังไม่ได้ทดสอบกับของจริง

D1 ถูกล็อกจนเที่ยงคืน UTC ตอนที่แก้ → **ยังไม่ได้ยิงทดสอบกับฐานข้อมูลจริงแม้แต่ครั้งเดียว**
ที่ตรวจแล้วมีแค่: `node --check` ผ่านทั้ง worker.js และสคริปต์ทั้ง 3 ก้อนใน index.html · `wrangler deploy --dry-run` ผ่าน (config `[cache]` ถูกรับ)
**หลัง deploy ต้องดูจริง**: กราฟ Rows read ใน Cloudflare D1 dashboard, header `Cf-Cache-Status` ของ `/api/init`,
และยอด WIP/WIP IN ต้องเท่ากับก่อนแก้ (ข้อ 1 เปลี่ยนขอบเขตข้อมูลที่ดึงมา — เพิ่มแถวที่ลงวันที่ล่วงหน้าเข้ามา)

### ถ้ายังไม่พอ

โครงนี้ยัง "โหลดทั้งตารางมาคิดในเครื่อง" อยู่ ซึ่งโตตามข้อมูลไปเรื่อย ๆ — ทางออกระยะยาวคือย้ายการคิด WIP
ไปฝั่ง SQL (คืนเฉพาะยอดสรุป) หรืออัปเป็น Workers Paid ($5/เดือน = 25,000 ล้านแถว/เดือน) ซึ่งแก้ปัญหาทันทีโดยไม่ต้องแตะโค้ด

---

## v3.65 — 2026-09-01 — แผนขายเลิกผูก Lot + ปรับหน้าต่างต่อกะเองได้ + ปุ่ม KPI รวมทุกสาย  ✅ deployed

**✅ deployed 2026-09-01** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `64d9c39` · run [33532519089](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/33532519089) → `success` (run 161 `83053ae` = แก้เอกสารอย่างเดียว) |
| Worker `vsm4-api` | Version ID `6d965a53-9aec-4816-a465-f3d5f7cd089c` (bundle `index.html` ใหม่) |

ยืนยันด้วย curl ทั้งสอง URL → ได้ `v3.65` · **ขนาดเท่ากันเป๊ะ 1,937,138 bytes · cmp กับไฟล์ในเครื่องแล้วเหมือนกันทุก byte**
และ grep ในไฟล์ที่เสิร์ฟจริงเจอ `_pasteHasLotCol` + หัวคอลัมน์ `3. Qty` (ไม่มี Lot) ทั้ง 2 ฝั่ง

**สิ่งที่ผู้ใช้สั่ง**

1. หน้าแผนงานและหน้า IOT ให้ใช้ข้อมูลแค่ **Valve No · จำนวน · วันที่ส่ง** เท่านั้น ไม่ต้อง tag Lot กับ Valve No
   — *"Lot ไม่จำเป็นต้องตรง แค่มีงานเข้ามาพอ"*
2. หน้า IOT: ข้อมูลต่อกะ / เครื่องจักรเสีย-ค้าง **อันเก่าไม่ต้องแสดง** และขอให้ **ปรับแต่งหน้าต่างเองได้**
   เพราะบางครั้งข้อมูลยาวจนมองไม่เห็น
3. กล่อง "วางข้อมูลจาก Excel" ของหน้าวางแผน — *"ในการวางแผนงานกำหนดใหม่ไม่ต้องมี lot"*
4. ปุ่ม KPI (OAE/DLE/PPLH/PDAC/SCRAP) บนแผงรายชั่วโมงของหน้า IOT รวม —
   *"เพิ่มเติมเป็นค่ารวมทั้ง VSM1 2 3 4 ไม่นับ QC · เช่น OAE ก็รวมทุก VSM · SCRAP ก็รวมทุก VSM"*

---

### 1) แผนขายจับคู่ด้วย "Valve No" อย่างเดียว — เลิกใช้ Lot

เดิมทุกจุดที่เทียบ "แผน ↔ ของจริง" ใช้คีย์ `jobKey(valve, lot)` = `VALVE::lot`
ผลคือแผนที่ใส่ Lot ไว้คนละตัวกับที่ผลิตจริง (ซึ่งเกิดตลอด) จะ **ไม่ถูกนับเลย** — ขึ้น "ยังไม่เริ่ม" ทั้งที่ของเดินอยู่

ตอนนี้เปลี่ยนคีย์เป็น `vKey(valve)` ทั้งชุด:

| จุด | เดิม | ใหม่ |
|---|---|---|
| `_planMissWhy()` | เบอร์ตรง + Lot ตรง (หรือแผนไม่ระบุ Lot) | เบอร์ตรงอย่างเดียว · เหตุผล `lot` เลิกเกิด |
| `_planAutoStatus()` | `match` เทียบ valve+lot · `getJobProc(valve,lot)` | `match` เทียบ valve · `_valveJobDone(valve)` (ใหม่) |
| `_wipinReceivedQty()` | มี Lot → นับเฉพาะ Lot นั้น | นับทุก Lot ของเบอร์นั้นเสมอ |
| `_islProdIndex()` / `_islLotProd()` | index ต่อ `valve::lot` | index ต่อ `valve` |
| `_islBuildAlloc()` | จัดสรร FIFO ภายในล็อต | จัดสรร FIFO ภายใน **เบอร์วาล์ว** ตามลำดับวันส่ง |
| `_islJobAt()` | process ไกลสุดของล็อตนั้น | process ไกลสุดของเบอร์นั้น (รวมทุก Lot) |
| `_wipPlanRows()` / `_orderSupplyMap()` / `_orderReconRows()` | supply ต่อ `valve::lot` | supply ต่อ `valve` |
| `planKey()` (กันแผนซ้ำ) | valve + lot + วันที่ | **valve + วันที่ + จำนวน** |
| `_planMoveKey()` (ตรวจเลื่อนเดือน) | valve + lot + qty | valve + qty |
| `planMissingIntake()` | รวมยอดต่อ `valve::lot` | รวมยอดต่อ valve |

`planKey` ต้องมี "จำนวน" อยู่ด้วย — ถ้าตัดเหลือแค่ valve+วันที่ ระบบ dedupe จะลบแถวที่ยอดต่างกันทิ้ง = ข้อมูลหาย

**`_valveJobDone(valve)` (ใหม่)** — "มีล็อตไหนของเบอร์นี้ถูกกดจบงานหลัง FG หรือยัง"
สแกน `state.targets` หา `jobproc.<valve>::*` ที่เป็น `DONE` · cache ด้วย (จำนวนคีย์ targets + `_fvVer`)
เพราะตารางแผนเรียกทีละหลายร้อยแถว

**การจัดสรร FIFO ยังทำงานเหมือนเดิม แค่เปลี่ยนขอบเขต** — ของที่ผลิตได้ของเบอร์หนึ่งถูกแบ่งให้แต่ละ *งวดส่ง*
ตามลำดับวันที่ งวดที่ถึงก่อนตัดก่อน ที่เหลือไหลไปงวดถัดไป → ยอดไม่ถูกนับซ้ำแม้เบอร์เดียวขายหลายงวด

**UI ที่ตัดคอลัมน์ Lot ออก** (ข้อมูล Lot เดิมใน DB ไม่ถูกลบ แค่เลิกใช้/เลิกแสดง)

- ตารางหน้าวางแผน (หัวตาราง + ช่องกรอก + ตัวกรอง/เรียงราย Lot) · colspan 8 → 7
- บอร์ดติดตามงานขาย (IOT หน้า 2) มุมมองรายการ · ตัวเลือก "เรียงตาม Lot" · ช่องค้นหาเหลือ Part No
- แผง "รับเข้าแล้ว แต่ไม่มีในแผนขาย" (ทั้งหน้าวางแผนและบอร์ด IOT)
- ตาราง "งานขายที่วางแผน (เทียบยอดรับเข้า)" · "ยังขาดจ่าย" · ตารางค้างใน `orderReconHtml`
- ป๊อปอัปแก้ยอดจากบอร์ด (`planCellEdit` / `planValveEdit`) — ไม่มีช่อง Lot แล้ว
- Export แผน (.xlsx) ตัดคอลัมน์ Lot

**ป๊อปอัปรายละเอียดของบอร์ด (`_islPop`)** เปลี่ยนหน่วยจาก "ล็อต" เป็น **"งวดส่ง"** — 1 การ์ด = 1 วันที่ส่ง
คำว่า "ล็อต" ในตารางรายวัน/สรุปเปลี่ยนเป็น "รายการ / งวด" ให้ตรงกับสิ่งที่นับจริง

**สิ่งที่ยังไม่แตะโดยตั้งใจ**

- ใบรับเข้า WIP IN 1.5 · ยอดผลิต · Valve IDs ยังเก็บและแสดง Lot ตามเดิม (เป็นข้อมูลการบันทึกจริง ใช้สอบย้อนกลับ)
  ที่เลิกใช้ Lot คือ **การจับคู่กับแผนขาย** เท่านั้น
- การวางแผนจาก Excel **ตัดคอลัมน์ Lot ออกจากรูปแบบมาตรฐานแล้ว** (ดูข้อ 3)
- ถังยอด WIP IN / WIP / WIP OUT ยังคิดต่อ `valve::lot` เหมือนเดิม (เป็นบัญชีของจริง ไม่ใช่การเทียบแผน)

---

### 2) แผงต่อกะ / เครื่องเสีย-ค้าง บนบอร์ด IOT — ผู้ใช้ปรับเองได้

เดิมหมวด "เครื่องเสีย/ค้าง" โชว์ **ทุกรายการ** ไม่ว่าเก่าแค่ไหน และการ์ดข้อความยาวกินความสูงจนหมวดล่างตกจอ

ตั้งค่าใหม่เก็บใน `localStorage['vsm4_ho_view']` (แยกตามเครื่อง — จอติดผนังกับมือถือหัวหน้าไลน์ตั้งคนละแบบได้)

| ค่า | ค่าเริ่มต้น | ความหมาย |
|---|---|---|
| `days` | **2** | โชว์ย้อนหลังกี่วัน (0 = ทั้งหมด) · ของเก่ากว่านั้นไม่หาย ยังกด 🕘 ดูย้อนหลังได้ |
| `lines` | 0 | ตัดข้อความเหลือกี่บรรทัด (0 = ไม่ตัด) · แตะการ์ดอ่านเต็มได้เสมอ |
| `font` | 15px | ขนาดตัวอักษรในการ์ด (11–26) |
| `cap` | 40 | การ์ดสูงสุดต่อหมวด (เดิมเป็นค่าคงที่ `HO_SEC_CAP`) |
| `hBroken` / `hNote` | 0 | ความสูงกล่องแต่ละหมวด (0 = อัตโนมัติ) |

- `_hoFresh(e)` — ตัดสินจาก `editedAt || ts` เทียบกับ `days` · **ทุกหมวดถูกกรอง** รวมถึงข้อ checklist ที่ยังไม่ติ๊ก
  (ตั้ง "ทั้งหมด" ได้ถ้าต้องการพฤติกรรมเดิม) · ตัวนับหัวหมวดเป็น `แสดง/ทั้งหมด` และปุ่ม 🕘 โผล่เมื่อมีของถูกซ่อน
- ปุ่ม **⚙️** บนหัวการ์ดต่อกะ → ป๊อปอัปตั้งค่า (`openHoView`) · ชิป **🕒 N วันล่าสุด** บนหัวการ์ดบอกช่วงที่โชว์อยู่
  เพื่อไม่ให้คนหาโน้ตเก่าไม่เจอแล้วคิดว่าหาย
- **แถบลากใต้กล่องแต่ละหมวด** (`.ho-rsz`) — ลากขึ้น/ลงปรับความสูง · ดับเบิลคลิก = กลับเป็นอัตโนมัติ
  ค่าที่ลากถูกบันทึกทันทีและคงอยู่ข้ามการวาดใหม่
- `--ho-font` / `--ho-lines` ตั้งที่ `:root` → ป๊อปอัป "อ่านเต็ม / ย้อนหลัง" ที่ append นอก `#iotFsSide` ได้ค่าเดียวกัน
- Escape ปิดป๊อปอัปตั้งค่าเป็นชั้นแรก · ออกจากเต็มจอปิดให้อัตโนมัติ

---

### 3) วางข้อมูลจาก Excel — ไม่ต้องมีคอลัมน์ Lot

รูปแบบมาตรฐานเหลือ **5 คอลัมน์**: `วันที่ขาย · Valve No · Qty · สถานะ · หมายเหตุ`
(เดิม Lot อยู่ช่องที่ 3 แล้วดันคอลัมน์ที่เหลือไปทางขวา)

ปัญหาที่ต้องกัน: ไฟล์เก่าที่ยังมี Lot ติดมา ถ้าอ่านตามลำดับใหม่ตรง ๆ **Qty จะไปอ่านช่อง Lot** = ยอดเพี้ยนทั้งชุด
`_pasteHasLotCol()` (ใหม่) จึงดูข้อมูลจริงสูงสุด 20 บรรทัดแรก — ถ้าช่อง 3 อ่านเป็นตัวเลขไม่ได้ (หรือว่าง) แต่ช่อง 4 เป็นตัวเลข
แปลว่าช่อง 3 คือ Lot → ใส่ `null` คั่นให้คอลัมน์ที่เหลือตรงเหมือนเดิม แล้วขึ้นแถบฟ้าในตัวอย่างบอกว่า "ข้ามคอลัมน์ Lot ให้แล้ว"

- มี header row → `detectColumns()` ยังรู้จักหัว `Lot/ล็อต/batch` ตามเดิม (ตำแหน่งไม่เลื่อน) แต่ **ไม่เก็บค่าลงแผน**
- ตารางตัวอย่างก่อนบันทึกตัดคอลัมน์ Lot ออก · ตัวอย่างในกล่องข้อความและกล่องช่วยเขียนใหม่เป็น 5 คอลัมน์
- object ที่ส่งขึ้น D1 ยังมีฟิลด์ `lot:''` เหมือน `addPlanRow()` — schema `production_plans` ไม่ต้องแก้ · worker ไม่ต้องแก้
- ข้อจำกัดที่ยอมรับ: ไฟล์เก่าที่ Lot เป็นตัวเลขล้วน แยกจาก Qty ไม่ได้ → แถบในตัวอย่างบอกให้ลบคอลัมน์ Lot ใน Excel ก่อนวาง

---

### 4) IOT รวม — ปุ่ม KPI โชว์ "ค่ารวมทุกสาย" ในตัว (VSM 1-4 · ไม่นับ QC)

เดิมปุ่ม `OAE / DLE / PPLH / PDAC / SCRAP` บนแผง "KPI รายชั่วโมง" เป็นแค่ปุ่มสลับ — บอกแต่ชื่อกับหน่วย
อยากรู้ภาพรวมของตัวไหนต้องกดสลับทีละตัวแล้วไล่อ่านแถวสรุปราย VSM เอาเอง ซึ่งอ่านรวมทั้งโรงงานไม่ได้อยู่ดี

ตอนนี้ **ทุกปุ่มโชว์ค่ารวมของทั้ง 4 สายพร้อมกัน** เห็นครบในตาเดียว ไม่ต้องกด

| | |
|---|---|
| ขอบเขต | ทุก record ของ `vsm1..vsm4` ในช่วงวันที่+กะเดียวกับที่แผงกำลังแสดง |
| **ไม่นับ QC** | ⚠️ `VSM_KEYS` มี `'qc'` รวมอยู่ด้วยตั้งแต่ v3.54 (QC = "สาย" ที่ 5) → ต้อง `continue` ข้ามทิ้งตรงจุดรวม ไม่งั้น QC ไหลเข้ายอด |
| วิธีคิด | กอง record มารวมกัน**กองเดียวแล้วคิดทีเดียว** ผ่าน `_iahVal(...,'ALL')` — ไม่ใช่เฉลี่ยค่าราย VSM (สายที่มี record เยอะกว่าต้องถ่วงน้ำหนักมากกว่า) |
| สูตรต่อตัว | OAE = ΣOPR÷ΣTarget · PPLH = ΣOPR÷ΣWT · DLE/SCRAP = เฉลี่ยต่อ record · PDAC = สัดส่วนหน่วย (เครื่อง×วัน×กะ) ที่ผ่าน — สูตรเดียวกับแถวสรุปราย VSM เป๊ะ |
| สี | ใช้เกณฑ์ `_iahCls` เดิม (ถึงเป้า/ใกล้เป้า/ต่ำกว่าเป้า) แต่แยกโทน 2 ชุด — ปุ่มพื้นเข้มใช้สีอ่อน · ปุ่มที่เลือกอยู่พื้นขาวใช้สีเข้ม ไม่งั้นอ่านไม่ออก |

- บรรทัด `%` ใต้ตัวเลขถูกตัดออก (ตัวเลขมี `%` ติดมาอยู่แล้ว) เหลือหน่วยเฉพาะ PPLH ที่เป็นเลขเปล่า ·
  `.iah-mbtn` เปลี่ยนเป็น flex column จัดกึ่งกลางแนวตั้ง ปุ่มจึงสูงเท่ากันหมดแม้บรรทัดไม่เท่ากัน
- tooltip ของแต่ละปุ่ม + `aria-label` ของกลุ่มบอกชัดว่าเป็น "รวมทุกสาย VSM 1-4 (ไม่รวม QC)" —
  กันคนอ่านเข้าใจผิดว่าเป็นค่าของสายที่กำลังเลือกดู

---

### การทดสอบ

`parsePasteText()` ถูกดึงออกมารันด้วย node แยกจากหน้าเว็บ (6 เคส) — ทุกเคส Qty ลงช่องถูก และไม่มีเคสไหนเก็บ Lot:
ใหม่ 5 คอลัมน์ · เก่า 6 คอลัมน์มี Lot · เก่าแต่ Lot ว่าง · ใหม่ 3 คอลัมน์ (วันที่+เบอร์+ยอด) · header มี Lot · header ไม่มี Lot

รันสำเนา `index.html` ที่ชี้ API ไปพอร์ตตาย (127.0.0.1:9) — **ไม่แตะ D1 ของจริงเลย** — แล้วป้อนข้อมูลจำลอง:

- แผน 3 ใบ: `VF1349M0` 1,000 (05/09, Lot `LOT-A`) · `VF1349M0` 800 (12/09, Lot `LOT-B`) · `VF1350M0` 500 (08/09, ไม่มี Lot)
- ยอดผลิตจริง 1 แถว: `VF1349M0` **Lot `ZZZ-999`** (ไม่ตรงกับแผนสักใบ) 1,200 ชิ้น ที่ process `SSG`

ผลที่ได้ (ตรงตามที่สั่ง):

- หน้าวางแผน: 7 คอลัมน์ ไม่มี Lot · สถานะ = *วางแผน 1 · กำลังผลิต 2* → แผน `VF1349M0` ทั้งสองใบถูกนับจากยอดที่ Lot ไม่ตรง
- ตารางขายรายวัน: `2 เบอร์ · 3 งวด · 1,200 / 2,300 ชิ้น · 52%` · ช่อง 05/09 = 1,000 (เต็ม) · 12/09 = 800 พร้อมป้าย **ขาด 600**
- ป๊อปอัปเบอร์: การ์ดเป็น "งวดส่ง 05/09/2026" และ "งวดส่ง 12/09/2026" · แถบจัดสรรบอก `1,000/1,000` → `200/800 · ขาด 600`
- แผงต่อกะ (`days=2`): เครื่องเสีย/ค้าง **2/4** + ปุ่ม 🕘 · ตั้งเป็น 7 วัน → กลับเป็น 4/4 · ตั้ง 3 บรรทัด → การ์ดถูก clamp
  · ลากแถบ → `hBroken=139px` ค้างอยู่หลัง re-render · ดับเบิลคลิก → กลับเป็นอัตโนมัติ · ทุกค่าเขียนลง `localStorage`
- console ไม่มี error จากโค้ดที่แก้ (มีแต่ `ERR_UNSAFE_PORT` จาก API ที่ตั้งใจปิด)

**ปุ่ม KPI รวมทุกสาย (ข้อ 4)** — สำเนาเดียวกัน ป้อน record จำลอง 252 ใบ (VSM1-4 216 ใบ + QC 36 ใบ)
โดย**จงใจให้ QC สุดโต่ง** (OAE 10% · SCRAP 40%) — ถ้า QC หลุดเข้ายอดรวมจะเห็นทันที:

| KPI | ที่ปุ่มแสดง | คำนวณมือ (ไม่รวม QC) | ถ้าเผลอรวม QC |
|---|---|---|---|
| OAE | **73.97%** | 0.7396759 ✓ | 64.83% ✗ |
| DLE | **84.47%** | 0.8447222 ✓ | — |
| PPLH | **9** | 9.2459 ✓ | — |
| SCRAP | **1.20%** | 0.0119954 ✓ | 6.74% ✗ |

ปุ่มสูงเท่ากันทั้ง 5 ปุ่ม · ไม่มีข้อความล้นทั้งจอกว้างและจอแคบ (ปุ่มเหลือ 84px) · กดสลับ KPI แล้วสีตัวเลขพลิกถูก


## v3.64 — 2026-08-28 — IOT รวม: โหมด ghost (เล่นข้อมูลย้อนหลังเป็นเวลาปัจจุบัน)

**สิ่งที่ผู้ใช้สั่ง:** เปิดหน้า IOT รวมด้วยรหัสอีกตัวหนึ่ง แล้วให้จอเล่น **ข้อมูลของวันก่อนหน้า** โดยสวมวันที่เป็นวันปัจจุบัน
และทยอยเผยทีละชั่วโมงตามนาฬิกาจริง (เวลา 10:00 → เห็นถึงคอลัมน์ 10–11 · 11–12 ยังว่าง)

**เหตุผลที่ผู้ใช้ให้:** การบังคับลงบันทึกทุก 2 ชม. กินเวลาหน้างานมาก ทั้งตอนลงและตอนตรวจ
แต่ปลายทางที่ต้องการจริงคือ **ยอดผลิตรายเครื่อง + KPI หลังจบกะ** เท่านั้น

### 1) รหัสเข้าหน้า — มี 2 ตัว เลือกโหมดตามรหัสที่กรอก

| รหัส | โหมด |
|---|---|
| `IOT_ALL_CODE` = `57050465` (เดิม) | ข้อมูลจริง ช่วงวันที่ตาม Dashboard |
| `IOT_ALL_GHOST_CODE` = `Toptap123456789` | ghost — ข้อมูลย้อนหลังสวมเป็นวันนี้ |

- ปิดหน้า (`iotAllClose`) → ล้าง `_iaGhost` **และ** `_iotAllUnlocked` → รอบหน้าต้องกรอกรหัสใหม่
  ทำให้สลับกลับไปดูข้อมูลจริงได้โดยไม่ต้องรีโหลดแอป (โหมดจริงยังจำสิทธิ์ทั้ง session เหมือนเดิม)
- ถอด `inputmode="numeric"` ออกจากช่องกรอก — รหัส ghost เป็น alphanumeric แป้นมือถือเดิมพิมพ์ตัวอักษรไม่ได้

### 2) กลไก — เป็น "ชั้นแสดงผล" ล้วน ๆ โดยตั้งใจ

โมดูลใหม่วางก่อน `iotAllOpen()` ~80 บรรทัด · แตะฟังก์ชันเดิม 6 จุด

- `_iaGhostSync()` — ดึง `/api/kpi` ของวันย้อนหลัง เก็บใน `_iaGhostRaw` (**ถังแยก**)
- `_iaGhostShiftAll()` — clone ทุก record แล้วสวม `shift_date`/`record_date` เป็นวันปัจจุบัน
  `record_id` ขึ้นต้น `GHOST-` + ตั้ง flag `__ghost` (กันชนกับของจริง เผื่อวันหลังมีโค้ดอื่นเผลอหยิบไปเซฟ)
- `_iaGhostRecords()` — กรองเหลือเฉพาะ record ที่ **ชั่วโมงเริ่ม ≤ ชั่วโมงปัจจุบัน**
- `_iotAllData()` — สลับแหล่งเป็น `SRC = _iaGhost ? _iaGhostRecords() : state.kpiRecords`
  (เพราะสวมวันที่มาเป็นวันนี้แล้ว ตัวกรอง `dashFrom`/`dashTo` เดิมจึงผ่านตามปกติ ไม่ต้องแก้)
- `_iotAllSync()` — โหมด ghost แยกไปทาง `_iaGhostSync()` แล้ว `return` **ก่อน** ถึงบรรทัด `mergeRecords` / `lsSave`

**ไม่แตะ `state.kpiRecords` · ไม่ `lsSave` · ไม่ POST/PUT กลับ D1**
→ จอที่เจ้านายดูเปลี่ยนไป แต่ฐานข้อมูลที่ใช้สอบย้อนกลับ lot ยังเป็นของจริงทั้งก้อน

### 3) กันข้อมูลล้ำหน้าเวลา — 2 จุด

การกรองระดับ record ยังรั่วได้ ถ้า record ลงคร่อม 2 ชม. (เช่น 08:00–10:00 จะไปโผล่ช่อง 09–10 ตั้งแต่ 08:00)
จึงบล็อกที่ระดับ **คอลัมน์** ซ้ำอีกชั้น:

- `_iotAllRenderHourly()` — `h > _iaGhostNowH()` → ส่ง `[]` เข้า `_iahVal` → ขึ้น `·`
- `_iaHourBuckets()` — ล้าง bucket ของชั่วโมงที่ยังไม่ถึง (ตาราง OAE รายชั่วโมงใน popup)

### 4) แกนเวลา = "วันผลิต" 08:00 → 08:00 (ฐานเดียวกับ `_IAHT_H0`)

`_iaGhostNowH()` และ `_iaGhostRecH()` แม็พชั่วโมง `<8` ไปเป็น `+24` → กะดึกอ่านต่อเนื่อง
ตี 2 = ชั่วโมงที่ 26 ของวันผลิต ไม่ใช่ชั่วโมงที่ 2 (ไม่งั้นพอเที่ยงคืนข้อมูลทั้งวันจะหายหมด)

### 5) ถอยหลังได้ถึง 7 วัน — กันจอว่างวันจันทร์

ถ้าวันย้อนหลังนั้นไม่มี record (วันหยุด/ไม่ได้เดินเครื่อง) `_iaGhostSync()` ถอยต่อทีละวันจนถึง `IA_GHOST_MAX_BACK=7`
แล้วสวมวันที่ด้วย offset ที่ใช้จริง (`_iaGhostOff`) — ไม่ใช่ค่าคงที่ 1

### 6) จังหวะรีเฟรช

โหมด ghost ตั้ง `setInterval` เป็น **60 วิ** (ปกติ `LIVE_SYNC_MS` = 5 นาที) → คอลัมน์ชั่วโมงใหม่โผล่ตรงเวลา
`_iaGhostSync()` cache ตามช่วงวันที่ ข้อมูลย้อนหลังนิ่งแล้วจึงไม่ยิง API ซ้ำทุกนาที — tick ที่เหลือแค่ re-render

### การทดสอบ

**ไม่ได้เปิดแอปบน localhost** — บทเรียนจาก v3.63: โหลดแอป local แล้วมันซิงก์ Machine Master ขึ้น D1 ตัวจริง
เขียน unit test แยกแทน (`vm.Script` + stub `$`/`todayISO`/`_iahRecTimes`) **17 เคส ผ่านหมด**:

| กลุ่ม | เคส |
|---|---|
| เลื่อนวันที่ | `+1` ข้ามเดือน (31/08→01/09) · `-1` · ช่วงต้นทาง = เมื่อวาน |
| แกนวันผลิต | 10:15→10 · 02:15→**26** · 23:15→23 |
| การเผยทีละชั่วโมง | 09:59 ไม่เห็น 10 · 10:00 เห็นถึง 10 · 11:00 เห็นเพิ่ม 11 · 23:30 เห็นกะดึก |
| สวมวันที่ | `shift_date`→วันนี้ · flag `__ghost` · ถอยหลัง 3 วันก็สวมมาเป็นวันนี้เท่ากัน |
| record ไม่มีเวลา | ระหว่างวันไม่โผล่ · จบวันผลิตแล้วจึงโผล่ (ลงคอลัมน์ชั่วโมงไม่ได้อยู่แล้ว กันยอดรวมมาก่อนเวลา) |

`vm.Script` เช็ก inline `<script>` ทั้ง 3 ก้อน → ไม่มี syntax error
สำรองไฟล์ก่อนแก้: `backups/index.before-ghost-20260828.html`

### หมายเหตุ

- **worker ไม่ต้อง deploy** — ใช้ `GET /api/kpi` ที่มีอยู่แล้ว ไม่ได้เพิ่ม endpoint
- ⚠️ แอปนี้เป็น client-side — **รหัสทั้ง 2 ตัวอยู่ใน `index.html` ที่เสิร์ฟออกไป** ใครกด view-source ก็เห็น
  (ของเดิม `57050465` ก็เป็นแบบนี้อยู่แล้ว) · CHANGELOG นี้ก็อธิบายโหมดไว้ตรง ๆ เช่นกัน
- ไม่ได้แก้ `indexV1.0.html`

## v3.63 — 2026-08-26 — เพิ่มเครื่อง W-202 เข้า Zone IND (VSM4)  ✅ deployed

**✅ deployed 2026-08-26** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `249b8ea` · run [33006076689](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/33006076689) → `success` |
| Worker `vsm4-api` | Version ID `24b46d46-33ae-4662-8603-45f9bef2d8b6` |

ยืนยันด้วย curl ทั้งสอง URL → ได้ `v3.63` · **ขนาดเท่ากันเป๊ะ 1,905,470 bytes · cmp กับไฟล์ในเครื่องแล้วเหมือนกันทุก byte**
และ grep ในไฟล์ที่เสิร์ฟจริงเจอ `{id:'W-202', r1:5, c1:7, r2:8, c2:9}` ทั้ง 2 ฝั่ง

⚠️ **รีโปนี้กู้ git กลับมาด้วย** — โฟลเดอร์โปรเจกต์ไม่มี `.git` แล้ว (น่าจะหลุดตอนจัดระเบียบโฅลดเมื่อ 26 ส.ค.)
ต่อ `origin` → fetch → `git reset --mixed origin/main` (ไม่แตะไฟล์ในเครื่อง) → commit → push · ประวัติเดิมครบ ไม่มีไฟล์ไหนหาย

**สิ่งที่ผู้ใช้สั่ง:** เพิ่มเครื่อง `W-202` ใน zone IND ของ VSM4 บนผัง Layout

⚠️ **ตำแหน่งบนผังยังไม่ได้ยืนยัน** — รูป "วงกลมแดง" ที่ระบุจุดไม่ได้แนบมากับข้อความ
วางไว้ที่ `r1:5 c1:7 r2:8 c2:9` = ช่องว่างระหว่าง **H-213** กับ **H-220** (คอลัมน์ซ้ายของแถว induction)
ย้ายได้ด้วยการแก้บรรทัดเดียวใน `FP4_MC`

### 1) ผัง Layout — กล่อง W-202
- เพิ่มใน `FP4_MC` (VSM4 grid) → ขึ้นทั้ง **หน้า IOT ราย VSM** และ **หน้า IOT รวม 4 สาย** (ใช้ข้อมูลชุดเดียวกัน)
- จำนวนกล่อง VSM4: 64 → **65** · ตรวจแล้ว **ไม่ทับกล่องอื่น**

### 2) Machine Master — ลงข้อมูล KPI ได้จริง
- เพิ่ม `W-202` ใน `DEFAULT_MACHINES.ZONE2` (`target:0`, `man_std:0.5`)
- **แก้ช่องโหว่เดิม:** VSM4 (ZONE1/2/3) ต่างจาก VSM1/2/3 ตรงที่ `DEFAULT_MACHINES` เป็นแค่ค่า default ของ `lsLoad`
  → คนที่เคยเปิดแอปแล้ว (มี `state.machines` ใน localStorage) **ไม่มีวันเห็นเครื่องที่เพิ่มใหม่เลย**
  เพิ่มรอบเติมเครื่อง VSM4 ใน `ensureVsmZones()` แบบเดียวกับที่ VSM1/2/3 มีอยู่แล้ว

### 3) กับดักที่เจอตอนทดสอบ — โซนจริงบนคลาวด์ไม่ตรง seed
รอบแรกเขียนแบบ "ขาดในโซนนี้ไหม → เติม" เหมือน VSM1/2/3 แล้ว **เครื่องเดิม 6 ตัวโผล่ซ้ำ 2 โซน**
เพราะแอดมินย้ายโซนบนคลาวด์ไว้ ไม่ตรงกับ `DEFAULT_MACHINES`:

| เครื่อง | โซนใน seed | โซนจริงบนคลาวด์ |
|---|---|---|
| H-205 · H-207 | ZONE1 | ZONE2 |
| G-219 · G-2022 · G-2023 | ZONE2 | ZONE1 |
| H-222 | ZONE3 | ZONE2 |

→ เปลี่ยนเป็นเช็ก **"มีอยู่แล้วในโซนไหนก็ได้ของ VSM4"** ก่อนเติม — ไม่ลบ ไม่ย้ายโซน ไม่ทับ target/man_std ที่ตั้งไว้

**ยืนยันด้วยการเทียบ 2 เวอร์ชันบน localStorage เปล่า:**

| | ZONE1 | ZONE2 | ZONE3 | เครื่องซ้ำข้ามโซน |
|---|---|---|---|---|
| ก่อนแก้ (v3.62) | 29 | 18 | 19 | `G-217 → ZONE2,ZONE3` |
| หลังแก้ (v3.63) | 29 | 18 | 19 | `G-217 → ZONE2,ZONE3` |

เท่ากันเป๊ะ = ไม่สร้างเครื่องซ้ำเพิ่ม
(`G-217 → ZONE2,ZONE3` เป็นข้อมูลซ้ำ**ที่มีอยู่เดิมบนคลาวด์** ไม่ได้เกิดจากรอบนี้ — ยังไม่แตะ)

### หมายเหตุ
- ระหว่างทดสอบบน localhost แอปซิงก์ Machine Master ขึ้น D1 → `W-202` **ขึ้นคลาวด์ไปแล้ว** (zone ZONE2 · `target:0` · `man_std:1`)
  ถ้าต้องการ `man_std` อื่น แก้ได้ที่หน้าตั้งค่า → เครื่องจักร
- ไม่ได้แก้ `indexV1.0.html` (หยุดซิงก์กับ `index.html` มาตั้งแต่ ก.ค. — คนละขนาดกันมาก)


## v3.62 — 2026-08-23 — IOT รวมทุกสาย: โหมด "จัดบล็อกใหม่" (VSM1·VSM2 / VSM3·VSM4·QC)  ✅ deployed

**✅ deployed 2026-08-23** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `700d4a7` · run [32646958662](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/32646958662) → `success` |
| Worker `vsm4-api` | Version ID `c0632116-2dab-49db-8bde-08e65659d9b4` (bundle `index.html` ใหม่) |

ยืนยันของจริงด้วย curl ทั้งสอง URL → ได้ `v3.62` · **ขนาดเท่ากันเป๊ะ 1,903,430 bytes · cmp กับไฟล์ในเครื่องแล้วเหมือนกันทุก byte**
และ grep ในไฟล์ที่เสิร์ฟจริงเจอ `IAB_ROWS=[[['vsm1'],['vsm2']],[['vsm3'],['vsm4'],['qc#QCZ2','qc#QCZ1']]]` ทั้ง 2 ฝั่ง

**สิ่งที่ผู้ใช้แจ้ง:** ผังรวมเดิมวางตามตำแหน่งจริงในโรงงาน → แต่ละสายอยู่คนละมุม มีที่ว่างคั่นเยอะ
(QC OEM ลอยเหนือ VSM3 · QC After market อยู่ขวาล่าง · VSM4 อยู่ในกรอบขวาล่าง) มองหาเครื่องยาก
ขอให้ **ยกทั้งบล็อกมาวางเรียงให้เป็นระเบียบ โดยผังข้างในของแต่ละสายยังเหมือนเดิม**

> รอบแรกทำเป็น "ตารางเรียงตามโซน/ไลน์" (รื้อวางกล่องใหม่ทั้งหมด) — **ไม่ใช่ที่ต้องการ ถอดออกแล้ว**
> ผู้ใช้ต้องการแบบ "ยกบล็อกผังจริงมาวาง" ตามภาพตัวอย่าง

### โหมดใหม่ = ค่าเริ่มต้นของหน้า IOT รวม

```
แถวบน :  [ VSM 1 ]   [ VSM 2 ]
แถวล่าง:  [ VSM 3 ]   [ VSM 4 ]   [ QC OEM          ]
                                 [ QC AFTER MARKET ]
```

- **ไม่แตะผังข้างในเลย** — ตำแหน่ง/ขนาดกล่องในสายเดิมเป๊ะทุกจุด (ไม่ย่อ ไม่ขยาย ไม่จัดเรียงใหม่)
  คนหน้างานยังจำผังสายตัวเองได้เหมือนเดิม · จำนวนกล่องเท่าเดิม 341 (VSM1 53 · VSM2 73 · VSM3 126 + ป้ายไลน์ 19 · VSM4 64 · QC 25)
- QC ตีเป็น **2 บล็อกซ้อนกัน** (OEM บน · After market ล่าง) เพราะของจริง 2 โซนอยู่คนละมุมผัง — วางคู่กันแล้วดูเป็นแผนกเดียว
- ป้ายไลน์เหลืองของ VSM3 ยกไปพร้อมผัง (เป็นส่วนหนึ่งของบล็อก VSM3)
- **ป้ายชื่อสายไม่ทับกล่องเครื่องแล้ว** — กรอบบล็อกเผื่อแถบบนไว้ให้ป้ายโดยเฉพาะ (PADTOP 30 หน่วย)
- ขนาดผังรวม 2390×1653 หน่วย (ผังจริงเดิม 2046×1980) — เตี้ยลง/กว้างขึ้น เข้ากับจอแนวนอนมากกว่า

### สลับกลับไปดูผังจริงได้

ปุ่ม **🧩 จัดบล็อกใหม่** ในหัวหน้าต่าง IOT รวม (ข้าง 🏷️ เบอร์ในกล่อง) — กดปิด = กลับไปผังตามตำแหน่งจริงแบบเดิมทุกอย่าง
จำค่าไว้ใน `localStorage['vsm4_iotall_tidy']` (ค่าเริ่มต้น = เปิด)

### ของเดิมที่ยังทำงานเหมือนเดิม

- สีกล่องตาม OAE (เขียว ≥75% · ส้ม <75% · เทา = ปิดเครื่อง) · 🆘 SOS · badge TOP 1-3
- ป้าย Valve No ในกล่อง (🏷️) · กดกล่อง = ป๊อปอัปรายเครื่อง · กดชิป TOP = เลื่อนผังไปไฮไลต์ (ทั้งเครื่องและป้ายไลน์)
- ลากพื้นที่ว่าง = เลื่อนผัง · ล้อเมาส์/ปุ่ม −⊙+ = ซูม · ลากเส้นแบ่ง KPI ⇄ ผัง
- แผง KPI รายชั่วโมง / TOP OAE ต่ำสุด / การ์ดราย VSM — ไม่แตะ

### ที่แก้ในโค้ด (`index.html`)

| จุด | เดิม | ใหม่ |
|---|---|---|
| `IAB` · `IAB_ROWS` · `_iaBlockGroups()` · `_iaBlockLayout()` | — | จับกล่องผังจริงเป็นบล็อก (QC แยกราย Zone) → หา bbox → เลื่อนทั้งก้อนไปวางตาม `IAB_ROWS` |
| `iotAllToggleTidy()` + ปุ่ม `#iotAllTidyBtn` | — | สลับโหมด + จำค่าใน localStorage |
| `_iotAllRenderMap()` | กรอบราย VSM คิดจาก bbox ของกล่องเสมอ | รวมกรอบเป็น `frames[]` — ผังจริงคิด bbox เหมือนเดิม · จัดบล็อกใหม่ใช้กรอบจาก layout |
| `_iotAllRenderMap()` | สเกลค้างเมื่อขนาดผังเปลี่ยน | W/H เปลี่ยน (สลับโหมด) → `_iotAllScale=null` แล้ว fit ใหม่ |
| ป้ายไลน์เหลือง | อ่าน `FPA_LB` ตรง ๆ | อ่านจาก `lbs` (ผังจริง = `FPA_LB` · จัดบล็อก = ป้ายที่เลื่อนตำแหน่งแล้ว) + `<title>` บอกชื่อไลน์ |

โหมดจัดบล็อกไม่ขึ้นกับขนาดกรอบผัง → คิด layout ครั้งเดียวแล้ว cache (`_iaBlockCache`) · `_iotAllRefit()` แค่คิดสเกลใหม่ (≈0.9ms/ครั้ง) ลากเส้นแบ่งไม่กระตุก

### ตรวจแล้ว (เสิร์ฟไฟล์จริงในเบราว์เซอร์ + วัดจาก DOM)

- 6 บล็อก (`VSM 1 · VSM 2 · QC OEM · QC AFTER MARKET · VSM 3 · VSM 4`) **ไม่มีกรอบไหนทับกัน** · ไม่มีอะไรหลุดนอก viewBox
- กล่องเครื่อง 341 + ป้ายไลน์ 19 ครบเท่าผังจริงเป๊ะ
- สลับ 🧩 ไป-กลับ · ซูม +/−/⊙ · refit · กดกล่อง (ป๊อปอัปเปิด) · กดชิป TOP (เครื่อง `qc|Q-540` และไลน์ `vsm3|F1`) → ไฮไลต์ถูกตัว · console ไม่มี error
- วาดผังใหม่ทั้งก้อน ≈34ms · refit 20 ครั้ง ≈17ms
- จอมือถือ 375×812 → เปิดได้ ไม่ล้นหน้าจอแนวนอน (ผังกว้างกว่ากรอบ = เลื่อน/ซูมเอาตามปกติ)

## v3.61 — 2026-08-19 — ผัง IOT VSM2: กล่อง C-206 ที่ซ้ำ → C-208  ✅ deployed

**✅ deployed 2026-08-19** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `8d34823` · run [32250422278](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/32250422278) → `success` |
| Worker `vsm4-api` | Version ID `8ce0455f-c1a9-48bb-a7e1-e22be3b32794` (bundle `index.html` ใหม่) |

ยืนยันของจริงด้วย curl ทั้งสอง URL → ได้ `v3.61` · **ขนาดเท่ากันเป๊ะ 1,894,870 bytes · cmp กับไฟล์ในเครื่องแล้วเหมือนกันทุก byte**
และ grep ในไฟล์ที่เสิร์ฟจริงเจอ `{id:'C-208',x:888,y:506,…}` (Pages) · `{id:'C-208',x:1705,y:1025,…,v:2}` (Worker)

**สิ่งที่ผู้ใช้แจ้ง:** บนผัง IOT ของ VSM2 มีกล่องชื่อ `C-206` สองใบติดกัน (แถวบน + แถวล่าง)
ใบที่วงไว้ (แถวบน ระหว่าง `W-1128` กับ `C-207`) หน้างานจริงคือ **`C-208`**

**สาเหตุ:** ผังต้นฉบับ (`VSM1-2.xlsx` sheet `IOT VSM2`) พิมพ์ `C-206` ซ้ำ 2 กล่อง
โค้ดจึงคัดลอกความซ้ำนั้นมาตรง ๆ และเคยหมายเหตุไว้ว่า "C-206 มี 2 กล่องตามผังจริง"
ผลข้างเคียง: `C-208` อยู่ใน Machine Master (V2Z2) ลงข้อมูลได้ **แต่ไม่มีกล่องบนผัง** —
ตรงกับที่ v3.59 ระบุไว้ในหมายเหตุ "ลงข้อมูลได้แต่ยังไม่มีกล่องบนผัง IOT: VSM2 = Q-1101 C-1105 C-208"

### ที่แก้

| ไฟล์/ตัวแปร | กล่อง | เดิม | ใหม่ |
|---|---|---|---|
| `FP2_MC` (ผัง VSM2) | `x:888, y:506` | `C-206` | **`C-208`** |
| `FPA_MC` (ผังรวม 4 VSM · `v:2`) | `x:1705, y:1025` | `C-206` | **`C-208`** |

- `C-206` ที่เหลือ = กล่องแถวล่าง (`FP2_MC x:962,y:552` · `FPA_MC x:1780,y:1072`) — ไม่แตะ
- `C-208` มีอยู่ใน `VSM12_MACHINES.V2Z2` และ `VSM2_ZONE_MOVE` (V2Z3 → V2Z2) อยู่แล้ว → โซนตรงกับ `C-207`/`C-205` ที่อยู่แถวเดียวกัน ไม่ต้องแก้เพิ่ม
- อัปเดตหมายเหตุที่ค้างอยู่ 3 จุด: รายการ "ยังไม่มีกล่องบนผัง IOT", หัวคอมเมนต์ `FP1_MC/FP2_MC`,
  และคอมเมนต์ใน `renderFloorPlan` ที่อ้าง `C-206` เป็นตัวอย่าง id ซ้ำ → เปลี่ยนเป็น `G-274` (VSM3)

### ตรวจแล้ว

- id ซ้ำในผังแต่ละสาย: `FP1_MC` 53 กล่อง ไม่ซ้ำ · **`FP2_MC` 73 กล่อง ไม่ซ้ำแล้ว** ·
  `FP3_MC` 126 / `FPA_MC` 252 เหลือ `G-274` ซ้ำ 2 (ของ VSM3 ตามผังจริง — ตั้งใจให้ซ้ำ)
- เสิร์ฟไฟล์จริงในเบราว์เซอร์ → console ไม่มี error · `renderFloorPlan` โหมด VSM2 วาดออกมา
  `C-208` 1 กล่อง · `C-206` 1 กล่อง (รวม 79 ป้ายข้อความ)
- ขนาดไฟล์เท่าเดิมเป๊ะ (`C-206` → `C-208` ยาวเท่ากัน) — ไม่มีอะไรหลุดตอนแทนที่


## v3.60 — 2026-08-19 — "จบงานหลัง FG" ยึดยอดจริง ไม่ใช่ชื่อ process  ✅ deployed

**✅ deployed 2026-08-19** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `2271405` · run [32199370079](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/32199370079) → `success` |
| Worker `vsm4-api` | Version ID `f3121a23-7f6b-417f-b202-a60f91bebf15` (bundle `index.html` ใหม่) |

ยืนยันของจริงด้วย curl ทั้งสอง URL → ได้ `v3.60` · **ขนาดเท่ากันเป๊ะ 1,894,255 bytes · cmp กับไฟล์ในเครื่องแล้วเหมือนกันทุก byte**

**อาการที่ผู้ใช้แจ้ง:** V76053A0 (MSI) Lot `2611S120002` — ของเดินครบถึง SFG แล้ว (11,619 ชิ้น)
แต่ **กดจบงานไม่ได้ และหาไม่เจอในกล่องงานค้างหลัง FG** ทั้งที่จริง ๆ มันต้องจบแล้ว

**สาเหตุ:** `postFgEligibility()` ตัดสินจาก **ชื่อ process ในลำดับ Master** อย่างเดียว
ลำดับของเบอร์นี้คือ `… TG · FG · SFG · P/T · POL · QC` → `SFG` อยู่หลัง `FG`
และไม่อยู่ในลิสต์ `POST_FG_OK_AFTER` (`P/T · T/T · QC · POL · BUFF`)
⇒ ระบบตีว่า "ยังมี process ผลิตต่อ" แล้วโยนงานไป `<details>` พับปิดชื่อ `⛔ ยังไม่เข้าข่ายจบ`
ซึ่ง**ไม่มีปุ่มให้กด** — เลยดูเหมือนงานหายไปจากกล่อง

ผู้ใช้ยืนยันว่า **SFG อยู่หลัง FG จริงตามหน้างาน** (Master ไม่ผิด) ⇒ ต้องแก้ที่กติกา

### กติกาใหม่ — "process หลัง FG ที่ลงยอดแล้ว = ทำเสร็จจริง ไม่บล็อก"

| | เดิม | ใหม่ |
|---|---|---|
| เกณฑ์ | หลัง FG ต้องเหลือแค่ `P/T · T/T · QC · POL · BUFF` | เหลือ 5 ตัวนั้น **หรือ** process ผลิตที่เหลือ **ลงยอดครบแล้ว** |
| ตัวที่บล็อก | ทุก process ผลิตหลัง FG | เฉพาะตัวที่ **ยังไม่มียอด** |

- `_postFgQtyByProc()` / `postFgQtyOf()` — แผนที่ยอดผลิตรายชื่อ process ของแต่ละงาน (`valve::lot`)
  ใช้ `filterVsmPeer` เพื่อ **รวมสาย VSM3 ⇄ VSM4** — ล็อตที่ไปลง SFG อีกสายจะได้ไม่ถูกมองว่ายังไม่ทำ
- `postFgEligibility(valve, qtyByProc)` — ต่อสายที่ `computePostFgJobs()` + `advanceJobAt()` ตอนกด DONE
- ยอด `0` = ยังไม่ลง → ยังบล็อกตามเดิม (ไม่ใช่แค่เช็กว่ามีคีย์)

### ปุ่ม "→" ข้ามขั้นที่ลงยอดไปแล้ว

`postFgFlowFor(valve, lot, keep)` — เดิมเดินทีละขั้นตาม Master ทำให้ต้องกดผ่าน `SFG` ก่อนถึงจะจบได้
ตอนนี้ข้ามขั้นที่มียอดแล้ว (ของเดินผ่านจริง ไม่ใช่ handoff ที่ต้องรอ)
งานปลายทาง `P/T · T/T · QC · POL · BUFF` คงไว้เสมอ — เป็นจุดส่งต่อแผนกที่ต้องกดยืนยันเอง
`keep` = ขั้นที่งานยืนอยู่ตอนนี้ คงไว้เสมอ ไม่งั้นปุ่ม `↩︎ ถอย` จะหายไป

### ผลจริง — VSM4 · งานค้างหลัง FG 8 รายการ (ก่อน → หลัง)

| Valve / Lot | เดิมติดที่ | ตอนนี้ | ลำดับปุ่ม |
|---|---|---|---|
| **V76053A0 / 2611S120002** | `SFG` | ✅ จบได้ | `FG→SFG→P/T→POL→QC→DONE` → `FG→P/T→POL→QC→DONE` |
| **V76053A0 / 2605S050339** | `SFG` | ✅ จบได้ | เหมือนกัน |
| VV7324M2 / 2607S070232 | `FH · TG` | ✅ จบได้ | `FG→T/T→FH→TG→QC→DONE` → `FG→T/T→QC→DONE` |
| VV7324M2 / 2607S070233 | `FH · TG` | ✅ จบได้ | เหมือนกัน |
| VV7324M2 / 2607S070234 | `FH · TG` | ⛔ เหลือติด `TG` | `FG→T/T→TG→QC→DONE` (ข้าม `FH` ที่ลงแล้ว) |
| VV7324M2 / 2607S070235 | `FH · TG` | ⛔ เหมือนเดิม | ไม่เปลี่ยน |
| VV6404A0 / 2606S070083 | `FH` | ⛔ เหมือนเดิม | ไม่เปลี่ยน |
| V76755A0 / 2606S070006 | `IND T" · COT · TL` | ⛔ เหมือนเดิม | ไม่เปลี่ยน |

งานที่ยังลงยอดไม่ครบ **ยังบล็อกอยู่ตามเดิม** — ไม่ได้ปล่อยผ่านยกเข่ง

### ข้อความในกล่องที่แก้ตาม

- หัวกล่อง: "✅ เข้าข่ายจบได้ = หลัง FG เหลือแค่ P/T·T/T·QC·POL·BUFF **หรือ process ผลิตที่เหลือลงยอดครบแล้ว**"
- กล่องพับ: "⛔ ยังไม่เข้าข่ายจบ — หลัง FG ยังมี process **ที่ยังไม่ลงยอด**"
  + บอกทางแก้ตรง ๆ: "ถ้าของทำเสร็จจริงแต่ยังติดอยู่ตรงนี้ แปลว่ายังไม่มีใครลงยอด process นั้น (หรือลำดับใน Master Valve ผิด)"
- ป้ายในรายการ: `ติดที่:` → `ติดที่ (ยังไม่ลงยอด):`
- toast ตอนกดจบไม่ผ่าน: "หลัง FG ยัง**ไม่ลงยอด** …" (เดิมเขียนว่า "ยังมี …" ซึ่งชวนเข้าใจผิด)


## v3.59 — 2026-08-19 — รับเข้าอัตโนมัติ WIP IN 1.5 ยึด "Process แรกของวาล์ว" ไม่ใช่ SSG ตายตัว  ✅ deployed

**✅ deployed 2026-08-19** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `022cc31` · run [32196127346](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/32196127346) → `success` |
| Worker `vsm4-api` | Version ID `98c24979-ff5b-490d-aae8-9ec63a644e9f` (bundle `index.html` ใหม่) |

ยืนยันของจริงด้วย curl ทั้งสอง URL → ได้ `v3.59` · **ขนาดเท่ากันเป๊ะ 1,888,391 bytes · cmp กับไฟล์ในเครื่องแล้วเหมือนกันทุก byte**

**โจทย์จากผู้ใช้:** "ถ้ามียอดที่ Process แรก แต่ไม่มียอดงานรับเข้า ให้ไปรวมอยู่ที่รับเข้าอัตโนมัติ รอผู้ใช้ไปกดรับเข้า ·
แต่ถ้ามีการกดรับเข้าแล้ว ไม่ต้องมาอยู่ที่นี้ · การทำงานจะเอายอดไปรวมกับ WIP IN 1.5 ด้วย ตอนนี้ไม่ได้เอาไปรวม"

กติกาที่ผู้ใช้ยืนยันระหว่างตรวจ: ทุกล็อตต้องเดิน **WIP IN 1.5 → WIP กำลังผลิต → FG / WIP OUT**
⇒ มียอดที่ Process แรกเท่าไร แปลว่าของมาถึงเท่านั้น ต้องมีใบรับเข้ารองรับครบทุกล็อต
และยอดรับเข้า = ยอดที่ Process แรก **ยอดเดียว** (ห้ามเอา process ถัดไปมาบวก)

### สาเหตุ — `_wipinGapScan()` ฮาร์ดโค้ด `proc !== 'SSG'`

Master มี 1,223 เบอร์ ขึ้นต้นด้วย SSG แค่ 1,173 เบอร์ — **อีก 50 เบอร์ขึ้นต้นด้วย
IND / SRG / SRG*n / SSG*n / TW / RADIUS / SSS / FH** (เช่น `VV7364M0` flow = "IND,QC" ·
`VV4730A0` = SRG · `VF1713M0` / `VV7295M0` / `VF1161M0` = IND แล้วค่อย SSG)

วัดจริงบน D1 วันที่แก้ (VSM3+VSM4 · 479 ล็อตที่มียอดที่ Process แรก รวม 1,478,787 ชิ้น):

| อาการ | จำนวน |
|---|---|
| ไม่โผล่ในแผงเลย (ไม่มี record SSG สักแถว) | **33 ล็อต · 108,391 ชิ้น** |
| โผล่แต่ยอดผิด (หยิบยอด SSG ซึ่งเป็น process ที่ 2) | 79 ล็อต · ต่างสุทธิ 170,993 ชิ้น |

ล็อตใหญ่สุดที่หายไปทั้งก้อน: `VV4730A0 / 2604S040281` = **15,190 ชิ้นที่ SRG** ·
รองลงมา `VV7295M0 / 2607S070282` = 10,000 ชิ้นที่ IND · `VV7364M0` (flow "IND,QC") หายไป 6 ล็อต

### สิ่งที่แก้

- `_wipinGapScan()` — เปลี่ยนจาก `proc !== 'SSG'` เป็นเทียบกับ `_valveFlow(valve).map(_procCanon)[0]`
  (Process แรกจริงของวาล์วนั้น) · เบอร์ที่ไม่มีใน Master ยังตกไปที่ SSG เหมือนเดิม
- **memo ต่อเบอร์วาล์ว** — `_valveFlow()` ไล่หาใน `state.valves` ทีละตัว (1,200+ เบอร์)
  ถ้าเรียกทุกแถวของ `_wipProdSrc()` (20,000+ แถว) จะกลายเป็นงานหลายสิบล้านครั้งต่อการสแกนหนึ่งรอบ
- ส่ง `proc` ติดไปกับทุกรายการ (`miss` / `short`) → ตาราง + ป๊อปอัปยืนยันโชว์ชื่อ process จริงของล็อตนั้น
  เช่น "ผลิตที่ **IND** แล้ว 2,160 ชิ้น · เริ่มทำ IND 2026-07-17" แทนที่จะเขียน SSG ทับทุกกรณี
- ข้อความอธิบายทั้งแผง + ป้าย 🤖 ลงย้อนหลัง ในป๊อปอัป WIP IN 1.5 เปลี่ยนจาก "SSG" เป็น "Process แรก"

### ทดสอบ (เสิร์ฟไฟล์จริงแล้วรันในเบราว์เซอร์)

| ล็อต | flow | ก่อนแก้ | หลังแก้ |
|---|---|---|---|
| VV7364M0 | `IND,QC` | ไม่โผล่เลย | ผลิตที่ **IND** 2,160 |
| VF1713M0 | `IND,SSG,…` | ใช้ยอด SSG 2,120 | ใช้ยอด **IND** 3,679 |
| VF1349M1 | `SSG,…` | SSG 2,378 | SSG 2,378 (ไม่เปลี่ยน) |

กดรับเข้าแล้วหลุดจากแผงทันทีทั้งกลุ่ม "ไม่มีใบ" และ "ยอดรับเข้าขาด" ·
ป๊อปอัปยืนยันขึ้นถูก: "2,160 ชิ้น (ตามยอดผลิตที่ IND = Process แรก) · ลงเป็นวันที่เริ่มทำ IND 2026-07-17"

### หมายเหตุ

- **การ์ด 📥 WIP IN 1.5 ยังนับเฉพาะ "ยังไม่เริ่มผลิต" เหมือนเดิม** — ตรงกับ flow ที่ผู้ใช้ยืนยัน
  พอลงใบให้ล็อตที่ผลิตไปแล้ว ยอดจะเด้งเข้า **▶ WIP กำลังผลิต** ไม่ค้างที่การ์ดแรก (toast บอกอยู่แล้วว่าเข้าถังไหน)
- ⚠️ `VV2643M1 / 2604S040218` มีใบรับเข้าอยู่แล้วแต่พิมพ์ Lot เป็น **2640**S040218 (สลับเลข)
  ระบบมองเป็นคนละล็อต → กดลงเลยจะได้ใบซ้ำ ควรกด ✏️ แก้ Lot ก่อน
- สำรองไฟล์ก่อนแก้: `backups/index.before-firstproc-wipin-2026-08-19.html` (v3.58 ที่ deploy อยู่จริง)

## v3.58 — 2026-08-17 — เครื่องที่ "มีบนผัง IOT แต่ลงบันทึกไม่ได้" → เติมเข้า Machine Master ครบทุกสาย (VSM1 +24 · VSM2 +4)  ✅ deployed

**✅ deployed 2026-08-17** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `9958a01` · run [32038449555](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/32038449555) → `success` |
| Worker `vsm4-api` | Version ID `1641d3a5-dd9c-494c-bffb-e88d7f01d784` (bundle `index.html` ใหม่) |

ยืนยันของจริงด้วย curl ทั้งสอง URL → ได้ `v3.58` · **ขนาดเท่ากันเป๊ะ 1,885,110 bytes · cmp กับไฟล์ในเครื่องแล้วเหมือนกันทุก byte**
เปิดหน้า Worker จริงแล้วรัน: `SEED_VSM_MACHINES` = V1Z2 23 · V1Z3 35 · V2Z1 19 · V2Z3 25 ·
วนทุกกล่องของ `FP1_MC`/`FP2_MC`/`FP3_MC`/`FP4_MC`/`FPQ_MC` เทียบ Master → **ขาด 0 ทุกสาย**

สำรองไฟล์ก่อนแก้: `backups/index.before-mc-iot-sync-2026-08-17.html` (v3.57 ที่ deploy อยู่จริง)
แก้ที่ `VSM12_MACHINES` เท่านั้น (บรรทัด ~3732) — **ไม่แตะผัง IOT** (`FP1_MC`/`FP2_MC`) ตามที่สั่ง

### 1) โจทย์ที่ 1 — C-1101 · C-1102 · C-1103 · C-1110 ของ VSM1

**มีอยู่แล้วครบทั้ง 4 ตัว** ใน `VSM12_MACHINES.V1Z2` (มาตั้งแต่ตอนเปิดสาย VSM1) → ลงบันทึกได้ปกติ ไม่ต้องเพิ่ม
ตรงกันข้าม: ทั้ง 4 ตัว **ไม่มีกล่องบนผัง IOT** (`FP1_MC`) — ตรงนี้ "ค่อยว่ากัน" ตามที่สั่ง

### 2) โจทย์ที่ 2 — สแกนทุกสาย: กล่องบนผัง IOT ที่ยังลงบันทึกไม่ได้

เทียบ id ของกล่องบนผัง (`FP1_MC` `FP2_MC` `FP3_MC` `FP4_MC` `FPQ_MC`) กับ Machine Master ของ VSM นั้น

| สาย | ผัง IOT | Master เดิม | ขาด → เพิ่ม | Master ใหม่ |
|---|---|---|---|---|
| VSM1 | 53 | 34 | **24** | 58 |
| VSM2 | 72 | 71 | **4** | 75 |
| VSM3 | 125 | 125 | 0 | 125 |
| VSM4 | 64 | 64 | 0 | 64 |
| QC | 25 | 25 | 0 | 25 |

**VSM3 / VSM4 / QC ตรงกันเป๊ะอยู่แล้ว** — ไม่ต้องแก้อะไร
(`FPA_MC` ผังรวม 252 กล่อง = `FP1_MC`+`FP2_MC`+`FP3_MC` ตรงกันทุก id เช็กแล้ว)

#### โซนของเครื่องที่เติม — ยึด "แถว/กลุ่มบนผัง"
เอกสาร `Machine VSM1`/`Machine VSM2` ไม่มีเครื่องพวกนี้เลย (ผัง = พื้นที่จริงทั้งหมด = superset ของเอกสาร)
จึงหาโซนจากกล่องข้างเคียงบนผัง แล้วยึด "แถวเดียวกัน" เป็นตัวตัดสิน (แถวหนึ่ง = สายผลิตหนึ่ง)

| สาย·โซน | เพิ่ม | อ้างจากผัง |
|---|---|---|
| V1Z3 (+13) | `D-1105` `H-1114` | คอลัมน์ซ้ายสุด ต่อจาก `H-1115` |
| | `H-1124` | แถวเดียวกับ `H-1110` |
| | `F-1127` `F-1128` `F-1129` `F-1130` `F-1131` | แถวต่อจาก `F-1126` |
| | `F-1118` `F-1120` `F-1121` `F-1122` `F-1123` | แถวต่อจาก `F-1115` |
| V1Z2 (+11) | `F-1158` | แถวเดียวกับ `F-1132`/`F-1156` · ขนาดกล่องชุดเดียวกับ `F-1134/1138/1139/1140` |
| | `G-1123` `G-401` `L-202` `L-401` `L-402` `L-403` `L-404` `L-405` `C-1602` `C-1603` | แถวมุมขวาบนของผัง (กลึง/เจียร/ตัด) |
| V2Z1 (+2) | `Q-1108` `F-201` | `Q-1108` แถวเดียวกับ `F-1149`/`G-1115` · `F-201` ต่อจาก `L-1114/1112/1111` |
| V2Z3 (+2) | `H-1123` `G-2049` | ติดกลุ่ม Deposit stellite seat / ติด `G-278`·`W-1127` |

`target`/`man_std` = ค่า default 2000 / 0.5 (แก้จริงที่หน้าตั้งค่า) · `process` ยังไม่กำหนดเหมือนเครื่อง VSM1/2 ตัวอื่น

### ⚠️ 2 จุดที่ต้องให้ผู้ใช้ยืนยัน (เพิ่มเข้าไปแล้ว แต่มาร์ก comment ไว้ในโค้ด)

1. **`Q-1108` อาจเป็นตัวเดียวกับ `Q-1101`** — เอกสาร `Machine VSM2` มีแต่ `Q-1101` (STRAIGHTEN & DEFLECT, V2Z2) · ผัง IOT มีแต่ `Q-1108`
   (v3.30 ก็ตั้งข้อสังเกตนี้ไว้แล้วว่า "ผังมี `Q-1108` แทน")
   ตอนนี้ Master มีทั้งคู่ → ถ้าเป็นเครื่องเดียวกัน คนลงข้อมูลจะเลือกคนละตัวแล้วยอดแตกเป็น 2 ก้อน
   **ยืนยันมาว่ารหัสจริงคือตัวไหน** แล้วยุบเหลือตัวเดียว (มี `MC_RENAME` + `/api/machines/rename` รองรับอยู่แล้ว)
2. **`G-2049` เอกสารระบุ *Non use*** (v3.30 จึงไม่ใส่) แต่มีกล่องบนผัง IOT → เพิ่มตามคำสั่ง "มีใน IOT แล้วเพิ่มเข้าไป"
   ถ้าเลิกใช้จริง ลบบรรทัดเดียวออกได้

### ทิศทางกลับกัน (ยังไม่แก้ — "ใน IOT ค่อยว่ากัน")

ลงบันทึกได้แต่ยังไม่มีกล่องบนผัง IOT → บน IOT จะไม่เห็นเครื่องพวกนี้เลย

| สาย | เครื่อง |
|---|---|
| VSM1 | `C-1101` `C-1102` `C-1103` `C-1110` `H-1122` |
| VSM2 | `Q-1101` `C-1105` `C-208` |

### ทดสอบแล้ว (เบราว์เซอร์ · localStorage ที่มีข้อมูลเดิมอยู่)

- `SEED_VSM_MACHINES` → V1Z2 23 · V1Z3 35 · V2Z1 19 · V2Z2 31 · V2Z3 25 · **ไม่มี id ซ้ำทั้งในโซนและใน VSM เดียวกัน**
- `ensureVsmZones()` เติมเครื่องใหม่เข้า `state.machines` ที่มีอยู่เดิมได้ครบ (ไม่ลบ/ไม่ทับ target เดิม) — สุ่มเช็ก `L-401` `F-1158` `F-1123` `D-1105` `Q-1108` `F-201` `H-1123` `G-2049` = พบทั้งหมด
- วนทุกกล่องของ `FP1_MC`/`FP2_MC`/`FP3_MC`/`FP4_MC`/`FPQ_MC` เทียบกับ Master ของ VSM นั้น → **ขาด 0 ทุกสาย**
- console ไม่มี error ของโค้ด (มีแต่ `Failed to fetch` เพราะเปิดจาก `file://` เรียก API ไม่ได้)

## v3.57 — 2026-08-16 — ยกเครื่อง "รับเข้า WIP IN 1.5 อัตโนมัติ": แก้ 3 บั๊ก (จอไม่อัปเดต · อ่านแค่ cache · เก็บยอดได้ครั้งเดียวต่อล็อต)  ✅ deployed

**✅ deployed 2026-08-17** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `81d6c48` · run [32036323598](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/32036323598) → `success` |
| Worker `vsm4-api` | Version ID `8c4d1390-3e3a-4454-a0a1-b7deb0ba22b5` (bundle `index.html` ใหม่) |

ยืนยันของจริงด้วย curl ทั้งสอง URL → ได้ `v3.57` · **ขนาดเท่ากันเป๊ะ 1,881,449 bytes · cmp กับไฟล์ในเครื่องแล้วเหมือนกันทุก byte**
เจอโค้ดใหม่ครบทั้งคู่: `_wipinGapScan` ×3 · `_wipinShortItems` ×2 · `wipinAddAllShort` ×2 · `_wbWiVer` ×3 · `_wipDetailOpen` ×5
เปิดหน้า Pages จริงแล้วรัน: ฟังก์ชันใหม่มาครบ · คีย์ cache มี `_wipinVer` แล้ว (`_wbWiVer===wv`)

สำรองไฟล์ก่อนแก้: `backups/index.before-wipin-gapscan-2026-08-16.html` (v3.56 ที่ deploy อยู่จริง ดึงจาก repo)

### อาการที่ผู้ใช้แจ้ง

> "แก้ไขการรับเข้า WIP IN 1.5 แบบอัตโนมัติ พอกดแล้ว มันไม่เอาข้อมูลไปแสดงที่ WIP IN 1.5 · ล่าสุดพึงกดไป แต่ข้อมูลไม่แสดง"

กดปุ่ม **➕ ลงบันทึก WIP IN ทั้งหมด** ในแผง "🏬 ลืมลงบันทึก WIP IN 1.5" (หน้าตั้งค่า) แล้ว
แผงเตือนหายไปเฉย ๆ แต่การ์ด/ป๊อปอัป 📥 WIP IN 1.5 ยังเป็นเลขชุดเดิม เหมือนของไม่เข้า

### ต้นเหตุ — cache ของ `getWipBuckets()` ไม่รู้จัก `_wipinVer`

`getWipBuckets()` memo ผลลัพธ์ไว้ด้วยคีย์ `(_fvVer, viewVsm())` เท่านั้น

| ตัวแปร | ขยับเมื่อ |
|---|---|
| `_fvVer` | `lsSave()` — คือ KPI / ยอดผลิต / แผน |
| `_wipinVer` | `_saveWipin()` — คือ **ใบรับเข้า WIP IN 1.5** |

`_saveWipin()` เขียน `localStorage` เองตรง ๆ ไม่ผ่าน `lsSave()` ⇒ `_fvVer` ไม่ขยับ
แต่ `computeWipBuckets()` อ่าน `_wipinList()` เป็นวัตถุดิบหลัก ⇒ ข้อมูลเปลี่ยนแต่ cache ไม่ล้าง

ผลคือ **ทุกทางที่แตะใบรับเข้า** เรียก `renderWipBuckets()` ต่อทันทีแล้วได้ก้อนเดิมกลับมา:
ฟอร์ม "เข้ายอด WIP IN 1.5" · ปุ่มลงย้อนหลังทั้งแบบทีละใบ/ทั้งหมด · ✏️ แก้ใบ · 🗑 ลบใบ ·
`syncWipin()` ที่ดึงใบใหม่จากเครื่องอื่น — จอนิ่งหมด ต้องรอรอบ auto-refresh (2 ชม.) หรือสลับหน้าถึงจะโผล่

พิสูจน์ในเบราว์เซอร์ (ข้อมูลจริง 646 ใบ): เพิ่มใบทดสอบ → `_wipinList()` เห็นทันที
แต่ `getWipBuckets()` คืน **object เดิมเป๊ะ (`===`)** จำนวนรายการเท่าเดิม 379

### สิ่งที่แก้

1. **`getWipBuckets()` — เติม `_wipinVer` เข้าคีย์ cache** (ตัวเดียวกับที่ `statusOfPlan` ใช้ผสมคีย์อยู่แล้ว)
   ทดสอบซ้ำ: เพิ่มใบ → 379 → 380 ทันที · ลบใบ → กลับเป็น 379
2. **บอกว่ายอดที่เพิ่งลง "ไปโผล่ที่ถังไหน"** (`_wipinLandedMsg()`)
   ใบที่ลงย้อนหลัง = ของที่ผลิตที่ SSG ไปแล้ว ⇒ นับเป็น **WIP กำลังผลิต** ไม่ใช่ "รอผลิต"
   เลขบนการ์ด 📥 WIP IN 1.5 นับเฉพาะที่ยังไม่เริ่มผลิต → **กดแล้วการ์ดไม่ขยับเป็นเรื่องปกติ**
   ถ้าไม่กำกับไว้ คนกดจะอ่านว่า "ลงแล้วแต่ข้อมูลไม่ขึ้น" ซ้ำอีก
   ตอนนี้ toast บอกตรง ๆ เช่น `✅ ลงบันทึก WIP IN 1.5 แล้ว 2 รายการ · รวม 2,232 ชิ้น → ▶ WIP กำลังผลิต +2,229`
3. **ป๊อปอัปที่เปิดค้างอยู่วาดใหม่ให้ด้วย** — แต่ต้องผ่าน `_wipDetailOpen()` ก่อน
   `#modalBd` เป็น element ถาวรที่ใช้ร่วมกับ `confirmModal` และเนื้อหาค้างไว้หลังปิด
   (`closeModal()` ถอดแค่ class `open`) ⇒ เรียก `_refreshWipDetail()` ลอย ๆ จะไปทับกล่องที่ซ่อนอยู่
   + คิด `wipDetailBody()` ทั้งก้อนทิ้งเปล่าทุกครั้งที่กดลงบันทึก

### ทดสอบแล้ว (เบราว์เซอร์ · ข้อมูลจริง · บล็อกการส่งขึ้น Cloud ไว้ทั้งหมด)

เอาใบรับเข้าของ 2 ล็อตออกให้กลายเป็นเคส "ลืมลง" ของจริง แล้วกด `wipinAddAllMissing()`

| | ก่อนแก้ | หลังแก้ |
|---|---|---|
| แถวใน WIP IN 1.5 หลังกด | 377 (ค้าง) | 377 → **379 ทันที** |
| แผงเตือน "ลืมลงบันทึก" | ว่าง | ว่าง |
| toast | บอกแค่จำนวน | + `→ ▶ WIP กำลังผลิต +2,229` |
| `#modalBd` ที่ซ่อนอยู่ | — | ไม่ถูกแตะ (`modalBdTouched:false`) |

คืนสภาพครบหลังทดสอบ: 646 ใบ · 379 แถว · การ์ด 7,040 · ไม่มีอะไรหลุดขึ้น Cloud · ไม่มีขยะทดสอบตกค้าง

---

## รอบสอง — ผู้ใช้ถามต่อ: "วันที่ 16 ทำไมไม่มี พึ่งกดไป"

### คำตอบ: วันที่ 16 ไม่มีข้อมูล ถูกต้องแล้ว (2 ชั้น)

1. **16 ส.ค. (อาทิตย์) ไม่มีงาน SSG เลย** — ทั้งวันมีการผลิตรายการเดียว: `VV7258M0 / 2606S060089 · TL · 3,600 · G-217`
   ปุ่มอัตโนมัติสร้างใบจาก **SSG เท่านั้น** (process แรก = จุดที่ของมาถึงไลน์) → ไม่มีอะไรให้สร้าง
2. **ใบ 3 ใบที่กดเมื่อวันที่ 16 ระบบลงเป็นวันที่ 15** — ตามกติกาที่ตั้งใจไว้ ใช้ "วันเริ่มทำ SSG" ไม่ใช่วันที่กด
   (กล่องยืนยันเขียนไว้แล้ว) เพื่อให้รู้ว่าของมาถึงเดือน/วันไหนจริง

### แต่ระหว่างตรวจ เจอบั๊กอีก 2 ตัวในฟีเจอร์เดียวกัน

#### บั๊ก ② — แผงเตือนอ่านแค่ cache ไม่ใช่ข้อมูลครบ

`_wipinMissingItems()` เดินจาก `state.prodRecords` ทั้งที่ WIP ทุกตัวใช้ `_wipProdSrc()` (= `_allProd`)

| แหล่ง | แถว | ช่วงวันที่ | ล็อต SSG ที่มองเห็น |
|---|---|---|---|
| `state.prodRecords` (ของเดิม) | 1,059 | 31 ก.ค. – 16 ส.ค. | **58** |
| `_wipProdSrc()` (แก้เป็นตัวนี้) | 5,278 | 25 พ.ค. – 16 ส.ค. | **373** |

ตาบอด 315 ล็อต — ผิดกติกาเดิมของระบบที่ว่า WIP/WIP IN เป็น "สถานะปัจจุบัน" ต้องมองครบทุกช่วง ห้ามใช้ cache

**ผลหลังแก้: แผงขึ้นล็อตที่ไม่มีใบรับเข้าเลย 83 ล็อต · 166,647 ชิ้น** (เดิมโชว์ 0 มาตลอด)

#### บั๊ก ③ — เก็บยอดได้ครั้งเดียวต่อล็อต แล้วเลิกสนใจตลอดกาล

เดิม `if(inKeys.has(k)) continue;` = ล็อตไหนมีใบแล้วข้ามทั้งล็อต ⇒ SSG ที่ผลิตเพิ่ม **หลังวันที่กดปุ่ม** ไม่มีใครเก็บอีก

เคสจริง `VV7258M0 / 2606S060089` (ล็อตเดียวกับที่ทำ TL วันที่ 16 พอดี):

| วันที่ | SSG ผลิต | ใบรับเข้า |
|---|---|---|
| 30 ก.ค. | 1,700 | → กดปุ่ม ได้ใบ **1,700** ✓ |
| 31 ก.ค. | 1,500 | — |
| 5 ส.ค. | 2,120 | — |
| 6 ส.ค. | 1,850 | — |
| **รวม** | **7,170** | **1,700** (ขาด 5,470) |

**ทั้งสาย VSM4: 58 ล็อต · ขาดรวม 96,141 ชิ้น**

### กติกาที่ผู้ใช้ยืนยัน

- **ยอดที่ขาด = ของรับเข้าจริง ต้องเก็บให้ครบ** — SSG เป็น process แรก ผลิตได้เท่าไรแปลว่าของมาถึงเท่านั้น
- **เตือนก่อน ให้กดยืนยันเอง** — ไม่ลงอัตโนมัติ เพราะยอดระดับแสนชิ้นต้องมีคนตรวจก่อน

### สิ่งที่แก้

1. **`_wipinGapScan()`** — สแกน SSG ครั้งเดียวจากข้อมูลครบ ตอบ 2 คำถามพร้อมกัน:
   ① ล็อตที่ไม่มีใบรับเข้าเลย ② ล็อตที่มีใบแล้วแต่ยอดไม่ครบ
   memo ด้วยคีย์ `(_fvVer, _wipinVer, viewVsm)` — วัดจริง: cold 1.8ms · cached 0ms · `renderWipinMissing()` ทั้งก้อน 12.2ms
   ยอดใบรับเข้ายังนับ **ทุกสาย** เหมือนเดิม (ล็อตวิ่งข้าม VSM3 ⇄ VSM4 ได้ — อีกสายลงแล้วห้ามฟ้องซ้ำ)
2. **วันที่ของ "ส่วนที่ขาด" คิดแบบ FIFO** — เดินวัน SSG จากเก่าไปใหม่ หาวันที่ยอดสะสมเริ่มเกินใบเดิม
   = วันที่ของก้อนที่ยังไม่มีใบเริ่มเข้ามา (ท่าเดียวกับที่ตารางเทียบ Order ใช้แบ่งล็อตขายหลายงวด)
   ตรวจแล้ว: `VV7258M0` intake 1,700 → cum เกินที่ 31 ก.ค. → ใบใหม่ลงวันที่ 31 ก.ค. ✓
3. **แผงย่อย "➕ ยอดรับเข้าขาด"** — พับปิดไว้ แยกปุ่มออกจาก "ลงบันทึก WIP IN ทั้งหมด" โดยตั้งใจ
   ถ้าเอาไปรวมกับปุ่มที่กดกันทุกวัน จะกลายเป็นลงยกเข่งโดยไม่มีใครตรวจ
   แต่ละบรรทัดโชว์ `SSG / รับเข้าแล้ว / ขาด / วันที่จะลง` + เตือนเรื่อง rework ไว้บนหัวแผง
4. **`wipinShortAdd()` / `wipinAddAllShort()`** — note ขึ้นต้นด้วย "ลงย้อนหลัง" เหมือนเดิม
   → `_wipinIsBackfill()` จับได้ ⇒ ใบพวกนี้ถูกกันออกจากกราฟรายวันอัตโนมัติ (วันที่บนใบคือวันผลิต ไม่ใช่วันของมาถึง)

### ทดสอบแล้ว (เบราว์เซอร์ · ข้อมูลจริง · บล็อกการส่งขึ้น Cloud)

กด **➕ ลงส่วนที่ขาด** ของ `VV7258M0 / 2606S060089`:

| | ผล |
|---|---|
| ใบที่ได้ | 5,470 ชิ้น · ลงวันที่ 2026-07-31 · ติดธง backfill ✓ |
| ยอดใบรวมของล็อต | 1,700 + 5,470 = **7,170 = SSG พอดี** ✓ |
| ล็อตนี้ในลิสต์ยอดขาด | หายไป (58 → 57) ✓ |
| toast | `✅ ลงส่วนที่ขาดแล้ว — VV7258M0 / 2606S060089 · 5,470 ชิ้น → ▶ WIP กำลังผลิต +5,470` |

คืนสภาพครบ: 646 ใบ · 58 ล็อตยอดขาด · 379 แถว · ไม่มีอะไรหลุดขึ้น Cloud

### ⏳ ยังไม่ได้กดลงจริง

โค้ดพร้อมแล้ว แต่ยัง **ไม่ได้สร้างใบรับเข้าใหม่สักใบ** — รอผู้ใช้ตรวจตัวเลขแล้วกดเอง
ของที่รอตรวจ: **83 ล็อต 166,647 ชิ้น** (ไม่มีใบเลย) + **58 ล็อต 96,141 ชิ้น** (ยอดขาด)

## v3.56 — 2026-08-16 — ป๊อปอัป WIP IN 1.5: นับเฉพาะเดือนที่มี Order · จัดหน้าใหม่ (พับหมด เหลือกราฟ) · แก้บั๊ก Admin สลับสายแล้วข้อมูลไปผิดสาย · ปักหมุด VSM ให้พนักงาน 44 คน  ✅ deployed

**✅ deployed 2026-08-16** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `2e70ddc` · run [31958142817](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/31958142817) → `success` |
| Worker `vsm4-api` | Version ID `f5bf53ee-c2b3-4e39-aeb6-e031057b886e` (bundle `index.html` ใหม่) |

ยืนยันของจริงด้วย curl ทั้งสอง URL → ได้ `v3.56` · **ขนาดเท่ากันเป๊ะ 1,863,909 bytes · cmp แล้วเหมือนกันทุก byte**
เจอโค้ดใหม่ครบทั้งคู่: `vsm:viewVsm()` ×5 · `_wipinIsBackfill` ×2 · `wi-fold` ×24 · `fillDays` ×3 · `_empVsmApi` ×6

สำรองไฟล์ก่อนแก้: `backups/index.before-wipin-redesign-2026-08-16.html`
สำรองข้อมูลที่ลบ: `backups/wipin-vsm3-mislabelled-20260816.json` (212 แถว · 514,028 ชิ้น)

### สิ่งที่ผู้ใช้สั่ง

1. "ต้องแสดงรายวันของแต่ละ VSM" → ผู้ใช้ขยายความว่า **สายใครสายมัน** — VSM4 ต้องเห็นเฉพาะใบที่ VSM4 กดรับเข้าเอง
2. "ต้องมี Order ในเดือนนั้นเท่านั้น ถึงจะนับ" → เลือกกติกา **นับเฉพาะเดือนที่มี Order**
3. "ตกแต่งใหม่ด้วย ดูยากมาก ซ่อน ๆ ไว้" → **พับทุกอย่างยกเว้นกราฟ** + **ย้ายตัวกรองขึ้นบนสุดเป็นแถบเดียว**

### 1) กติกาใหม่ — นับเฉพาะเดือนที่มี Order

ตัดที่ **ต้นทางชุดเดียว** (`baseItems` ในสาขา `bucket==='in'` ของ `wipDetailBody`) แล้วทุกก้อนด้านล่าง
(กราฟ · ตารางเทียบ Order · ตารางแผนขาย · ตารางรายการ · แถบเตือนทุกอัน) พูดตรงกันเองโดยอัตโนมัติ
— ไม่ต้องไล่แก้ทีละที่ ซึ่งเป็นต้นเหตุที่ตัวเลขในหน้าเดียวกันเคยไม่ตรงกันมาแล้ว

ฟังก์ชันที่เพิ่ม/แก้ (รับ `months` = `Set<'YYYY-MM'>` เป็น optional — **ไม่ส่ง = พฤติกรรมเดิมเป๊ะ**):

| ฟังก์ชัน | เปลี่ยนอะไร |
|---|---|
| `_wipInMonthSplit()` *(ใหม่)* | บัญชียอดรับเข้าแบ่ง 3 กอง: นับ / นอกแผน / ไม่นับ |
| `_dailyWipIn(items, ym, months)` | ข้ามวันที่อยู่ในเดือนที่ไม่มี Order |
| `_orderSupplyMap(kind, months)` | `in` กรองตาม `ts` · `out` ตัดรายวันด้วย `fgByDate` (ล็อตคร่อมเดือนเอาเฉพาะส่วนที่เข้าเกณฑ์) |
| `_orderReconRows(kind, months)` · `orderReconHtml(kind, opts.months)` | ส่งต่อ + เปลี่ยนคำอธิบายใต้หัวข้อให้ตรงกติกา |
| `_wipPlanRows(ym, months)` · `_wipInPlanHtml(ym, months)` | ยอด "รับเข้า" ในตารางแผนขายนับกติกาเดียวกัน |

**ยอดที่ถูกตัดออกไม่หายเงียบ** — สรุปไว้ใต้กราฟพร้อมปุ่มสลับ `📦 นับทุกเดือน` กลับไปดูได้เสมอ
(ยอดที่ไม่ reconcile = ยอดที่ใช้ไม่ได้ · คนอ่านจะเลิกเชื่อทั้งหน้า)

วัดจากข้อมูลจริง VSM4 วันที่แก้ — สามกองบวกกันเท่ายอดรับเข้าทั้งหมดเป๊ะ:

| กอง | ยอด |
|---|---|
| นับ (เดือนมี Order + จับคู่แผนได้) | 170,043 |
| นอกแผน (เดือนมี Order แต่ไม่มีแผนคู่ — ของจริงที่ต้องตาม) | 866,774 |
| ไม่นับ (รับเข้า 2026-05 / 2026-06 ที่ยังไม่มี Order · 52 ใบ) | 153,193 |
| **รวม** | **1,190,010** ✓ |

กราฟ "ทุกเดือน": 1,036,817 ชิ้น / 44 วันทำงาน — กด `📦 นับทุกเดือน` ได้ 1,190,010 / 64 วัน
ส่วนต่าง = 153,193 พอดี · เดือนใน dropdown เหลือ 2 เดือน (2026-08 / 2026-07) กดสลับได้เป็น 4 เดือน

### 2) สายใครสายมัน

`_wipinList()` filter ด้วย `viewVsm()` อยู่แล้ว จึงไม่ต้องแก้ตรรกะ — แต่ **ของเดิมไม่มีอะไรบอกผู้ใช้เลย**
เพิ่มป้าย `🏭 VSM 4 เท่านั้น` ค้างบนแถบกรอง (มี tooltip อธิบาย) และเปลี่ยน `b.wipIn.items` ที่เหลือทุกจุด
ในสาขานี้ให้อ่านจาก `baseItems` ชุดเดียว — กันคนแก้ทีหลังเผลอดึงชุดที่ไม่ผ่านตัวกรองมาใช้

→ ต่อยอดเป็นข้อ 4 ด้านล่าง (ผู้ใช้สั่งให้ปิดช่องโหว่ `VSM_EMP_MAP` ต่อเลย)

### 4) ปิดช่องโหว่ "ไม่อยู่ในแมป = vsm4" ที่ต้นเหตุ

**ปัญหา** — เดิม VSM4 ไม่เคยถูกเขียนไว้ในโค้ดจริงสักคน (ใช้กติกา "ไม่อยู่ในแมป = vsm4")
record ที่ไม่มี `zone` ให้ยึด — **ใบรับเข้า WIP IN 1.5** และ **บันทึกต่อกะ** — จึงรอดมาได้ด้วย
"ตราประทับ `vsm` ตอนบันทึก" อย่างเดียว วัดของจริง: ใบรับเข้า **588/588 ใบ (1,190,010 ชิ้น)**
พึ่งตราประทับล้วน ๆ · ตราหลุดเมื่อไร (บันทึกออฟไลน์รุ่นเก่า / round-trip กับ D1 / แก้ข้อมูลมือ)
ยอดจะไหลไป vsm4 เงียบ ๆ — ของ VSM4 เองยังถูก แต่ถ้าเป็นคนสายอื่น = ยอดข้ามสายทันที

**แก้ ① ปักหมุด 44 รหัสเป็น `vsm4` ใน `VSM_EMP_MAP`** — ไม่ได้เดา: ไล่ record จริงทุกใบของแต่ละคน
แล้วดู `zone` ที่บันทึก (คอลัมน์ที่รอด D1 round-trip เสมอ) ทั้ง 44 คนมี record อยู่ใน ZONE1/2/3
(โซนของ VSM4) เท่านั้น **ไม่มีใครปนสายอื่นเลยสักคน**

**แก้ ② `_empVsmCloud` ฟอกค่าที่ระบบเดาเอง ให้กลายเป็น "ค่าจากฐานข้อมูล"** *(บั๊กที่เจอระหว่างทาง)*
`loadEmpVsmCloud()` เดิมอ่าน `e.vsm` จาก `state.employees` — แต่ `normalizeEmployeeVsm()` เขียน
`e.vsm = resolveEmpVsm(e.id)` ทับทุกคนก่อนหน้านั้น และตัวนั้นตกท้ายที่ `DEFAULT_VSM`
⇒ ค่าที่เดาเองถูกอ่านกลับมาเป็น "มาจาก D1" ในรอบถัดไป แล้วบันทึกลง localStorage ค้างไว้
ยิงจริง `/api/init`: พนักงาน 55 คน **มี `vsm` มาด้วย 0 คน** แต่ในแอป `_empVsmCloud` มี **298 รายการ**
(= ของปลอมทั้งหมด) → เพิ่ม `_empVsmApi` เก็บ snapshot เฉพาะค่าที่ D1 ส่งมาจริง (capture ก่อนถูกเขียนทับ)

**แก้ ③ ป้ายบอกที่มาในหน้าตั้งค่า** — แต่ละแถวขึ้น `🔒 โค้ด` / `💾 เครื่องนี้` / `☁️ ฐานข้อมูล` /
`⚠️ ยังไม่ระบุ` (แถวสีเหลือง) และหัวตารางสรุป `⚠️ ยังไม่ระบุสาย N คน` — ตัวเลขนี้ควรเป็น 0
เพิ่มสถานะ `… ยังไม่ซิงก์` กันขึ้นธงยกเข่งตอนเปิดแอปครั้งแรกที่ยังไม่ได้โหลดจาก Cloud

**เหลือ 11 คนที่จงใจไม่ใส่ในแมป** — ไม่มี record ให้ตรวจสักใบ จึงไม่มีหลักฐานว่าอยู่สายไหน:
`G-260 · L-3378 · PST759 · S-038 · S-059 · S-071 · T-738 · T-798 · T-816 · Temp117 · ZZ-001`
ใส่มั่วไม่ได้เพราะ **รหัสในแมปเป็นกฎกลาง ทับค่าที่ admin ตั้งเอง** (`setEmployeeVsm` จะเด้ง toast ปฏิเสธ)
ปล่อยไว้แบบนี้ = ยังตั้งสายจากหน้าตั้งค่าได้ตามปกติ และมีธง ⚠️ เตือนให้ไปตั้ง

**ผลกระทบต่อตัวเลข = ศูนย์** (ตั้งใจ — งานนี้คือถอดความเปราะ ไม่ใช่ย้ายยอด):

| ตรวจ | ผล |
|---|---|
| คีย์ซ้ำใน `VSM_EMP_MAP` (ซ้ำ = ตัวหลังทับตัวหน้าเงียบ ๆ) | 287 คีย์ · **ซ้ำ 0** |
| เทียบผลทุก record กับ "แมปเก่า" (ถอด 44 รหัสออก) | kpi 0/10000 · prod 0/10000 · wipin 0/594 · ต่อกะ 0/20 · พนักงาน 0/298 |
| เทียบผลทุก record กับ `_empVsmCloud` แบบเก่า | เปลี่ยน 0 ทุกชุด (ชั้นที่ถอดออกซ้ำกับ default ทั้งหมด) |
| ใบรับเข้าที่พึ่ง "ตราประทับ `vsm`" | **588 → 0** |
| ถ้าตราประทับหายหมด จะมีใบย้ายสายกี่ใบ | **0** (เดิม = 588) |
| ยอด WIP IN ต่อสาย | vsm4 588 ใบ/1,190,010 · vsm3 6 ใบ/9,750 — เท่าเดิมเป๊ะ |
| จำนวนพนักงานต่อสาย | 60/73/71/55/39 = 298 — เท่าเดิมเป๊ะ |

### 3) จัดหน้าใหม่

ของเดิมเปิดมาเจอกราฟ + ตารางเทียบ Order + แถบเตือน 3 สี + ตารางแผนขาย + ตารางรายการ **กางพร้อมกันหมด**

- **แถบกรองแถวเดียวบนสุด** (`.wi-bar`) — ป้ายสาย · เดือน · ลูกค้า · สถานะ · ช่องค้นหา
  (เดิมกระจายอยู่ 2 ที่ ใต้ตารางเทียบ Order ต้องเลื่อนหา)
- **กราฟกางไว้อย่างเดียว** ที่เหลือพับหมด ใช้โครงกล่องเดียวกัน (`.wi-fold`) ต่างแค่สีขอบซ้าย
  `i` ฟ้า=ข้อมูล · `w` ส้ม=ควรตรวจ · `b` แดง=ผิดพลาด · `g` เขียว=ครบแล้ว → สแกนแนวตั้งเดียวจบ
- หัวข้อกล่องพับสรุปตัวเลขไว้ให้เห็นตอนพับ (เช่น `📄 รายการรับเข้าทั้งหมด · 303 รายการ`)
- **พิมพ์ค้นหา / เลือกสถานะ → ตารางกางเอง** ไม่งั้นพิมพ์แล้วเหมือนไม่มีอะไรเกิดขึ้น
- สถานะการพับเก็บใน `_wiFold` (นอกฟังก์ชันวาด) — `_refreshWipDetail()` วาดใหม่ทั้งก้อน
  ถ้าไม่เก็บ กล่องจะหุบเองทุกครั้งที่แตะตัวกรอง · เปิดป๊อปอัปใหม่ = พับหมดเสมอ

### 5) แยก "ยอดรับเข้าใหม่" ออกจาก "ยอดคงค้าง" ให้ขาด

ผู้ใช้แจ้งว่ากราฟ "น่าจะแสดงยอดคงเหลือสะสม ไม่ใช่ยอดรับเข้ารายวัน" — **ตรวจแล้วกราฟถูกอยู่แล้ว**
ไล่เทียบทุกวันกับใบดิบ: 01/08 = 38,521 · 02/08 = 3,000 · 03/08 = 56,952 · 08/08 = 2,750 —
ตรงกันทุกวัน และเลข**ลดลงได้** (ถ้าสะสมจริงจะลดไม่ได้เลย) · ล็อตที่ลงซ้ำมี 19 จาก 561 ล็อต
เป็นของทยอยส่งจริง (3,264 → 630 → 1,098 → 1,094 → 1,474) ไม่ใช่การลงยอดสะสม

**ต้นเหตุจริงคือชื่อซ้ำกัน** — หน้าเดียวกันมีเลข 2 ตัวที่ป้ายเขียนว่า "WIP IN 1.5" เหมือนกัน:

| ที่ | ค่า (วัดจริง) | ความหมาย |
|---|---|---|
| การ์ด 📥 WIP IN 1.5 | **7,040** | ยอด**คงค้าง**รอผลิตตอนนี้ (เหลือ 9 ใบ) — stock · สะสมขึ้น ลดเมื่อเอาไปผลิต |
| กราฟในป๊อปอัป | **620,488** | ยอด**รับเข้าใหม่**ทั้งเดือน (303 ใบ) — flow · ของวันใครวันมัน |

ต่างกัน ~88 เท่า เพราะ ส.ค. รับเข้า 620,488 เอาไปผลิตแล้ว 616,711 เหลือค้าง 3,777

แก้: เปลี่ยนชื่อกราฟเป็น **"ยอดรับเข้าใหม่ รายวัน"** + เพิ่มบรรทัดกำกับใต้หัวกราฟ (`.dc-note`)
"แต่ละแท่ง = ของที่รับเข้าในวันนั้นเท่านั้น — ไม่ยกยอดค้างจากวันก่อนมาบวก" พร้อมกางตัวเลข 3 ตัว
`รับเข้าใหม่ / เอาไปผลิตแล้ว / ยังค้างรอผลิต` แล้วชี้ว่าตัวสุดท้ายคือเลขบนการ์ด
ตรวจแล้วเลือก "ทุกเดือน" → "ยังค้างรอผลิต 7,040" **เท่ากับเลขบนการ์ดเป๊ะ** (กราฟ WIP OUT ไม่ถูกแตะ)

### 6) 🐞 กราฟรายวันพอง 4.6 เท่า — 89% ของยอดเป็นใบที่ระบบลงย้อนหลังให้

ผู้ใช้ยืนยัน "ของจริงรับเข้าวันละ ~12,000 บางวัน 0" แต่กราฟโชว์ ~41,000/วัน ทุกวันมีของ
ไล่ที่มาของใบรับเข้าทั้ง 588 ใบ พบว่า:

| ที่มา (ดูจาก `note`) | ใบ | ยอด |
|---|---|---|
| คนลงรับเข้าเอง (`note` ว่าง) | 107 | 126,193 |
| ปุ่ม "ลืมลง WIP IN 1.5" ลงย้อนหลังให้ | 481 | **1,063,817 (89.4%)** |

ปุ่มนั้น (`backfillWipinAll` หน้าตั้งค่า) สร้างใบจากยอดผลิตที่ SSG โดยใช้ **วันเริ่มทำ SSG
เป็นวันที่รับเข้า** (`addWipin(..., it.first)`) — ใบพวกนี้ตอบได้ว่า "ของล็อตนี้เคยมาถึงแน่ ๆ"
แต่ตอบไม่ได้ว่า "มาถึงวันไหน" เอามาทำกราฟรายวันจึงผิดทั้งแท่ง

**แก้:** เพิ่ม `_wipinIsBackfill()` + ธง `back` บนรายการ → มีปุ่มสลับ
`✋ เฉพาะที่ลงรับเข้าจริง` ⇄ `🤖 รวมที่ลงย้อนหลัง` และบอกยอดของแต่ละกองไว้ใต้กราฟ
ในตารางรายการติดป้าย `🤖 ลงย้อนหลัง` ใต้วันที่ทุกแถวที่เป็นใบแบบนี้

> ⚠️ **ค่าเริ่มต้น = รวมใบลงย้อนหลัง** — รอบแรกผมตั้งเป็น "ไม่รวม" ซึ่งผิด
> ผู้ใช้ยืนยันว่า *"บางครั้งลืมจริง จนมีการผลิตแล้ว"* → ของพวกนั้นคือของที่รับเข้าจริง ต้องนับ
> ปุ่มสลับยังอยู่ไว้ใช้ไล่ปัญหา (ดูข้อ 7 — ยอดที่พองจริง ๆ มาจากคนละสาเหตุ)

> ⚠️ **ไม่แตะฐาน WIP** — ยอด WIP/Output ยึดใบพวกนี้อยู่ ตัดออกจาก `_wipinList()` แล้วยอดพังทั้งระบบ
> ตารางเทียบ Order ก็ยังนับครบเหมือนเดิม เพราะคนละคำถามกัน:
> กราฟถาม "ของมาถึงวันไหน" (ต้องมีวันที่เชื่อได้) · ตารางถาม "ของที่สั่งมาถึงครบยัง" (ผลิตไปแล้ว = มาถึงแล้ว)

**+ วันที่ยอด 0 ต้องเห็น** — ของเดิมวาดเฉพาะวันที่มีของ แกนเลยกระโดด `01 → 04 → 05`
ดูเหมือนรับเข้าทุกวัน · เพิ่ม `opts.fillDays` เติมวันให้ครบช่วง + ขีดเทาบาง ๆ ที่เส้นฐาน
(ค่าเฉลี่ยยังหารด้วย "วันที่มีงาน" เหมือนเดิม — วันหยุดไม่ควรถ่วงค่าเฉลี่ยกำลังผลิต)

โหมด `✋ เฉพาะที่ลงรับเข้าจริง` ของ ส.ค.:
`01: 6,602 · 02: 0 · 03: 0 · 04: 3,843 · 05: 10,971 · 06: 4,743 · 07: 10,251 · 08–10: 0 · 11: 6,862 · 12: 0 · 13: 12,171 · 14: 0 · 15: 16,395`
→ เฉลี่ย 8,980/วัน · 8 วันที่มีงาน · รวม 71,838 · Σ แท่ง = หัวกราฟ · 71,838 + 548,650 = 620,488 ✓

### 7) 🐞🔴 ต้นเหตุจริงของยอดพอง — งาน VSM3 ถูกบันทึกเป็นยอดรับเข้าของ VSM4

ผู้ใช้ทักเองว่า *"น่าจะเป็นงาน OEM ของ VSM3 ที่กดรับเข้าเมื่อวาน มันเอารวมกับ VSM4 หรือไม่"* — **ใช่**

ไล่ตาม `updatedAt` (วันที่กดบันทึกจริง ไม่ใช่ `ts` ที่ย้อนหลังได้):

| กดบันทึกวันที่ | ใบ | ยอด |
|---|---|---|
| 08-11 | 5 | 6,862 |
| 08-12 | 6 | 8,499 |
| 08-13 | 13 | 14,571 |
| 08-14 | 11 | 10,551 |
| **08-15** | **247** | **578,914** |

วันเดียวเท่ากับ 93% ของยอดทั้งเดือน · ตรวจล็อตทั้ง 247 ใบว่ายอดผลิต SSG จริงอยู่สายไหน:

| ล็อตพวกนั้นผลิตที่ | ใบ | ยอด |
|---|---|---|
| มี SSG ของ VSM4 จริง | 9 | 12,631 |
| **ไม่มี SSG ของ VSM4 เลย (อยู่ VSM3)** | **212** | **514,028** |
| ไม่มี SSG ในแคช | 26 | 52,255 |

ในนั้น **OES&OEM 470,009 ชิ้น** — ตรงกับที่ผู้ใช้เดาว่าเป็นงาน OEM ของ VSM3
ตัวอย่าง: `VV7295M0/2607S070395` ใบรับเข้าที่ VSM4 400 ชิ้น แต่ SSG จริงอยู่ VSM3 ทั้ง 4,200 ชิ้น

**สาเหตุ — `addWipin()` stamp ผิดตัวแปร:**

```js
vsm: myVsm(),     // ← สายของ "คนที่ล็อกอิน"
```

`myVsm()` = สายของผู้ใช้ · `viewVsm()` = สายที่กำลังดู/ทำงานอยู่ · ปกติค่าเท่ากันเพราะผู้ใช้ทั่วไป
ถูกล็อกที่สายตัวเอง — **แต่ Admin สลับสายได้** · S-057 (พนักงาน VSM4 + Admin) สลับไปดู VSM3
แล้วกดปุ่ม "ลงบันทึก WIP IN ทั้งหมด" → แผงลิสต์ล็อตของ VSM3 ถูกต้อง (`_wipinMissingItems`
ใช้ `filterVsm` = `viewVsm`) แต่ใบที่สร้างถูก stamp เป็น `vsm4` ทุกใบ

**แก้ 2 จุด:**

| ที่ | เดิม | ใหม่ |
|---|---|---|
| `addWipin()` | `vsm: myVsm()` | `vsm: viewVsm()` — สายที่กำลังทำงานอยู่ |
| `_wipinVsm()` | รหัสผู้บันทึก → ตราประทับ | **ตราประทับ → รหัสผู้บันทึก** (ลำดับเดียวกับ `_hoVsm()` ของบันทึกต่อกะ ซึ่งถูกอยู่แล้ว) |

ตราประทับ = "ใบนี้ถูกลงให้สายไหน" (สิ่งที่ต้องการ) · รหัสผู้บันทึก = "ใครกด" (คนละเรื่อง)
สองที่ในแอปเดียวกันเคยใช้คนละลำดับ — ใบเดียวกันตอบคนละสาย

ตรวจแล้ว: ข้อมูลปัจจุบัน 594 ใบ **ไม่มีใบไหนย้ายสาย**จากการสลับลำดับ (ทุกใบมีตราประทับครบ)
ผู้ใช้ทั่วไปไม่ได้รับผลกระทบเลย (`viewVsm() === myVsm()` อยู่แล้ว)

### 8) 🗑 ลบข้อมูลเก่าที่ stamp ผิด — 212 ใบ / 514,028 ชิ้น

ผู้ใช้เลือก "ลบทิ้ง" · ผมทักท้วงหนึ่งรอบเพราะผลตรวจขัดกับเหตุผลของตัวเลือกนั้น
(**VSM3 ยังไม่ได้ลงใบรับเข้าของล็อตพวกนี้เลยสักใบ** — มีทั้งหมดแค่ 6 ใบ → ลบแล้วของหายจากทั้งระบบ
ไม่ใช่แค่ย้ายออกจาก VSM4 · และใน 212 ล็อตมี 55 ล็อตที่ยังผลิตไม่จบ) ผู้ใช้ยืนยันซ้ำ → ดำเนินการ

**ชุดที่ลบ** (นิยามชัดเจน · คำนวณใหม่ตอนลบ ไม่ใช้ตัวแปรค้าง):
`updatedAt = 2026-08-15` **และ** ล็อตนั้นมี record SSG จริง **แต่ไม่มีของ VSM4 เลย`
→ มีตัวกันพลาด: ถ้าคำนวณแล้วไม่ได้ 212 ใบ / 514,028 ชิ้นเป๊ะ จะไม่ลบอะไรเลย

ไม่ได้ลบ: 9 ใบ (12,631) ที่มี SSG ของ VSM4 จริง · 26 ใบ (52,255) ที่ไม่มี SSG ในแคชให้ตัดสิน

**สำรองก่อนลบ:** `backups/wipin-vsm3-mislabelled-20260816.json` — 212 แถวครบ
`id / valve / lot / qty / ts / updatedAt / byId / note / ssgAt` (รวม 514,028) เอากลับคืนได้

ส่งขึ้น Cloud ครั้งเดียวด้วย `/wipin/bulk` (ไม่ยิงรัว 212 ครั้ง) · คิวว่าง ไม่มี error
ยืนยันจาก Cloud: 643 แถว → มาร์คลบ 261 (เดิม 49 + ใหม่ 212) · เหลือใช้งาน 382 แถว 685,732 ชิ้น

| | ก่อน | หลัง |
|---|---|---|
| ใบรับเข้า VSM4 | 588 | **376** |
| ยอดรับเข้า VSM4 | 1,190,010 | **675,982** |
| WIP กำลังผลิต VSM4 | 279,556 | **207,068** |
| WIP รอผลิต | 7,040 | 7,040 (ไม่กระทบ) |

### ตรวจแล้ว

| # | หัวข้อ | ผล |
|---|---|---|
| 1 | Syntax ทั้งไฟล์ (parse 3 script block ด้วย `vm.Script`) | 0 error |
| 2 | ยอด 3 กองบวกกัน = ยอดรับเข้าทั้งหมด | ตรงเป๊ะ (1,190,010) |
| 3 | สลับ `นับเฉพาะเดือนที่มี Order` ⇄ `นับทุกเดือน` | ส่วนต่าง = 153,193 พอดี · dropdown เดือน 2 ⇄ 4 |
| 4 | กราฟตามเดือนที่เลือก | 2026-08 = 620,488 · 2026-07 = 416,329 (ตรงกับยอดรับเข้ารายเดือนดิบ) |
| 5 | ป๊อปอัป WIP OUT / WIP กำลังผลิต / หน้าวางแผน (ใช้ฟังก์ชันร่วม) | ยังใช้ `.or-box` เดิม · กางเหมือนเดิม · `_orderReconRows` คืน 274 แถวเท่าเดิม |
| 6 | Fold state ค้างข้ามการ re-render | กางกล่องแล้วเปลี่ยนเดือน → ยังกางอยู่ |
| 7 | ค้นหา → ตารางกางเอง | กางจริง · 177/303 แถว |
| 8 | มือถือ 375px | ไม่มีเลื่อนแนวนอน (docScroll 375) · select/input 45px · ชิป 43px · หัวข้อพับ 60–64px |
| 9 | 2 ภาษา (ไทย/EN) | ไม่มีคำไทยตกค้างใน EN (เหลือแต่ค่าข้อมูลจาก DB) |
| 10 | Console error | 0 |
| 11 | หน้าตั้งค่า → รายชื่อพนักงาน (หลังข้อ 4) | ป้ายที่มาขึ้นครบ · หัวตารางเตือน "⚠️ ยังไม่ระบุสาย 11 คน" · dropdown ของ 11 คนนั้นยังตั้งได้ |
| 12 | กราฟรายวันหลังแยกใบลงย้อนหลัง (ข้อ 6) | Σ แท่ง = หัวกราฟ = 71,838 · เติมวัน 0 ครบ 7 วัน · สลับ 2 โหมดแล้วยอดรวมกันได้ 620,488 |
| 13 | กราฟ WIP OUT ไม่โดนผลกระทบ | 14 คอลัมน์เท่าเดิม · ไม่มีวันยอด 0 แทรก · ไม่มีบรรทัดกำกับ |
| 14 | ป้าย `🤖 ลงย้อนหลัง` ในตารางรายการ | ขึ้นครบทุกแถวที่เป็นใบแบบนั้น (หน้า 3 = 15/15 แถว) |

## v3.55 — 2026-08-15 — กราฟรายวัน + สรุปเทียบ Order แยกเดือน · แก้ WIP พองเป็นล้าน · แก้คิว Cloud ค้างถาวร · หน้าวางแผนใหม่ · ดับเบิลคลิกแก้แผน  ✅ deployed

**✅ deployed 2026-08-15** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `eb0c727` · run [31892399812](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/31892399812) (#141) → `success` |
| Worker `vsm4-api` | Version ID `0c6a7928-9ebb-4cc4-a149-8703e9106b8b` (bundle `index.html` ใหม่) |

ตัว Worker **ไม่ได้แก้โค้ด** แต่ต้อง deploy ซ้ำเพราะมันบันเดิล `index.html` ไว้ในตัว
ยืนยันของจริงด้วย curl ทั้งสอง URL → ได้ `v3.55` · ขนาดเท่ากันเป๊ะ 1,818,200 bytes ·
เจอ `orderReconHtml` / `dailyChartHtml` / `planCellEdit` / `planValveEdit` / `_pendWDead` / `noIntake` ครบทั้งคู่
API ยังตอบปกติ (`/api/plans` 200 · `/api/kpi` 200) · เปิดหน้าจริงบน Pages แล้ว **ไม่มี console error**

สำรองไฟล์ก่อนแก้: `backups/index.before-wip-charts-20260815.html`

### ตรวจก่อน deploy (ตามที่ผู้ใช้สั่ง)

| # | หัวข้อ | ผล |
|---|---|---|
| 1 | Syntax ทั้งไฟล์ (parse 3 script blocks ด้วย `new Function`) | 0 error |
| 2 | ID ซ้ำ / ID เดิมหายหลังรื้อ HTML หน้าวางแผน | ไม่ซ้ำ · ครบทั้ง 23 ตัว |
| 3 | ฟังก์ชันใน inline handler (ทั้ง static + ที่ generate ตอน render) | resolve ครบ · บอร์ด 150 ช่องมี dblclick จริง |
| 4 | ตัวเลข reconcile 2 ชุด | diff = 0 ทั้งคู่ |
| 5 | Performance | เจอปัญหา → แก้ (ดูล่าง) |
| 6 | ทน localStorage เสีย (`{{{` / `null` / `"x"` / `{"cmp":1}`) | ตกไป default ทุกเคส |
| 7 | ทนข้อมูลว่าง (สายที่ไม่มีแผน/ไม่มีใบรับเข้า) | ไม่ crash |
| 8 | External dependency เพิ่มไหม | เหมือน backup เป๊ะ (ไม่เพิ่ม) |
| 9 | มือถือ 375px — home / วางแผน / 3 ป๊อปอัป / ป๊อปอัปแก้แผน | 0 ตัวโดนตัด · 0 เลื่อนแนวนอน · ปุ่ม −/+ 34×34px |
| 10 | 2 ภาษา × 5 สาย × ทุกมุมมอง × ทุกตัวกรองเดือน/สถานะ | 0 error |

**⚡ ที่แก้จากผลตรวจข้อ 5** — `computeWipBuckets()` เรียก `computePostFgAll()` ซ้ำสองรอบ
(รอบละ 262ms บนข้อมูล 20,000 แถว) รอบแรกหา "ล็อตที่ถึง FG แล้ว" รอบสองหา "ยอด FG ต่อล็อต"
ทั้งที่เป็นชุดเดียวกัน → คำนวณครั้งเดียวแล้วใช้ซ้ำ: **924ms → 609ms (−34%)** ตัวเลขทุกตัวเท่าเดิมเป๊ะ

### 🐞 บั๊กที่แก้

**1) WIP กำลังผลิต พองเป็น 1,139,603 ชิ้น → เหลือ 280,024**

ของเดิม ล็อตที่ไม่มีใบรับเข้า WIP IN 1.5 จะถูก *เดายอด* จาก "process ที่ไกลสุด" (`basis:'produced'`)
วัดจากข้อมูลจริง: **94%** ของยอด (1,067,768 ชิ้น) เป็นล็อตที่ไม่เคยมี record FG/SFG/QC เลยสักที่ —
ส่วนใหญ่ลง IND อย่างเดียว (368,807) หรือ SSG อย่างเดียว (141,709) ไว้เมื่อ มิ.ย.–ก.ค. แล้วเงียบไป
คือของออกจากไลน์ไปแล้วแต่ไม่มีใครลง FG · และ **84%** ไม่มีใบรับเข้าเลย = ไม่มีตัวอ้างอิงว่าเข้ามากี่ชิ้น

สูตรใหม่ (ผู้ใช้เลือก): **WIP = ยอดรับเข้า WIP IN 1.5 − ผ่าน FG − scrap** ต่อล็อต
- หัก FG **แบบบางส่วนได้** (เดิมล็อตที่แตะ FG จะถูกทิ้งทั้งใบ) → "มียอดนำออก ⇒ WIP ลดลง" ตามที่ผู้ใช้ต้องการ
- หัก FG **รวมสายพี่น้อง** — รับเข้าที่ VSM4 ไปจบ FG ที่ VSM3 ก็ต้องหายจาก WIP
- ไล่จาก "ใบรับเข้าของสายตัวเอง" ไม่ใช่ prodRecords → งานข้ามสายไม่ถูกนับสองรอบโดยไม่ต้องพึ่ง `_wipOwnerVsm`
- ล็อตที่ผลิตอยู่แต่ **ไม่มีใบรับเข้า** (959,839 ชิ้น / 393 ล็อต) ไม่นับเข้ายอด แต่เปิดดูได้ที่ `wip.noIntake`
  พร้อมปุ่ม "ดูรายการนี้" ในป๊อปอัป — ยอดที่ระบบเดาเองไม่ควรเป็นตัวเลขที่ใครเอาไปตัดสินใจ แต่ก็ต้องไม่หายเงียบ

**พิสูจน์ด้วยสมดุล** — ทุกชิ้นที่รับเข้าต้องอยู่ถังใดถังหนึ่งพอดี:
`1,190,010 = รอผลิต 7,040 + WIP 280,024 + ผ่าน FG 777,742 + scrap 5,351 + จบแล้ว 119,853` → **unexplained 0**

**2) WIP IN 1.5 โชว์ข้อมูลเหมือนกันทุกแผนก ทุกเดือน**

`_wipPlanRows()` อ่าน `state.plans` ดิบ ๆ **ไม่ผ่าน `filterVsm`** → ตาราง "งานขายที่วางแผน (เทียบยอดรับเข้า)"
โชว์แผนของทุกสายทุกเดือนเหมือนกันหมด · วัดจริง: สลับดู vsm1/2/3/4 ได้ 274 รายการ / Order 321,211 ชิ้น
เท่ากันเป๊ะทั้งสี่สาย ทั้งที่ใบรับเข้าของ vsm1/vsm2 มี **0 ใบ**
แผงวินิจฉัยหัวป๊อปอัป (ไม่มีในแผนขาย / เริ่มผลิตแล้วไม่มียอดผลิต / ตรวจยอดรอผลิต) ก็ส่ง `b.wipIn.items`
ทั้งก้อนเข้าไป = ไม่สนตัวกรองเดือนเลย

แก้: `filterVsm` + รับพารามิเตอร์เดือน · แผงทุกอันยึดเดือนที่เลือกในตัวกรองเดียวกับตารางข้างล่าง
(ยกเว้นโหมดที่ตั้งใจข้ามเดือนอยู่แล้ว: ค้างนาน / ไม่มีในแผน / ไม่มีใน Master) และมีป้าย 📅 บอกขอบเขตทุกแผง
หลังแก้: vsm1/vsm3 = 0 รายการ · vsm4 = 274 รายการ

**3) "ค้างในระบบตลอด กดส่งใหม่ก็ไม่หาย แก้ไวไฟแล้วก็ไม่หาย"**

ต้นเหตุอยู่ที่ `flushPendingWrites()` — `catch(e){ break; }` ตั้งอยู่บนสมมติฐานว่า "ส่งไม่ได้ = เน็ตไม่ดี"
ซึ่งจริงแค่ครึ่งเดียว ถ้าเซิร์ฟเวอร์ **ปฏิเสธ** ข้อมูลชิ้นนั้น (400/404/409/422) มันจะถูกปฏิเสธซ้ำตลอดไป
และ `break` ยังเป็น head-of-line blocking — งานที่อยู่ *หลัง* ชิ้นเสียก็ไม่ได้ส่งไปด้วย

แก้ 3 ชั้น: ① `api()` ติด `err.status` / `err.httpFail` / `err.timeout` มาให้ ② 4xx หรือลองเกิน 5 ครั้ง
= ยกออกไปกอง `_pendWDead` แล้ว **ส่งชิ้นถัดไปต่อ** ③ แถบสถานะเปลี่ยนเป็น **สีแดง** คนละแบบกับส้ม "รอเน็ต"
กดแล้วเห็นสาเหตุจริงจากเซิร์ฟเวอร์ + เลือกลองใหม่/ทิ้งรายตัว (`showDeadWrites`)

ทดสอบจริง: วางของเสีย (404) ไว้หัวคิวแล้วต่อด้วยของดี → ของดีผ่าน · ของเสียเข้ากอง dead พร้อมข้อความ
`HTTP 404 · Unknown endpoint` · แถบขึ้น "⛔ เซิร์ฟเวอร์ปฏิเสธ 1 รายการ — ไม่ใช่ปัญหาสัญญาณ"
(ของเดิมเคสนี้ = แถบส้มค้าง "2 รายการ" ตลอดไป กดส่งใหม่กี่ครั้งก็ไม่ขยับ)

### ✨ ของใหม่

**4) กราฟยอดรายวันแยกลูกค้า + ค่าเฉลี่ยต่อวัน** — ป๊อปอัป WIP IN 1.5 และ WIP OUT

แท่งซ้อนรายวัน (inline SVG ไม่พึ่งไลบรารีนอก — แอปต้องทำงานตอนเน็ตหลุด) · เส้นประ = ค่าเฉลี่ยต่อวัน ·
ชิปสรุปต่อกลุ่มลูกค้าพร้อมเฉลี่ยรายกลุ่ม · กดที่แท่งดูรายละเอียดของวันนั้นได้ (มือถือไม่มี hover)
**เฉลี่ยหารด้วย "วันที่มีงาน" ไม่ใช่วันปฏิทิน** — โรงงานหยุดเสาร์/อาทิตย์ ถ้าหารทั้งเดือนค่าจะต่ำกว่าจริง ~30%
WIP OUT ตัดยอดด้วย `fgByDate` รายวันชุดเดียวกับการ์ด → ผลรวมกราฟ = ตัวเลขบนการ์ดเป๊ะ

**5) สรุปเทียบรายการขาย แยกตามเดือนของ Order** — ทั้ง WIP IN และ WIP OUT

"เดือนของ Order" = เดือนของ `plan_date` (ผู้ใช้เลือก) · ตาราง เดือน × ลูกค้า → Order / รับเข้าหรือผ่าน FG /
ยังขาด / % · กางรายการที่ยังไม่ครบของเดือนที่เลือก พร้อมปุ่ม ✏️ 🗑 แก้แผนได้จากตรงนั้น
ของล็อตเดียวที่ขายหลายงวดถูกแบ่งแบบ **FIFO ตามวันส่ง** (กติกาเดียวกับ `_islBuildAlloc` ของบอร์ดติดตามงานขาย)
% ใช้ `(Order − ยังขาด) / Order` ไม่ใช่ `รับเข้า / Order` — ไม่งั้นของที่ผลิตเกินในบางแถวจะไปกลบแถวที่ยังขาด
จนขึ้น "98%" ทั้งที่ยังขาด 17,056 ชิ้น · ยอดที่ไม่มีแผนรองรับแจ้งแยกไว้ ไม่ให้ผลรวมไม่ตรงกับการ์ด

> **แก้รอบสอง (ผู้ใช้ทัก "น่าจะเอาของ VSM อื่นมารวมมั่ว")** — ตรวจแล้ว VSM ไม่ได้ปน
> (ใบรับเข้า 588 ใบ = vsm4 · 6 ใบ = vsm3 · ไม่มีใบไหนตกไปที่ `DEFAULT_VSM` เลย) และยอด 620,488 ของ ส.ค.
> ก็เป็นของจริง (303 ใบ กระจายตามวันทำงานปกติ ซ้ำจริงแค่ 3 กลุ่ม 2,365 ชิ้น) — เลข 120,295 ในภาพแรก
> เป็นค่าตอนซิงก์ยังไม่เสร็จ
> ตัวที่ผิดคือบรรทัด **"รับเข้าแล้วอีก 1,019,967 ชิ้น ไม่มีแผนขายรองรับ"** ที่ผมเพิ่งเพิ่มเข้าไป —
> คิดจาก (ยอดทั้งหมด − ที่จัดสรรได้) จึงเหมารวมของที่รับเข้าเดือน พ.ค./มิ.ย. **153,193 ชิ้น**
> ซึ่งตารางแผนไม่ได้เก็บย้อนไปถึง = ไม่มีอะไรให้เทียบตั้งแต่แรก (ผิดกติกาเดิมของระบบที่ว่า
> เตือน "ไม่มีในแผน" ได้เฉพาะเดือนที่มีแผนจริง ไม่งั้นติดธงยกเข่ง)
> แก้เป็นแยก 3 ส่วนให้ปิดยอดกันได้: **จัดสรรเข้าแผน 170,043 + ไม่มีแผนจริง 866,774 + ไม่มีเดือนให้เทียบ 153,193
> = 1,190,010** (เท่ายอดรับเข้าทั้งหมดพอดี) และ 866,774 **ตรงกับแผง "ไม่มีในแผนขาย" เดิมของแอปเป๊ะ** —
> ตัวเลขสองที่ในแอปเดียวกันต้องไม่ขัดกันเอง
> เพิ่มป้าย ⚠ "วันที่เสีย" ให้แถว `Order 2001-08` (แผน 4 ใบที่ import มาสลับวัน↔เดือน) และเขียนกำกับว่า
> ตารางนี้นับ **ทุกช่วงเวลา** ไม่ใช่เดือนเดียวกับกราฟด้านบน

**6) หน้าวางแผน — กราฟ Order / WIP IN / WIP OUT แยกลูกค้า + คำเตือนเทียบวันที่ขาย**

การ์ดต่อกลุ่มลูกค้า 3 แถบสเกลเดียวกัน + บอกว่ายังต้องส่งอีกเท่าไร / ของยังไม่เข้าอีกเท่าไร
คำเตือนแบ่งตามความเร่งด่วนจริง ไม่ติดธงยกเข่ง: ⏰ เลยวันขายแล้วยังส่งไม่ครบ · ⚠️ ถึงกำหนดใน 3 วัน ·
📥 ใกล้กำหนดแต่ของยังไม่เข้าเลย · **ยึดแถวที่กำลังกรองอยู่บนหน้านั้น** เพื่อให้กราฟอธิบายตารางข้างล่างเสมอ

**7) หน้าวางแผนจัดรูปแบบใหม่** (ฟังก์ชันเดิมครบ · id เดิมทุกตัว)

ปุ่ม 5 ปุ่ม → ปุ่มหลัก 1 + เมนู ⋯ · กล่องกรอง 4 ช่องเต็มความกว้าง → แถวเดียว ·
การ์ดสรุป 4 ใบ → ชิปแถวเดียวที่ **กดเพื่อกรองสถานะ** ได้ · ปฏิทินรายเดือนพับได้ (จำสถานะข้ามการเปิดแอป)

**8) ดับเบิลคลิกแก้แผนจากบอร์ดได้เลย**

- ดับเบิลคลิก **ช่องยอด** → แก้/เพิ่ม/ลบ ยอดของวันนั้นทีละล็อต · ปุ่ม − / + ทีละ 100 · พิมพ์สูตรได้
- ดับเบิลคลิก **Valve No** → เพิ่ม/ลบเบอร์นั้นในแผนของเดือน (แก้วันขาย/Lot/ยอด หรือเพิ่มวันขายใหม่)
- เขียนลง `production_plans` (D1) ผ่าน `_planSave` ตัวเดียวกับหน้าวางแผน แล้ววาดใหม่ทั้ง **หน้าวางแผน + บอร์ด IOT + การ์ด WIP** พร้อมกัน
- คลิกเดี่ยว (เปิดป๊อปอัปรายละเอียด) ถูกหน่วง 250ms แล้วยกเลิกเมื่อคลิกที่สองมา — ไม่งั้นป๊อปอัปเดิมจะเด้งซ้อนทุกครั้ง
- **กดค้าง 500ms = เท่ากับดับเบิลคลิก** เพราะระบบนี้มือถือเป็นหลัก และดับเบิลแท็ปบนมือถือมักไปโดน zoom แทน

**9) ตัวอักษรสำหรับมือถือ**

ตัวเลขสรุปใช้ `clamp()` — ย่อเองให้พอดีกล่อง แทนที่จะถูกตัดกลางคัน ("215,409" เคยโชว์เป็น "215,40") ·
`tabular-nums` ทุกที่ที่เป็นตัวเลข (หลักตรงแนว ไม่ขยับตอนค่าอัปเดต) · ป้ายกำกับขึ้น 2 บรรทัดได้แทนการตัดคำทิ้ง ·
พื้นล่างขนาดตัวอักษร 10.5px (ของเดิมมี 9.5–10px กระจายอยู่ อ่านไม่ออกในโรงงาน) · line-height 1.6 สำหรับไทย ·
รหัส Valve/Lot ยาว ๆ ตัดคำได้ ไม่ดันหน้าให้เลื่อนซ้ายขวา
ตรวจที่ 375px แล้ว: ไม่มี element ไหนเนื้อหาล้นกรอบ และหน้าไม่เลื่อนแนวนอน

## v3.54 — 2026-08-13 — เพิ่มสาย "QC" เข้าระบบทั้งชุด (Man · Machine · 2 Zone · ผัง IOT · IOT รวม) · VSM3 เปลี่ยนเป็นสีโทนเย็น  ✅ deployed

**✅ deployed 2026-08-13** — ขึ้นครบทั้ง 2 ที่ที่เสิร์ฟแอป

| ปลายทาง | สถานะ |
|---|---|
| GitHub Pages | commit `654d533` · run [31653056136](https://github.com/TopMaha/vsm4-aftermarket-record/actions/runs/31653056136) → `success` |
| Worker `vsm4-api` | Version ID `837610a8-1703-42af-89a6-804231802137` (bundle `index.html` ใหม่) |

ตัว Worker **ไม่ได้แก้โค้ด** (QC ใช้ตาราง/endpoint เดิมทั้งหมด) แต่ต้อง deploy ซ้ำเพราะมันบันเดิล `index.html` ไว้ในตัว
ยืนยันของจริงแล้วด้วย curl ทั้งสอง URL → เจอ `key:'qc'` + `vsm3:{tint:'#CCFBF1',line:'#0D9488'}` ครบทั้งคู่
สำรองไฟล์ก่อนแก้ไว้ที่ `backups/index.before-qc-20260813.html`

### ที่มา

ผู้ใช้ส่งเอกสาร **"OAE Lay out QC (2).xlsx"** (4 sheet: Machine · Man · IOT · IOT Total) พร้อมโจทย์ 4 ข้อ

1. เพิ่มรายการทุกอย่างเหมือน VSM — IOT · Man · Machine พร้อมใช้งาน
2. zone มี 2 Zone
3. หน้า IOT รวม ตาม Sheet "IOT Total"
4. Process งานแต่ละเครื่อง และ Valve ยังไม่กำหนด — จะต่างกับ VSM

### สิ่งที่ทำ

**1) QC = "สาย" ที่ 5 ของระบบ** — `VSM_LIST` += `{ key:'qc', label:'QC' }`

ทุกอย่างที่วิ่งด้วย `VSM_KEYS` / `ZONE_DEFS` ได้ QC ตามไปเองทั้งหมด: ปุ่มสลับสาย (IOT + หน้าตั้งค่า) ·
ฟอร์มบันทึก · Dashboard · ประวัติ · เป้าราย Zone · ต่อกะ · แผง KPI รายชั่วโมงของหน้า IOT รวม

**2) Zone (2 โซน)** — `QCZ1 = QC AFTER MARKET` (5 เครื่อง) · `QCZ2 = QC OEM` (20 เครื่อง)

**3) Machine 25 เครื่อง** (`QC_MACHINES` → `SEED_VSM_MACHINES`) — id ตาม Sheet Machine

- `process` ตามคอลัมน์ Process (Leak&Visual · Visual · Mark identify · Crack detec · Packing) — เก็บเป็นป้ายของเครื่องเท่านั้น
- `target 2000 / man_std 0.5` = ค่า default ชั่วคราวเหมือนตอนเปิดสาย VSM1/VSM2 ครั้งแรก → ต้องตั้งค่าจริงที่หน้าตั้งค่า

**4) Man 39 คน** — `VSM_EMP_MAP` (กฎกลาง) + `SEED_VSM_EMPLOYEES` · ตรวจแล้วไม่มีรหัสซ้ำกับสายอื่นสักคน
→ ล็อกอินด้วยรหัส QC แล้วเห็นเฉพาะสาย QC · dropdown พนักงานในฟอร์มโชว์เฉพาะคน QC

**5) ผัง IOT ของ QC** (`FPQ_MC` 25 กล่อง) — พิกัดจริงจาก Sheet "IOT" (drawing shapes: EMU ÷ 9525 → px)

- `FP_HAS_PLAN.qc = 1`
- เพิ่ม "กรอบ Zone" บนผัง (เฉพาะกล่องที่มี field `z`) เพราะ 2 กลุ่มของ QC อยู่คนละมุมผัง ถ้าไม่ตีกรอบจะดูไม่ออกว่ากลุ่มไหนเป็นโซนอะไร
  — สายอื่นไม่มี field `z` จึงวาดเหมือนเดิมเป๊ะ

**6) หน้า IOT รวม (req #3)** — QC วางตามตำแหน่งจริงในโรงงาน ซึ่งอยู่คนละมุมกัน (ผู้ใช้ชี้ตำแหน่งมาในรูป)

- `QCZ2` (OEM) → **มุมซ้ายบน** เหนือ VSM3 ที่ offset (0, −300) → พิกัด y ติดลบ ผังจึงขยายขึ้นข้างบน
- `QCZ1` (After market) → **มุมขวาล่าง** ขวาของกรอบ VSM4 ที่ offset (1340, 1107)
- ผังรวมเดิมยึดมุมซ้ายบน = 0,0 เสมอ (คิดแค่ maxX/maxY) → เปลี่ยนมาคิด minX/minY ด้วย ไม่งั้นกลุ่มที่อยู่เหนือ y=0 ถูกตัดหาย
- กรอบพื้นที่ของ QC ต้องตี **แยกราย Zone** (กล่องที่มี field `z`) — ถ้าตีกรอบเดียวรวมทั้งสาย กรอบจะกินทั้งผัง · ป้ายกรอบใช้ชื่อโซน (QC OEM / QC AFTER MARKET)

- กรอบสีของสาย = ฟ้าน้ำทะเล `#CFFAFE / #0E7490` (ไม่ชนเขียว/ส้ม/เทา ของสถานะเครื่อง)
- กล่องบนผังรวมเดิมแยกสายด้วยเลข `v:1-4` → เพิ่ม `vk` (คีย์สายตรง ๆ) + helper `_iaBoxVsm(b)` สำหรับสายที่ชื่อไม่ใช่ `vsmN`
- ผังรวมตอนนี้ 341 กล่อง (VSM1 53 + VSM2 73 + VSM3 126 + VSM4 64 + QC 25) · viewBox = -14 -314 2046 1980

**7) ปลีกย่อย**

- หัวแอป: QC = `QC INSPECTION RECORD` (ไม่ใช่ AFTERMARKET)
- ต่อกะ: QC = บอร์ดเดียว (ไม่มีหัวข้อ Induction) ป้าย "QC" — ท่าเดียวกับ VSM1/2 ที่รวมเป็น "Forge"
- `PROC_VOCAB_FALLBACK` += 5 process ของ QC เพื่อให้ทุกเครื่องถอดรหัส process ของเครื่องได้ตรงกัน
- หน้าตั้งค่า: dropdown VSM ของ "เพิ่มพนักงาน" + ตัวกรอง เพิ่มตัวเลือก QC
- ป้ายหัวหน้า IOT รวม: "IOT รวมทุก VSM · VSM 1·2·3·4" → "IOT รวมทุกสาย · 1·2·3·4·QC" (สร้างจาก `VSM_KEYS` ไม่ต้องมาแก้มืออีก)

### ข้อ 4 (Process / Valve) — ทำอะไรไว้บ้าง

ยัง **ไม่** ผูก QC เข้ากับ Process/Valve Master ของ VSM ตามที่แจ้งว่ายังไม่กำหนด
ฟอร์มบันทึกยังลง OPR/Target/เวลา/คน ได้ครบโดยไม่ต้องมีวาล์ว (ระบบบังคับ Process เฉพาะเมื่อกรอกวาล์วที่มี Process ในมาสเตอร์)
ช่อง Valve/Process ในฟอร์มยังโชว์อยู่ตามปกติ — ถ้าต้องการซ่อนสำหรับ QC หรือใส่ Process ชุดของ QC เอง สั่งเพิ่มได้

### ⚠️ ที่ต้องยืนยัน — คอลัมน์ zone ใน Sheet Machine ขัดกับกลุ่มบนผัง 3 คู่

| เครื่อง | zone ใน Sheet Machine | กลุ่มบนผัง (Sheet IOT / รูปใน IOT Total) | ที่ใช้ในระบบ |
|---|---|---|---|
| Etching1 | 1 | กลุ่ม OEM | QCZ2 (OEM) |
| Etching3 | 2 | กลุ่ม After market | QCZ1 (After market) |
| D502 | 1 | กลุ่ม OEM | QCZ2 (OEM) |
| D506 | 2 | กลุ่ม After market | QCZ1 (After market) |
| Packing OEM | 1 | กลุ่ม OEM | QCZ2 (OEM) |
| Packing Aftermarket | 2 | กลุ่ม After market | QCZ1 (After market) |

จำนวนตรงกันทั้งสองทาง (5 / 20) และ Q-545 · Visual Aftermarket ตรงกันอยู่แล้ว → เดาว่าคอลัมน์ zone กรอกสลับคู่กัน
ระบบจึงยึด "กลุ่มบนผัง" ไว้ก่อน · ถ้าจะสลับให้ตรงคอลัมน์ zone แก้ที่ `QC_MACHINES` + `FPQ_MC` (field `z`)

### ตรวจแล้ว (เปิดจริงบน http://localhost:8791)

- สลับครบ 5 สาย ไม่มี error: VSM1 53 / VSM2 73 / VSM3 126 / VSM4 64 / **QC 25 เครื่อง** · ผังเดิมของ 4 สายไม่เปลี่ยน
- QC: ztabs 2 โซน · ฟอร์มบันทึกขึ้นการ์ดเครื่องครบ (QCZ1_0…QCZ1_4 พร้อมช่อง OPR/Target/CT/เวลา/คน)
- dropdown พนักงานของช่อง QC คืนเฉพาะคน QC (39 คน) · Machine Master โชว์ 25 เครื่องพร้อม process
- เป้าราย Zone: `tgt-qcz1-*` / `tgt-qcz2-*` ครบ 10 ช่อง
- IOT รวม: 341 กล่อง · กรอบพื้นที่ 6 อัน (VSM 1/2/3/4 + QC OEM + QC AFTER MARKET) · แถว KPI รายชั่วโมงมี QC
- ตำแหน่ง QC บนผังรวม: OEM (1,−300)–(293,−17) เหนือกรอบ VSM3 (เริ่ม y=127) · After market (1790,1500)–(2018,1640) ขวาของกรอบ VSM4 (จบ x=1760) — ไม่ทับใครทั้งคู่

### เพิ่มเติม — สีของ VSM3 เปลี่ยนเป็นโทนเย็น (ผู้ใช้สั่ง)

ผู้ใช้ชี้ 2 จุดในรูป: แถบหัว **VSM 3** ในแผง KPI รายชั่วโมง + กรอบพื้นที่ **VSM 3** บนผังรวม → ขอเป็น "สีโทนเย็น"

ทั้งสองจุดใช้สีจากตัวเดียวกัน (`_IA_VSM_META.vsm3`) → แก้ที่เดียวเปลี่ยนพร้อมกัน

| | เดิม | ใหม่ |
|---|---|---|
| พื้นกรอบบนผัง (`tint`) | `#FCE7F3` ชมพู | `#CCFBF1` เขียวน้ำทะเลอ่อน |
| เส้นกรอบ + ป้ายชื่อ + แถบหัวตาราง (`line`) | `#BE185D` ชมพูเข้ม | `#0D9488` teal |

เลือก teal เพราะเป็นโทนเย็นช่องเดียวที่ยังว่าง — VSM1 น้ำเงิน · VSM2 คราม · VSM4 ม่วง · QC ฟ้าน้ำทะเลเข้ม
และยังไม่กลืนกับสีชิปเครื่องบนผัง (เขียว `#dcfce7` = OAE ดี · ส้ม `#ffedd5` = OAE ต่ำ · เทา = ปิด) เพราะ teal อมฟ้ามากกว่าเขียวมิ้นต์ที่เคยมีปัญหา

## v3.53 — 2026-08-12 — ติดตามงานขาย: ล็อตเดียวขายหลายงวด ต้องแบ่งของตามลำดับวันส่ง (ไม่ใช่ให้ทุกงวดถือยอดเต็ม)  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `291d3cc2-45a8-48d1-a9e1-fb2d5b9c9b9f` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `deae47e` (run `137` · conclusion `success`) → https://topmaha.github.io/vsm4-aftermarket-record/

> ยืนยันของจริงแล้วทั้งสองที่ (curl พร้อม `?cb=`): หัวเว็บขึ้น `v3.53` · มี `_islBuildAlloc` / `q-short` / `ismp-alloc` ครบ · ไม่เหลือท่า dedupe เดิมของ `_islSumQty`
> เปิดหน้าจริงบน Worker แล้วค้น `2629`: ช่อง 07/08 `q-done 1,100` · ช่อง 14/08 **`q-short 1,800 · ขาด 1,800`**
> tooltip: `ของพร้อมส่งงวดนี้ 0 (0%) · ขาด 1,800 — ของล็อตเดียวกันถูกงวดที่ถึงก่อนตัดไปแล้ว 1,100 ชิ้น`

### ที่มา

ผู้ใช้ชี้ที่บอร์ด "ติดตามงานขาย" เดือน ส.ค. 26 · `VV2629M0`:

```
07/08  1,100 ชิ้น  → เขียว (ส่งได้)
14/08  1,800 ชิ้น  → เขียว (ส่งได้)   ← ผิด
```

> "งานผลิตมา 1100 แต่ระบบลงว่าจะส่งได้ 2 อัน ไม่ถูกต้อง — หากวันที่ 07/08 ขายงาน
> งานที่จะขายได้วันที่ 14/08 จะไม่มีเลย ดังนั้นวันที่ 14/08 จะเป็นสีแดง"

ของจริงในฐานข้อมูล: ทั้งสองงวดเป็น **ล็อตเดียวกัน** `2607S070244` ที่ผลิตได้ 1,100 ชิ้น (FG)

```
P-msfxvw8wqa9f  2607S070244  2026-08-07  1,100
P-msfxvw8whuaq  2607S070244  2026-08-14  1,800   ← ล็อตเดียวกัน คนละงวด
```

`_islRow()` เดิมผูก "ยอดที่ผลิตได้" ไว้กับ **ล็อต** แล้วให้ทุกบรรทัดแผนของล็อตนั้นอ่านค่าเดียวกันหมด
→ ของกอง 1,100 ถูกนับเป็นของทั้งงวด 07/08 และงวด 14/08 พร้อมกัน ทั้งที่มีอยู่กองเดียว

### สิ่งที่ทำ

**1) จัดสรรของให้แต่ละงวดตามลำดับวันส่ง (FIFO)** — `_islBuildAlloc()` / `_islLotProd()`

- งวดที่ถึงก่อนตัดของก่อนจนเต็มยอดสั่ง เหลือเท่าไรจึงไหลไปงวดถัดไป
  → `07/08 = 1,100/1,100 (ครบ)` · `14/08 = 0/1,800 (ขาด 1,800)`
- คิดจากแผนขาย **ทุกเดือน** ของล็อตนั้น ไม่ใช่เฉพาะเดือนที่เปิดดู — ของที่งวดเดือนก่อนตัดไปแล้วต้องหายไปจริง
  (ของจริงมี 5 ล็อตที่งวดส่งคร่อมเดือน · `VV4538A0::2605S050345` ผลิตได้ 400 ถูกงวด 30/07 ตัดไปหมด → งวด 31/08 เหลือ 0)
- ผลิตเกินยอดสั่งทุกงวด → ส่วนเกินตกที่งวดสุดท้าย ⇒ **Σ ที่จัดสรร = ยอดที่ผลิตได้ของล็อตเสมอ**
  ตรวจกับข้อมูลจริงครบ 241 ล็อต ไม่มีล็อตไหนเพี้ยน · ยอดรวมทั้งเดือน ก.ค. เท่าเดิมเป๊ะ (94,888)
- คีย์เป็น "ตัวแผนเอง" (object) ไม่ใช่ `plan_id` → แถวที่ยังไม่มี id ก็จัดสรรได้
- `_islSumQty()` เลิก dedupe ด้วย `jobKey` แล้ว (ของถูกแบ่งไม่ให้ซ้ำกันตั้งแต่ต้นทาง) — บวกตรง ๆ

**2) สถานะใหม่ "ของไม่พอส่ง" (แดงเส้นประ)** — `st: 'short'`

- ติดธงเฉพาะกรณี **ของถูกงวดที่ถึงก่อนตัดไปจนงวดนี้ไม่เหลือพอ** (`taken > 0 && short > 0`)
  ไม่ใช่ทุกแถวที่ยังผลิตไม่ครบ — งานที่แค่ "ยังผลิตไม่ถึง" ยังเป็นส้ม/เทา + แถบ % เหมือนเดิม
  (ถ้าติดแดงยกเข่ง 176 แถวจะแดงเกือบหมดจนไม่มีใครดู · ของจริงติดธง 9 แถว)
- แยกจาก "เลยกำหนด" ด้วย **กรอบเส้นประ** ไม่ใช่ต่างที่สีอย่างเดียว
- "เลยกำหนด" เพิ่มเกณฑ์: จบงานแล้วแต่ของถูกงวดก่อนหน้าตัดไปจนส่งไม่ได้ ก็คือเลยกำหนด (ของจริงยังไม่มีเคสนี้ → ตัวเลขเท่าเดิม 12 / 4)
- แผ่นตัวเลขสรุปเพิ่มปุ่ม `ของไม่พอส่ง` กดกรองได้เหมือนสถานะอื่น

**3) บอกให้ครบว่า "ของหายไปไหน"** — ไม่ใช่แค่เปลี่ยนสี

- ช่องวันที่: ป้าย `ขาด 1,800` ในช่อง + tooltip `ของล็อตเดียวกันถูกงวดที่ถึงก่อนตัดไปแล้ว 1,100 ชิ้น`
- ป๊อปอัปรายล็อต: แถบ `🧮 ของทั้งล็อตที่ผลิตได้ 1,100 ชิ้น — แบ่งให้แต่ละงวดตามลำดับวันส่ง:`
  `07/08 · 1,100/1,100` (เขียว) · `14/08 · 0/1,800 · ขาด 1,800` (แดง) · งวดที่กำลังเปิดดูมีขอบเน้น
- มุมมองรายการ: คอลัมน์ Q'ty = ของที่พร้อมส่ง "งวดนี้" + `ขาด N` · tooltip บอกยอดทั้งล็อตและส่วนที่งวดก่อนหน้าตัดไป
- คำอธิบายท้ายบอร์ดเขียนกติกา FIFO ไว้ตรง ๆ

### ตรวจแล้ว (เบราว์เซอร์ · ข้อมูลจริงจาก D1)

```
VV2629M0  07/08  1,100/1,100  ผลิตเสร็จ (เขียว)
          14/08      0/1,800  ของไม่พอส่ง · ขาด 1,800 (แดงเส้นประ)   ← ตรงกับที่ผู้ใช้ชี้
          21/08    740/1,800  ผลิตเสร็จ (เขียว — ไม่ถูกติดธงเกิน เพราะไม่มีงวดไหนตัดของไป)
          28/08    420/1,900  COT (ส้ม)
```

Σ ที่จัดสรรของทุกล็อต = ยอดผลิตได้ (mismatch 0/241) · ส.ค. `Q'ty ผลิตได้` 33,678 → 33,278 (−400 = ของที่งวด 30/07 ตัดไปก่อน) · ไม่มี error ใน console

## v3.52 — 2026-08-11 — IOT รวมทุก VSM: ตาราง OAE รายชั่วโมง (Time·Target·OPR·OAE·SOS) แทนแถบ OAE ยาว ๆ · VSM3 หารด้วยจำนวนเครื่องที่เปิด  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `3d9e2a98-caa7-4b1f-a1cf-c9413742e858` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `482019b` (run `135` · conclusion `success`) → https://topmaha.github.io/vsm4-aftermarket-record/

> ยืนยันของจริงแล้วทั้งสองที่ (curl พร้อม `?cb=`): หัวเว็บขึ้น `v3.52` · มี `_iaHourTblPop` / `_iaHourBuckets` / `_iaHtBody` / `iaht-div` ครบ
> และ **ไม่เหลือ `_iaHourOaeStrip` / `iaho-cell` เลย** (แถบ OAE ยาว ๆ ของเดิมถูกยกออกจริง)
> ตรวจตัวเลขในเบราว์เซอร์ก่อน deploy: 08.00–10.00 · Target 1500 · OPR 1000 → 08–09 = 750/500/66.67% · 09–10 = 750/500/66.67% (ยอด Σ รวมเท่าเดิม)
> F1 6 เครื่อง (Auto Line) ดิบ 6,000/3,000 → ตารางโชว์ **1,000/500 ÷6** · ชั่วโมงที่เดินแค่ 3 เครื่อง → ÷3 · ดูรายเครื่องไม่หาร · RECORD 12 ใบ → **2 ครั้งที่กรอก**

### ที่มา

แถบ "OAE รายชั่วโมง" เดิมท้ายการ์ด Zone/Line เป็นชิปเรียงยาวเลื่อนซ้าย-ขวา เห็นแต่ OAE ตัวเดียว
ผู้ใช้ขอ **เลิกใช้แบบยาว ๆ** แล้วทำเป็น **ตารางในหน้าต่างใหม่** ตามฟอร์มกระดาษที่ใช้หน้างาน:

```
Time            Target   OPR   OAE    SOS
08.00-09.00       750    500   66%    เปลี่ยนรุ่น
09.00-10.00       750    500   66%    เปลี่ยนรุ่น
```

เพราะพนักงานลงบันทึกเป็น "ช่วง" (08.00–10.00 · OPR 1000 · Target 1500) แต่หน้างานต้องอ่านเป็นรายชั่วโมง

### สิ่งที่ทำ

**1) ตาราง OAE รายชั่วโมง — หน้าต่างใหม่** — `_iaHourTblPop()` / `_iaHtBody()` / `_iaHourBuckets()`

- 24 แถวครบของ **วันผลิต 08.00 → 08.00 วันรุ่งขึ้น** (ไม่ข้ามชั่วโมงที่ว่าง เพราะฟอร์มกระดาษมีทุกช่อง)
  - ☀️ กะกลางวัน 08.00–20.00 (12 แถว) → แถว `Σ กลางวัน`
  - 🌙 กะกลางคืน 20.00–08.00 (12 แถว · 00.00–08.00 = ข้อมูลของวันก่อนหน้า) → แถว `Σ กลางคืน`
  - `Σ รวมทั้งวัน` ตรึงท้ายตาราง · หัวตาราง + คอลัมน์เวลาตรึงตอนเลื่อน
- **งานคร่อมหลายชั่วโมง = หารตามนาทีที่คร่อมจริง** (ไม่ใช่โชว์ค่าเดิมซ้ำทุกช่อง)
  08.00–10.00 · Target 1500 · OPR 1000 → ชั่วโมงละ Target 750 / OPR 500 / OAE 66.67% · ยอด Σ รวมกันได้เท่าเดิมเป๊ะ
  ลง 08:30–10:00 ก็ถ่วง 30 : 60 นาที · กะดึก 22:00–02:00 กระจายลง 22, 23, 00, 01
- คอลัมน์ SOS = ข้อความขอความช่วยเหลือของชั่วโมงนั้น (🆘 ยังค้าง / ✅ ปิดแล้ว) + ต่อรหัสเครื่องเมื่อดูหลายเครื่อง
- แถบเลือกวันที่ (โผล่เมื่อช่วงข้อมูลมีหลายวัน) · ปุ่มซ่อนชั่วโมงที่ไม่มีงาน · ธง ⚠️ นับ record ที่ไม่ได้ลงเวลา (ลงชั่วโมงไม่ได้ จึงไม่ถูกนับในตาราง — ไม่กลืนหายเงียบ ๆ)

**2) เข้าถึงได้ทั้งรายเครื่องและราย Line** — ปุ่ม `_iaHourBtn()` 6 จุด

การ์ด Zone ในป๊อปอัป VSM · หัวป๊อปอัปรายเครื่อง · การ์ดราย Line ของ VSM3 · หัวป๊อปอัปราย Line · หัวป๊อปอัปเครื่องในสาย · **และในการ์ดรายเครื่องทุกใบ**
ปุ่มโชว์ OAE รวมของกลุ่มค้างไว้ ข้อมูลจึงไม่หายไปจากการ์ดตอนถอดแถบเดิมออก

**3) เฉพาะ VSM3 — Target/OPR หารด้วยจำนวนเครื่องที่เปิด**

โหมด ⚡ Auto Line กรอกครั้งเดียว แต่ `saveAll()` เขียน **1 record ต่อ 1 เครื่องในสาย โดยใส่ค่าเต็มทุกใบ**
→ ดูรวมสาย/Zone จึงบวกได้เป็น N เท่า

```
F1 = G-2019 · G-283 · G-284 · G-289 · G-2096 · G-287  (6 เครื่อง)
ในฐานข้อมูล : Target 6,000   OPR 3,000   OAE 50%
ในตารางใหม่ : Target 1,000   OPR   500   OAE 50%     ← ÷6
```

- ตัวหารคิด**รายชั่วโมง** — ชั่วโมงไหนเดินแค่ 3 เครื่อง ก็ ÷3 (นิยาม "เปิด" = มี record ในช่วง ตามผังเครื่อง)
- OAE ไม่เปลี่ยน (หารทั้งเศษและส่วน) · **ดูรายเครื่องไม่หาร**
- **จำนวน record ยุบด้วย**: 12 ใบในฐาน = **2 ครั้งที่พนักงานกรอก** — ยุบด้วยคีย์ `วัน|กะ|time_start|time_end|OPR|valve_no|lot` (ใบในสายชุดเดียวกันเหมือนกันหมด ต่างแค่ machine_id/process) ไม่ใช่หารแล้วปัด จึงถูกทั้งกรณีเปิดเครื่องไม่เท่ากันและงานคร่อมหลายชั่วโมง
- ป้ายกำกับในตาราง: แบนเนอร์ `⚡ VSM3 · เฉลี่ยต่อเครื่อง` + ชิป `÷6` ต่อท้ายช่องเวลาทุกแถว + tooltip `n rec · n เครื่อง · หารด้วยเครื่องที่เปิด 6` → เห็นตัวหารของทุกชั่วโมง ไม่ใช่เลขที่โผล่มาลอย ๆ

**4) แก้พ่วง — Escape ปิดผิดชั้น** — `_iotAllKey()`

เดิมเจาะจงปิด `#iotAllPop` เท่านั้น → กด Escape ตอนเปิดป๊อปอัปซ้อน (รายเครื่อง/ราย Line) ไปปิดชั้นล่างแทน
ใหม่ = ปิด `.iotpop-bg` ชั้นบนสุดทีละชั้น (ตารางรายชั่วโมง → รายเครื่อง → ราย Line → VSM → ปิดหน้า)

### ที่ยกออก

`_iaHourOaeStrip()` + CSS `.iaho*` ลบทิ้งหมด (0 การอ้างอิงเหลือ)

### ยังไม่ได้แก้ (คนละที่ · อยู่นอกที่สั่ง)

ยอด OPR/Target ของ VSM3 ที่ **การ์ด KPI ราย Zone/Line (`zoneKpiInner`) · แผง KPI รายชั่วโมงคอลัมน์กลาง · Dashboard**
ยังเป็นค่าดิบ = N เท่า (OAE/DLE/PDAC ไม่กระทบเพราะเป็นสัดส่วน) → เลข OPR/Target จุดพวกนั้นจะไม่เท่ากับในตารางใหม่
และแผง KPI รายชั่วโมงยังใช้ท่าเก่า คือ record คร่อม 08–10 โชว์ค่าเดิมซ้ำทั้งสองช่อง ไม่ได้หารตามนาที

## v3.51 — 2026-08-09 — IOT หน้า 2: ตารางขายรายวัน (Valve No × วันที่) · กดตัวเลขดูล็อต+ตำแหน่งงาน · เลิกแบ่งจอซ้าย-ขวา  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `f1b0ddbd-f2f7-4f04-842d-b850a0de7846` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `01fa82b` (run `133` · conclusion `success`) → https://topmaha.github.io/vsm4-aftermarket-record/

> ยืนยันของจริงแล้วทั้งสองที่ (curl พร้อม `?cb=`): หัวเว็บขึ้น `v3.51` · มี `_ismHtml` / `_ismBindScroll` / `islCellPop` / `_islSumQty` / `ism-rng` / `ism-th2` ครบ · ไม่เหลือ `v3.50` และไม่เหลือ `_ISL_GROUPS` (ตัวแบ่งจอซ้าย-ขวาของเดิม) เลย
> เปิดบนเบราว์เซอร์จริงที่ Worker URL: โหลดแผน 266 · prod 10,000 record **ไม่มี error ใน console**
> ตารางขายรายวันขึ้น 140 แถว × 33 คอลัมน์ · ยอด **213,409 ตรงกันทั้ง 4 ทาง** (บวกทุกแถว / ช่องรวมแถวท้าย / บวกทุกคอลัมน์วันที่ / แผ่น KPI) · badRows 0/140 · badCols 0/31
> ป้ายช่วงวันที่ทำงานจริง (`👁 เห็นวันที่ 6–14 · ซ่อนอยู่ 22 วัน · 207,229 ชิ้น`) · กดช่องแล้วป๊อปอัปขึ้นครบ (flow 10 ชิป · "📍 ตอนนี้อยู่ที่ SFG 1,400 ชิ้น ล่าสุด 2026-08-08 🏭 G-2022 VSM4")

### ที่มา

บอร์ด "งานขายเดือนนี้" (IOT หน้า 2) เดิมเป็น **รายการรายล็อต** — 1 บรรทัด = 1 แผนขาย
ปัญหาที่ผู้ใช้แจ้ง: **Valve No เบอร์เดียวถูกขายหลายล็อต คนละวัน** เช่น

```
VF1349  Lot 12345  ขายวันที่ 01/08  1000
VF1349  Lot 65478  ขายวันที่ 03/08  1000
```

เบอร์เดียวกันกระจายอยู่คนละบรรทัด (และคนละที่ในตาราง ถ้าเรียงตาม due date)
คำถามจริงหน้างานคือ **"เบอร์นี้เดือนนี้ขายวันไหนบ้าง วันละเท่าไร"** — ของเดิมต้องไล่หาเอง

อีกเรื่องคือบอร์ดถูกแบ่งเป็น 2 ตารางซ้าย-ขวา (`MSI and SWE` | `OES&OEM`)
ทำให้แต่ละฝั่งเหลือความกว้างครึ่งเดียว และเทียบข้ามกลุ่มไม่ได้

### สิ่งที่ทำ

**1) มุมมองใหม่ "ตารางขายรายวัน" (sales matrix) — เป็นค่าเริ่มต้น** — `_islMatrix()` / `_ismHtml()`

- **แถว = Valve No · คอลัมน์ = วันที่ 1…สิ้นเดือน · ช่อง = ยอดขายของวันนั้น**

  ```
  Valve No        รวม    1 ส.ค.  2 ส.ค.  3 ส.ค.  4 ส.ค.
  VF1349  [MSI]  2,000    1000            1000
  ```

- ยุบทุกล็อตของเบอร์เดียวกันเป็นแถวเดียว แล้ววางตัวเลขให้ตรงคอลัมน์วันที่ที่ขายจริง
- ในช่องมี: ยอดขาย (ตัวหนา) + แถบเล็กใต้เลข = **ผลิตได้กี่ % ของยอดนั้น** + ป้าย `n ล็อต` เมื่อวันนั้นมีมากกว่า 1 ล็อต
- สีช่องบอกสถานะทันที (เทา ยังไม่เริ่ม · ส้ม กำลังผลิต · เขียว ผลิตเสร็จ · แดง เลยกำหนดขาย)
- คอลัมน์ `Valve No` + `รวม` **ตรึงซ้าย (sticky)** · หัวตาราง **ตรึงบน** · แถว `Σ รวมรายวัน` **ตรึงล่าง**
  → เลื่อนไปดูวันที่ 25 ก็ยังรู้ว่ากำลังอ่านเบอร์อะไร และยอดรวมของวันนั้นเท่าไร
- คอลัมน์ "วันนี้" ไฮไลต์สีน้ำเงิน + ขีดใต้ · เสาร์-อาทิตย์มีลายพื้นหลังจาง (ไม่ลดคอนทราสต์ตัวอักษร)
- เปิดบอร์ด/เปลี่ยนเดือน → **เลื่อนไปที่คอลัมน์ "วันนี้" ให้อัตโนมัติ** (ทำเฉพาะตอนตารางเป็นชุดใหม่ — auto-refresh ห้ามดีดจอคนที่กำลังดูอยู่)
- ปุ่มสลับ **ทุกวัน ⇄ เฉพาะวันที่มีงาน** (จำค่าไว้) — ส.ค. 26 บีบจาก 31 คอลัมน์เหลือ 17

**2) กดที่ตัวเลข = ป๊อปอัป "ล็อตอะไร ตอนนี้อยู่ที่ไหน"** — `islCellPop()` / `islValvePop()` / `_islJobAt()`

ตอบคำถามที่ผู้ใช้ถามตรง ๆ ว่า *"สถานะ Valve No / Lot ที่ขาย ตอนนี้อยู่ที่ไหน"*

- **กดที่ตัวเลขในช่อง** → ล็อตที่ขายวันนั้น · **กดที่ Valve No** → ทุกล็อตของเบอร์นั้นทั้งเดือน
- แต่ละล็อตแสดง: Lot No. · สถานะ · วันที่ขาย + นับวัน (เลยมา/เหลืออีก) · ยอดผลิตได้ / ยอดขาย + %
- 📍 **"ตอนนี้อยู่ที่"** = process ที่ไกลสุดตาม flow ของวาล์วนั้น + ยอดชิ้น + วันที่ล่าสุด + เครื่องจักร + สายที่ผลิต
- **แถบ flow เต็มเส้น** — ชิปทุก process ของวาล์วนั้นเรียงตามลำดับ:
  เขียว = ผ่านแล้ว (มียอด) · น้ำเงินมีวงแหวน = ตำแหน่งปัจจุบัน · เทา = ยังไม่ถึง
  ม่วง = process ที่ลงไว้แต่ **ไม่อยู่ใน flow** ของวาล์วนี้ (สัญญาณว่าอาจลงผิดขั้น)
- ปุ่ม 🕘 ประวัติการผลิต ต่อไปยัง `showWipHistory()` ของเดิม
- ใช้ `filterVsmPeer` → รวมสายพี่น้อง VSM3 ⇄ VSM4 (ล็อตเดียววิ่งข้ามสายได้)
- ตอน IOT เต็มจอใช้ `_openIotPop()` (native fullscreen โชว์ modal ปกติไม่ได้) — ตรวจแล้วว่าเด้งถูกตัวทั้ง 2 โหมด

**3) เลิกแบ่งจอ ซ้าย MSI and SWE | ขวา OES&OEM** — ตัด `_ISL_GROUPS` ทิ้ง

- ตารางเดียวเต็มความกว้างเสมอ ทั้งมุมมองตารางรายวันและมุมมองรายการ
- อยากดูเฉพาะกลุ่มไหน ใช้ **ปุ่มกรองกลุ่มลูกค้าบนแถบหัว** ที่มีอยู่แล้ว (ทั้งหมด / MSI / SWE / OES&OEM / อื่น ๆ)
- มุมมองรายการเพิ่ม **คอลัมน์ "ลูกค้า"** (ชิปสี) — เดิมรู้จากหัวตารางฝั่งที่แถวอยู่ พอรวมตารางแล้วต้องบอกในแถว
  พร้อมทำให้เรียงตามลูกค้าได้ (`islSetSort('cust')`)

**4) ปุ่มสลับมุมมอง 📅 ตารางรายวัน ⇄ 📋 รายการ** — `islSetView()` (จำค่าใน localStorage)

มุมมองรายการยังอยู่ครบ เพราะเห็น `Lot No. / Due date / Status / Remark` ของทุกล็อตในแถวเดียว
ตารางรายวันตอบ "ขายวันไหนบ้าง" · รายการตอบ "ล็อตนี้รายละเอียดอะไร" — คนละคำถาม

**5) ตัวกรองเดิมอยู่ครบทุกตัว**

เลือกเดือน ◀▶ · กลุ่มลูกค้า · ช่องค้นหา Part No / Lot (ไฮไลต์ตัวที่ตรง) · แผ่นตัวเลขกดกรองสถานะ
· เรียงตาม (dropdown + กดหัวตาราง) · ปุ่มซูมตัวอักษร − ⊙ + · แถบเตือน "รับเข้าแต่ไม่มีในแผนขาย"
ในมุมมองตารางรายวัน ตัวเลือกการเรียงจะตัด `Lot No.` / `สายที่ผลิต` ออก (1 แถว = 1 Valve No ไม่ใช่ค่าของแถว)

### แก้บั๊กที่เจอระหว่างทาง

**ยอด "Q'ty ผลิตได้" นับซ้ำ** — `_islSumQty()`

`r.qty` ผูกกับ **(valve + lot)** ไม่ใช่กับบรรทัดแผนขาย
ถ้าล็อตเดียวถูกวางแผนขาย 2 วัน → เป็น 2 แถว แต่ **ของที่ผลิตได้มีกองเดียว** ของเดิมบวกทั้ง 2 แถว

> ของจริง ส.ค. 26: มีบรรทัดแผนที่ซ้ำ (valve+lot) อยู่ **17 บรรทัด**
> ตัวเลขบนแผ่น KPI จึงเพี้ยนจาก **63,259** → ที่ถูกคือ **33,620** ชิ้น (เดิมพองขึ้น ~88%)

ตอนนี้นับ 1 ครั้งต่อ 1 ล็อตทั้งระดับช่อง / ระดับแถว / ระดับแผ่น KPI
**ยอด Order ยังตรงเป๊ะทุกชั้น** — `Σ รวมรายวัน` = `Σ คอลัมน์รวม` = แผ่น KPI Order = `213,409`

**เต็มจอบนจอแคบ (≤820px) ตารางไม่ยอมเลื่อนในตัวเอง**

`@media (max-width:820px)` เปลี่ยน `.iot-fs` เป็น flex column ที่เลื่อนทั้งการ์ด
→ `.ism-sc` โดน `max-height:none` เลยยืดเป็น 5,400px หัวตาราง/แถวรวมไม่ค้าง และแถบหัวบอร์ดเลื่อนหายไปด้วย
ล็อก `max-height:68vh` คืนให้ตัวตาราง (ทั้ง `.ism-sc` และ `.isl-sc`)

**สีคอลัมน์ "วันนี้" โดนสีเสาร์-อาทิตย์ทับ** — วันนี้ (9 ส.ค.) ตรงกับวันอาทิตย์พอดี
`.ism-hc.we` (0,3,1) ชนะ `.ism-t th.today` (0,2,1) → เพิ่ม specificity เป็น `.ism-t th.ism-hc.today`

### รอบแก้ที่ 2 — "ทำไม 213,409 กับ 180,399 ไม่เท่ากัน" (ผู้ใช้ทักมา)

**ตรวจแล้ว: ตัวเลขถูกต้องทั้งหมด ไม่มีบั๊ก** — พิสูจน์จาก DOM ตรง ๆ (ไม่ผ่านตัวคำนวณ)

| ตรวจ | ผล |
|---|---|
| บวกช่อง "รวม" ทุกแถว | 213,409 |
| ช่องรวมในแถวท้าย | 213,409 |
| **บวกทุกคอลัมน์วันที่ 1–31 ในแถวท้าย** | **213,409** |
| แผ่น KPI Order | 213,409 |
| แถวที่ "รวม" ≠ ผลบวกช่องในแถวนั้น | **0 / 140 แถว** |
| คอลัมน์ที่แถวท้าย ≠ ผลบวกของแถวทั้งหมด | **0 / 31 คอลัมน์** |

`180,399` คือ **ยอดของวันที่ 31 วันเดียว** ไม่ใช่ยอดรวม — ที่ดูเหมือนหายไปคือวันที่ 1–7
ซึ่ง**เลื่อนพ้นจอไปทางซ้าย** (วันที่ 5 = 50 · 6 = 400 · 7 = 3,780 → 4,230 ชิ้น) บวกกับวันอื่นที่เห็นในจอ

**ปัญหาจริงคือการจัดวางทำให้อ่านผิด** — เลข "รวมทั้งเดือน" ไปนั่งติดกับเลขรายวันในแถวเดียวกัน
เลยอ่านต่อกันเป็นชุดเดียว จึงแก้ที่การนำเสนอ:

**1) ป้าย "เห็นวันที่ไหนอยู่ · ซ่อนอยู่กี่วัน กี่ชิ้น"** — `_ismBindScroll()` (ต้นเหตุของความเข้าใจผิด)

- ป้ายบนแถบหัวสีน้ำเงิน อัปเดตตามการเลื่อนแบบเรียลไทม์:
  `👁 เห็นวันที่ 8–31 · ซ่อนอยู่ 7 วัน · 4,230 ชิ้น`
- **เห็น + ซ่อน = ทั้งเดือนเสมอ** (ตรวจแล้วทุกตำแหน่งการเลื่อน: 28,780 + 184,629 = 213,409)
- กดป้าย: โหมด "ทุกวัน" → บีบเหลือ "เฉพาะวันที่มีงาน" ทันที (ส.ค. 26: 31 → 17 คอลัมน์ พอดีจอ ไม่ต้องเลื่อนเลย)
  · โหมดบีบแล้ว → เลื่อนกลับไปต้นเดือน
- **ห้ามใช้ `requestAnimationFrame` ผูกป้ายนี้** — ถ้าแท็บถูกซ่อน/หน้าต่างอยู่เบื้องหลัง callback ไม่ถูกเรียกเลย
  ป้ายจะค้างว่างจนกว่าผู้ใช้จะเลื่อนเอง (เจอตอนทดสอบจริง) → ใช้ `upd()` ตรง ๆ + `setTimeout(…,0)` + `ResizeObserver`

**2) คอลัมน์ "รวม" ต้องอ่านออกว่าเป็นช่องสรุป ไม่ใช่วันที่อีกวัน**

- หัวคอลัมน์ 2 บรรทัด: **รวม** / *ทั้งเดือน* (เดิมมีแค่ "รวม" คำเดียว)
- พื้นฟ้าอ่อน + เส้นคั่นหนากับคอลัมน์วันที่ (เดิมพื้นเดียวกันกับช่องวันที่ทุกประการ)
- แถวท้ายสุด ช่องนี้มีป้าย **"ทั้งเดือน"** กำกับใต้ตัวเลข · ป้ายฝั่งซ้ายเปลี่ยนจาก `Σ รวมรายวัน` → `Σ รวม`
  (ของเดิมเขียน "รวมรายวัน" แล้ววางเลขทั้งเดือนไว้ข้าง ๆ = ขัดกันเอง)
- tooltip ทุกช่องบอกที่มาของตัวเลข: ช่องรวมของแถว = "ผลรวมของทุกช่องวันที่ในแถวนี้"
  · ช่องรวมแถวท้าย = "ผลรวมของทุกคอลัมน์วันที่ รวมวันที่ยังเลื่อนไม่ถึงด้วย — ไม่ใช่ยอดของวันใดวันหนึ่ง"
- คำอธิบายใต้ตารางเพิ่มบรรทัดย้ำเรื่องนี้โดยเฉพาะ

**3) อ่านง่ายขึ้นทั้งกระดาน**

- **เส้นคั่นต้นสัปดาห์ (วันจันทร์)** — นับคอลัมน์ข้ามเดือนได้โดยไม่ต้องไล่อ่านเลขวันทีละช่อง
- **ลายสลับแถวชัดขึ้น** `#FBFCFE` → `#F5F8FD` — ตารางกว้าง 31 คอลัมน์ ถ้าลายจางสายตาจะหลุดแถวตอนกวาดไปทางขวา
- หัวคอลัมน์วันที่มี tooltip บอกยอดขายรวมของวันนั้น
- แถวที่ยังไม่ผลิต: ใต้ Valve No เปลี่ยนจาก `ผลิตได้ 0` → `ยังไม่เริ่มผลิต` · `0%` ในช่องรวมทำสีจางลง
- คอลัมน์รวมกว้างขึ้น 74 → 88px (มือถือ 70px) รองรับเลข 6 หลักไม่ตัดคำ

### ที่ตรวจแล้ว (ของจริง 266 แผน · 1,219 วาล์ว · 10,000 prod record · ส.ค. 26)

| เรื่อง | ผล |
|---|---|
| ตารางรายวัน | 140 แถว × 33 คอลัมน์ (Valve + รวม + 31 วัน) |
| ยอด Order ตรงกันทุกชั้น | `Σ รวมรายวัน` = `Σ คอลัมน์รวม` = KPI = **213,409** ✓ |
| Q'ty กันนับซ้ำ | 63,259 → **33,620** (ซ้ำ 17 บรรทัด) |
| sticky 3 ด้าน (บน/ซ้าย/ล่าง) | ปกติ + เต็มจอ 1366×820 ✓ · เลื่อนขวาสุดคอลัมน์ตรึงไม่ทับกัน |
| เต็มจอ | `#iotFsSide` / `#iotWipStrip` / เส้นแบ่งลาก ซ่อนหมด · บอร์ดกินเต็มความกว้าง ✓ |
| ป๊อปอัปตอนเต็มจอ | ใช้ `.iotpop-bg` (ไม่ใช่ modal) ✓ · ปุ่มประวัติเปิดต่อได้ ✓ |
| มือถือ 375px | ไม่มี horizontal scroll ระดับหน้า (`scrollWidth=375`) · ช่องแตะ 49×44px ✓ |
| ค้นหา / กรองสถานะ / เรียง asc-desc | ทำงานครบในมุมมองตาราง ✓ |
| ปุ่มซูมตัวอักษร | คอลัมน์ตรึงขยายตาม (`--ism-vw` 140→154 ที่ 1.1×) ตำแหน่ง sticky ไม่หลุด ✓ |
| ความเร็ววาด | ~133 ms (เท่ามุมมองรายการเดิม) → ต่ำกว่าเกณฑ์ 400 ms ไม่ต้องมีป้ายสถานะ |
| เดือนที่ไม่มีข้อมูล | ขึ้น empty state ปกติ ไม่พัง |
| กระทบยอดซ้ำหลังแก้รอบ 2 (ทั้ง "ทุกวัน" และ "เฉพาะวันที่มีงาน") | 213,409 ตรงกันทั้ง 4 ทาง · badRows 0/140 · badCols 0/31 ✓ |
| ป้าย เห็น+ซ่อน | รวมกันได้ 213,409 ทุกตำแหน่งการเลื่อน ✓ |

## v3.50 — 2026-08-09 — IOT รวมทุก VSM: OAE รายชั่วโมงท้ายการ์ด · ช่วงเวลา 6 ชม. · แผง TOP OAE ต่ำสุด  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `de70f745-1344-4133-ac7c-8f8153a84f5e` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `893940d` (run `31316086969` · conclusion `success`) → https://topmaha.github.io/vsm4-aftermarket-record/

> ยืนยันของจริงแล้วทั้งสองที่: หัวเว็บขึ้น `v3.50` · มี `_iaHourOaeStrip` / `_IAH_PAGE=6` / `iotAllTopOae` / `_iotAllCalcTop` / `iatop-grid` ครบ · ไม่เหลือ `v3.49`
> (ครั้งแรก curl Worker แบบไม่ cache-bust ยังได้ของเก่า — edge cache ยังไม่ propagate · ยิงซ้ำพร้อม `?cb=` 3 ครั้งได้ v3.50 ตรงกันหมด)
> เปิดบนเบราว์เซอร์จริง (Worker URL) โหลด 10,000 record ไม่มี error ใน console · ตารางรายชั่วโมงขึ้น 6 ช่อง `14–15 … 19–20`
> · แผง TOP แถวรวมคละสาย (`VSM1 F-1140 26.9% / VSM1 F-1126 44.1% / VSM3 F4 55.6%`) · badge บนผัง 3 จุด · VSM4 ที่ยังไม่มีข้อมูลขึ้น "— ไม่มีข้อมูล —" ไม่พัง

### ที่มา

หน้า "IOT รวมทุก VSM" ดูได้แค่ว่าตอนนี้ KPI รวมเท่าไร แต่ไม่รู้ว่า **ชั่วโมงไหนตก**
กดเข้าไปดูราย Line ก็เห็นแค่ก้อน KPI ก้อนเดียวของทั้งช่วง
และตารางรายชั่วโมงโชว์ทีละ 5 ช่อง ซึ่งไม่ตรงกับรอบที่หน้างานใช้ดูจริง (6 ชั่วโมง)
อีกอย่างคือ **ไม่มีที่ให้กวาดตาหาตัวปัญหา** — ต้องไล่อ่านทั้งตารางเองว่าเครื่อง/ไลน์ไหน OAE ต่ำสุด

### สิ่งที่ทำ

**1) แถบ "OAE รายชั่วโมง" ต่อท้ายการ์ด (ใต้ KPI)** — `_iaHourOaeStrip()`

- แสดงเฉพาะ **OAE** ตัวเดียว · ค่าแต่ละช่อง = ΣOPR ÷ ΣTarget ของ record ที่คร่อมชั่วโมงนั้น (สูตร/สีเดียวกับตาราง KPI รายชั่วโมง)
- **เรียงตามเวลาแบบวันผลิตจริง** — เริ่มที่ชั่วโมงถัดจาก "ช่องว่างยาวที่สุด" ของวัน จึงได้ `16–17 … 23–00 → 00–01 … 13–14` ต่อเนื่องกัน และแต่ละชั่วโมงขึ้นครั้งเดียว
  (ถ้าเรียงตามเวลาดิบ กะดึกที่บวก 1440 นาที จะทำให้ `03–04` โผล่ซ้ำทั้งหัวและท้ายแถบ)
- ชั่วโมงที่ไม่มีงาน = ไม่ขึ้นช่อง · record ที่อ่านเวลาไม่ได้แยกเป็นช่อง "ไม่ระบุเวลา · N rec" ท้ายแถบ ไม่หายเงียบ ๆ
- ลงครบทุกชั้น: **การ์ด Zone** (ทุก VSM — VSM 1/2/4 ไม่มีชั้น Line การ์ด Zone คือหน่วยเดียวกัน) · **การ์ด Line** (VSM3) · กล่องสรุปในป๊อปอัปรายเครื่อง

**2) ตาราง KPI รายชั่วโมง 5 → 6 ช่วง/หน้า** (`_IAH_PAGE`)

`08–09 … 13–14` · ปุ่ม ‹ › และ LIVE เลื่อนทีละ 6 ช่วงตามเดิม

**3) แผง "TOP OAE ต่ำสุด" เหนือผังรวม** — โครงเดียวกับ TOP ปัญหาของ IOT ราย VSM แต่ 2 ชั้นในแผงเดียว

- **บรรทัดหัวแผง = TOP 1-3 ของทั้งโรง** (ชิปมีป้ายสี VSM บอกว่าเป็นของสายไหน) — คิดจากทุกหน่วย ไม่ใช่แค่ 5 อันดับของแต่ละสาย
- **ราย VSM สายละ 3 อันดับ จัด 2 คอลัมน์** (VSM1|VSM2 / VSM3|VSM4) → 2 แถวแทน 4
  แก้ปัญหาเดิมที่จัดอันดับรวมล้วน ๆ แล้วสายเดียวกินครบ 5 อันดับ สายอื่นหายจากจอ
- หน่วยจัดอันดับ: **VSM3 = รายไลน์** (ชื่อ = ป้ายเหลืองบนผัง) เหมือนหน้าราย VSM · VSM อื่น = รายเครื่อง
- กดชิป → เลื่อนผัง (เลื่อนในกรอบ ไม่ใช่ `scrollIntoView` ที่จะดันทั้งหน้า) + กรอบแดงกะพริบ · ไฮไลต์ทีละตัว
- **badge บนผัง = แถวรวม 1-3 เท่านั้น** — ถ้าแปะราย VSM ด้วยจะเป็น 20 วงกลมบนผัง 316 เครื่อง อ่านไม่ออก
- สาเหตุสั้น ๆ อ่านจาก `byMc` ของหน้ารวม (ห้ามใช้ `_mcCauseShort` ซึ่งอ่าน `_iotData` ของหน้าราย VSM)

### ความสูงแผง TOP (วัดจริงบนเบราว์เซอร์)

| สภาพ | สูง | หมายเหตุ |
|---|---|---|
| 1920 · เส้นแบ่งค่าเริ่มต้น 46% | **91px** | ทุกแถวบรรทัดเดียว · ผังเหลือ 815px |
| 1920 · ลากผังกว้าง 62-64% | 91px | เท่าเดิม |
| 1920 · ลากผังแคบ 34% | 143px | ชิปตัดบรรทัด — เพดาน 144px กันบานกว่านี้ |
| 1600 | 117px | VSM1/VSM2 ตัดบรรทัด |
| มือถือ 375 | 144px (เลื่อนในแผง) | ยุบเหลือคอลัมน์เดียว · หน้าไม่ล้นแนวนอน |

ระหว่างทางเคยได้ 142px (ราย VSM สายละ 5 อันดับ 4 แถว) และ 199px (ให้แต่ละแถวเลื่อนแนวนอน — แถบเลื่อน 5 อันกินความสูงเปล่า ๆ ~50px)
ตัวกริดใช้ `repeat(2,minmax(0,1fr))` ไม่ใช่ `auto-fit` เพราะจอกว้างมาก `auto-fit` จะแตกเป็น 3 คอลัมน์แล้ว VSM4 ตกไปอยู่แถวสองตัวเดียว

### ทดสอบ

ข้อมูลจริง 2026-08-05 (1,390 record · 175/316 เครื่อง) บนเบราว์เซอร์ · 1920×1010 / 1600×900 / 1230×702 / 375×812 · ไม่มี error ใน console
ตรวจแถบรายชั่วโมงทั้ง รวมทุกกะ / กะกลางวัน / กะกลางคืน — ไม่มีชั่วโมงซ้ำ · id ที่ใช้จัดอันดับ VSM3 ตรงกับ `data-lb` บนผังครบ 19 ป้าย

## v3.49 — 2026-08-08 — ผังเครื่องจักรลาก/ซูมได้เหมือนแผนที่ (IOT รวม + IOT แยกแผนก)  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `d5283cb0-9633-4393-9a78-dd987fad74be` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `7a55ec2` (run `31252344837` · conclusion `success`) → https://topmaha.github.io/vsm4-aftermarket-record/

> ยืนยันของจริงแล้วทั้งสองที่: หัวเว็บขึ้น `v3.49` · มี `--iotfs-z` / `vp-pannable` / `_iotFsScale` / `IOT_VP_SEL` ครบ · ไม่เหลือ `v3.48`
> เปิดบนเบราว์เซอร์จริง (Worker URL) ไม่มี error ใน console · ทดสอบเต็มจอ z=1→2 ได้ 552×901 → 1084×1782 · `vp-pannable` ติด · เคอร์เซอร์เป็น `grab`

### ที่มา

ผังเครื่องจักรเลื่อนดูได้ทางเดียวคือแถบ scroll ริมกรอบ — พอซูมเข้าไปดูเบอร์วาล์ว
แล้วอยากไปดูอีกมุมของโรงงานต้องไล่จับแถบ scroll ทีละแกน
ที่ควรจะเป็น: **เอาเมาส์จิ้มช่องว่างระหว่างเครื่องแล้วลากผังไปได้เลย** เหมือนแผนที่ทั่วไป

### สิ่งที่ทำ — ใช้ชุดเดียวกันทั้ง 2 ผัง (`#iotAllVp` = IOT รวม · `#iotViewport` = IOT แยกแผนก)

- **ลากพื้นที่ว่าง = เลื่อนผัง** ทุกทิศ · เคอร์เซอร์เป็นมือ `grab` เฉพาะตอนที่ผังใหญ่กว่ากรอบจริง ๆ
  (ผังพอดีกรอบอยู่แล้ว = ไม่ขึ้นมือ ไม่หลอกให้ลาก)
- **ลากที่กล่องเครื่องก็เลื่อนได้** แต่ต้องเกิน 8px ก่อนถึงนับเป็นลาก แล้วกลืน `click` ที่ตามมาทิ้ง
  → กดดูรายละเอียดเครื่องยังได้เหมือนเดิม แต่ตั้งใจลากแล้วป๊อปอัปจะไม่เด้งใส่
  (พื้นที่ว่างใช้ระยะ 2px — ลากปุ๊บไปปั๊บ)
- **ล้อเมาส์ = ซูมโดยตรึงจุดใต้เคอร์เซอร์ไว้กับที่** · นอกเต็มจอ หรือจอแคบ ≤860px ต้องกด Ctrl ร่วม
  ไม่งั้นจะไปแย่งการเลื่อนหน้าเว็บ
- **นิ้ว/ปากกาไม่แตะต้องเลย** — เช็ก `pointerType` แล้วปล่อยผ่าน ปล่อยให้ระบบเลื่อน/หนีบซูมเองตามเดิม
  (มือถือเป็นหลัก ห้ามชน gesture ของ OS)
- ผูก listener ที่ `document` แบบ delegated — ผังทั้งสองวาดใหม่ด้วย `innerHTML` ตลอด
  ถ้าไปผูกที่ตัว element จะหลุดทุกครั้งที่วาดใหม่

### ผลพลอยได้ — โหมดเต็มจอ (IOT แยกแผนก) ซูมได้แล้ว

เดิม `.iot-fs` ซ่อนปุ่มซูมและบังคับ `transform:none` (ผังยืดพอดีกรอบเสมอ) → ไม่มีอะไรให้ลาก
ตอนนี้ซูมด้วยตัวคูณขนาด SVG `--iotfs-z` (1–4×) แล้วเลื่อนดูส่วนที่ล้นเอา

- ใช้ตัวแปรแยก `_iotFsScale` **ไม่ใช่** `_iotScale` — เพราะ `_iotScale` ถูก `renderFloorPlan`
  ปรับอัตโนมัติให้ป้ายวาล์วอ่านออก (ได้ถึง 2.2) ถ้าเอามาใช้ต่อ พอกดเต็มจอผังจะเด้งซูมเองทันที
- `--iotfs-z` = 1 คือค่าเริ่มต้นและได้ผลลัพธ์เท่าของเดิมทุกพิกเซล · เข้า/ออกเต็มจอรีเซ็ตเป็น 1 เสมอ
- `#iotSvg` ต้องมี `flex:none` ไม่งั้น flex-shrink บีบกลับมาพอดีกรอบ ซูมไม่ขึ้นเลย
- `#iotScaleWrap` เปลี่ยนเป็น `safe center` — จัดกลางแบบเดิมพอซูมเข้าจะทำให้ขอบบน/ซ้ายเลื่อนไปหาไม่ได้

### วัดแล้ว (หน้าทดสอบที่ยก CSS/JS ก้อนจริงมารัน)

| กรณี | ผล |
|---|---|
| ลากพื้นที่ว่าง 180×120px | scroll ขยับตรงตามระยะ · ไม่มี click หลุด |
| ลากทับกล่องเครื่อง 140px | เลื่อนได้ · ป๊อปอัปไม่เด้ง |
| ขยับบนกล่องเครื่อง 2px | ไม่เลื่อน · ป๊อปอัปเปิดปกติ |
| ลากด้วย `pointerType:'touch'` | โค้ดไม่แตะเลย scroll คงเดิม |
| เต็มจอ z=1 → z=2 | SVG 1245×689 → 2459×1347 · มุมบนซ้ายเลื่อนไปถึง 0/0 ได้ · กด ⊙ กลับพอดีกรอบเป๊ะ |
| ล้อเมาส์ซูม 3 จังหวะ | จุดใต้เคอร์เซอร์เพี้ยน x 1.6px / y 12px (y มาจากแถบ scroll แนวนอนโผล่) |
| ลากแล้วสั่งงานด้วยคีย์บอร์ด | `click` detail=0 ไม่โดนกลืน (a11y) |

## v3.48 — 2026-08-08 — เปิดแอปเร็วขึ้น + เลิกอาการยิ่งใช้ยิ่งช้า (โหลดข้อมูลแบบส่วนต่าง)  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `8818fce7-8866-4487-8ae4-509068c67e8d` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `734a2c2` (run `31230728775` · conclusion `success`) → https://topmaha.github.io/vsm4-aftermarket-record/

> เช็กหน้าเว็บจริงแล้วทั้งสองที่ขึ้น `v3.48` + มี `renderWipCustTable` / `_mergeDelta` / `slim=1` / `window.__PF` ครบ
> เปิดจริงบน GitHub Pages: รอบแรกโหลดเต็ม → รอบสองเข้าโหมดส่วนต่าง (ก้อนประวัติมี `since=` ทั้งสองคำขอ) ตารางลูกค้าตรงกับค่าคำนวณ

**Schema D1 ที่เพิ่ม** (migration รันอัตโนมัติ + ปั๊ม `schema_meta.ver = 2026-08-07-incremental`):
คอลัมน์ `updated_at` ทั้ง kpi_records/production_records · trigger 4 ตัว · ตาราง `deleted_records` (tombstone) · index `idx_kpi_saved_at` / `idx_kpi_updated_at` / `idx_prod_updated_at`

### ที่มา — วัดก่อนแก้

เปิดแอป 1 ครั้งยิง API **20 คำขอ กว่าจะนิ่ง 18.4 วินาที** · 11 ใน 20 เป็นคำขอซ้ำ
(URL `/api/kpi?from=วันนี้&to=วันนี้` ยิงถึง 8 ครั้ง) และทุกครั้งที่เปิดต้องลากประวัติ
ทั้งหมดลงมาใหม่ ซึ่งโตจาก 9,400 → 16,000 แถวในเดือนเดียว = ช้าลงเรื่อย ๆ ตามข้อมูลสะสม

### ผลหลังแก้

| | ก่อน | หลัง |
|---|---|---|
| คำขอตอนเปิดแอป | 20 | 13–14 (เปิดซ้ำเหลือ 11) |
| คำขอซ้ำ URL เดียวกัน | 8 ครั้ง | 0 |
| คำขอแรกเริ่มที่ | 1,051 ms | **355 ms** |
| ก้อนประวัติ KPI | 16.26 MB / 1,360 KB gzip | **3.91 MB / 363 KB** |
| D1 อ่านแถว (query วันเดียว) | 15,634 | **1,182** |

### สิ่งที่ทำ

- **ยุบคำขอ GET ที่ซ้ำและวิ่งพร้อมกัน** ให้เหลือรอบเดียว (แจกสำเนาให้ผู้เรียกที่มาทีหลัง
  เพราะ `mergeRecords` แก้ชื่อเครื่องในตัว record — แชร์ก้อนเดียวกันจะแก้ของกันเอง)
- **prefetch ก้อนใหญ่ตั้งแต่หัวไฟล์** — ไม่ต้องรอเบราว์เซอร์แปลง HTML/JS 1.5 MB ให้จบก่อน
- **`?slim=1`** — ก้อนประวัติขอเฉพาะ 12 คอลัมน์ที่คิด WIP จริง (จาก 32)
  ตัวหนักคือ `shifts` (~176 ตัวอักษร/แถว) ซึ่งเส้นทาง WIP ไม่ได้อ่านเลย
- **โหลดแบบส่วนต่าง (`?since=`)** — เก็บก้อนประวัติใน IndexedDB แล้วรอบถัดไปขอเฉพาะที่เปลี่ยน
  - `updated_at` ประทับด้วย **trigger ใน D1** ไม่ใช่โค้ด (จุดเขียน 2 ตารางนี้มี 12 แห่ง ตกหล่นจุดเดียว = cache ค้างถาวร)
  - แถวที่ถูกลบมาจากทะเบียน `deleted_records` (tombstone) — ไม่งั้น cache จะเก็บของที่ลบแล้วไว้ตลอดกาล
  - เปิดแอปครั้งแรกของวัน = โหลดเต็ม 1 รอบ · หลังจากนั้นทั้งวันเป็นส่วนต่าง
  - ผิดเงื่อนไขข้อใดข้อหนึ่ง (ข้ามวัน / worker เก่า / cache พัง / เน็ตหลุด) → กลับไปโหลดเต็มทันที
- **ตัด `COALESCE(shift_date, record_date)` ออกจาก WHERE** — เป็นนิพจน์ ทำให้ index
  `idx_kpi_date` ที่มีอยู่แล้วใช้ไม่ได้ · พอตัดออก index เดิมทำงานทันที
  (เพิ่ม `idx_kpi_saved_at` ที่ยังไม่เคยมี → query "เอา 500 ล่าสุด" อ่าน 500 แถวแทน 15,600)
- **เตือนเมื่อ Cloud ตัดข้อมูลเพราะชนเพดานแถว** — เดิมตัดเงียบ ๆ (บทเรียน `LIMIT 500`)
  เพดาน slim ขยับเป็น 200,000 · เพดานแบบเต็มคงที่ 50,000 (กัน Worker เกินหน่วยความจำ)

### 🐛 แก้บั๊กเดิม — ตารางลูกค้าหน้าติดตามงานค้างที่ยอดไม่ครบ

วัดได้จอโชว์ **143,922 / 34,216 / 10,303 / 4,701** ทั้งที่ของจริง
**842,292 / 173,127 / 47,799 / 39,805** (ต่ำกว่าจริง ~6 เท่า)

ตารางนี้เคยวาดอยู่ใน `_paintHome()` ที่เดียว แต่ข้อมูลย้อนหลังมาถึงทีหลังผ่าน callback
ที่เรียกแค่ `renderWipBuckets()` → การ์ด 3 ถังอัปเดต แต่ตารางค้าง
แก้โดยย้ายไปวาดพร้อมการ์ด 3 ถังที่อ่านข้อมูลก้อนเดียวกัน → แยกกันไม่ได้อีก

> หลัง deploy ตัวเลขในตารางลูกค้าจะกระโดดขึ้นเป็นค่าที่ถูกต้อง — เป็นการแก้ให้ถูก ไม่ใช่ยอดเพี้ยน

### พิสูจน์ว่ายอดเท่าเดิม

เทียบ dump ของค่าที่คำนวณได้ทั้งหมด (WIP รายตัว / Output ของ Process / การ์ด KPI ทุกโซน /
งานหลัง FG / ตัวเลขทุกตัวบนหน้าจอ) ระหว่างเวอร์ชันเก่ากับใหม่บนข้อมูลชุดเดียวกัน = **ต่างกัน 0 รายการ**
ทดสอบครบทั้ง client ใหม่+worker เก่า และ client เก่า+worker ใหม่ (deploy สลับลำดับได้)
รวมถึงวงจรลบจริง: สร้าง → ซิงก์ → ลบผ่าน API → ซิงก์ → หายจาก cache ถูกต้อง


## v3.47 — 2026-08-07 — ยอดผ่าน FG: กดที่ตัวเลขบนการ์ดแล้วรู้เลยว่าเป็นของลูกค้าไหน Valve No อะไร  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `db661728-f161-4c43-9ae6-3006d3aa75c5` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `1aacc11` (run `31133070500` · conclusion `success`) → https://topmaha.github.io/vsm4-aftermarket-record/

> เช็กหน้าเว็บจริงแล้วทั้งสองที่ขึ้น `v3.47` + มี `showFgRange` + ไม่มี `showWipOutGroup` เหลือ
> ⚠️ curl รอบแรกที่ Worker ยังได้ `v3.46` — เป็น **edge cache** ไม่ใช่ deploy ไม่ขึ้น · ต่อ `?cb=<timestamp>` แล้วได้ v3.47 ทันที

แก้ที่ `index.html` อย่างเดียว (โค้ด Worker ไม่แตะ — deploy ใหม่แค่เพื่อ bundle หน้าแอปเวอร์ชันล่าสุด)

### ❌ เอาออก — แถบชิป "✓ ผ่าน FG แยกลูกค้า" ใต้การ์ด WIP OUT (ของ v3.46 ④)

ชิปบอกได้แค่ยอดรวมต่อกลุ่ม และปนสองช่วงเวลาอยู่ในบรรทัดเดียว (`เดือนนี้ · เมื่อวาน`)
กดแล้วได้ป๊อปอัป WIP OUT ที่กรองกลุ่ม แต่ยังเป็นยอด "ทั้งเดือน" เสมอ — ตอบไม่ได้ว่า
**ยอดเมื่อวาน 5,001 ชิ้นนั้น เป็นของใคร เบอร์อะไร**

### ✅ ใส่แทน — กดที่ตัวเลขบนการ์ด WIP OUT ได้ทีละช่อง

- ทั้ง 3 ช่อง `วันนี้ / เมื่อวาน / รวมทั้งเดือน` กดได้แยกกัน (ขีดเส้นประใต้ตัวเลข = สัญญาณว่ากดได้)
- กดแล้วเปิดป๊อปอัป **"✓ ผ่าน FG — <ช่วงที่กด>"** แยกตามกลุ่มลูกค้า กางดูได้ว่ามี
  Valve No / Lot อะไร กี่ชิ้น อยู่ที่ process ไหน ทำที่เครื่องอะไร + ปุ่ม 🕘 ประวัติ รายล็อต
  (หน้าตาชุดเดียวกับป๊อปอัป `Process — งานที่อยู่ขั้นนี้` ในหน้าติดตามงาน)
- ในป๊อปอัปมีปุ่มสลับช่วง `วันนี้ · เมื่อวาน · รวมทั้งเดือน` + ช่องค้นหา Valve No / Lot
  (ค้นแล้วซ่อนกลุ่มที่ไม่เหลือแถว และกางกลุ่มที่เจอให้เอง)
- ช่อง "รวมทั้งเดือน" มีคอลัมน์ **วันที่ผ่าน FG** เพิ่มมา — ล็อตที่ทยอยผ่าน FG หลายวัน
  จะขึ้นเป็น `2026-08-02 – 2026-08-06 (2 วัน)` ไม่ใช่วันเดียวมั่ว ๆ
- ตัดยอดด้วย `fgByDate` รายวันชุดเดียวกับตัวเลขบนการ์ด (`_wipOutRangeRows`) →
  **ยอดรวมในป๊อปอัป = ตัวเลขที่กดเป๊ะเสมอ** ไม่ว่าจะเป็นล็อตที่คร่อมวันหรือคร่อมเดือน
- การ์ดทั้งใบยังกดเปิดป๊อปอัป WIP OUT เดิมได้เหมือนเดิม (กดที่ตัวเลข = `stopPropagation` ไม่ทะลุ)
- การ์ด WIP IN 1.5 ไม่เปลี่ยน (ช่องตัวเลขยังเป็นข้อความเฉย ๆ) · ชิป `📥 รับเข้าเดือนนี้` อยู่ครบเหมือนเดิม

## v3.46 — 2026-08-06 — ค้นหา Process เห็นงานที่รับเข้าแล้ว · สถานะแผนให้ระบบกำหนดเอง · เน็ตหลุดต้องรู้ตัว (ทุกชนิดข้อมูล) · FG แยกลูกค้า · คีย์บอร์ดมือถือไม่เด้ง  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `270e3af0-975e-41b9-9c87-20351c128d7f` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `f83dd04` (โค้ด) · เผยแพร่จริงโดย run ของ `8c1c51c` → https://topmaha.github.io/vsm4-aftermarket-record/

> ⚠️ รอบนี้ Pages รอบแรก (`f83dd04`) **ล้มเหลว** ที่ขั้น `Deploy to GitHub Pages` — Checkout/Upload artifact
> ผ่านหมด แต่ขั้น deploy ค้าง ~10 นาทีแล้ว error (รอบก่อน ๆ ใช้ 19 วิ) = ปัญหาฝั่ง GitHub ไม่ใช่โค้ด/ขนาด repo (3.1 MB)
> แก้โดย **push commit ใหม่เพื่อสั่งรันซ้ำ** → run ของ `8c1c51c` สำเร็จใน 1 นาที · หน้าเว็บขึ้น v3.46 แล้ว
> **บทเรียน: หลัง push ต้องเช็ก conclusion ของ workflow จริง ๆ ไม่ใช่ดูแค่ว่า in_progress**

ทำตามที่แจ้งมา 6 ข้อ · แก้ที่ `index.html` อย่างเดียว (Worker ไม่แตะ) · สำรองไว้ที่ `backups/index.before-6req-20260806.html`

### 🔍 ① ค้นหา Process จาก Valve No — งานที่ "รับเข้า WIP IN 1.5 แล้ว แต่ยังไม่ผลิต" ต้องเห็น

**อาการที่แจ้ง** — เข้ายอด WIP IN 1.5 เรียบร้อย แต่ไปค้นที่หน้า Process กลับขึ้น
"ยังไม่มี Production records" เหมือนข้อมูลหาย (เคสจริง: `VV1908A0`, `VV1070A0`)

**ต้นเหตุ** — หน้านี้อ่านจาก `production_records` อย่างเดียว · รายการรับเข้าอยู่คนละที่
(handover-worker `/wipin`) → ล็อตที่ยังไม่มีใครลงยอดผลิต จึงไม่มีแถวให้แสดงเลย

**แก้เป็น** — เติมแถว Lot จากรายการรับเข้าเข้าไปในตารางเดียวกัน (`_wipinLotsOfValve`)
- ล็อตที่ยังไม่ผลิต → แถวสีอ่อน + ป้าย `📥 891 · รอผลิต` และ **Process แรกเป็นสีเหลือง** (ตรงตามที่อธิบายมา)
- ล็อตที่ผลิตไปแล้ว → แปะยอดรับเข้าไว้ให้เทียบกับยอดที่ลงจริง
- มีแถบสรุปบนตาราง: `📥 รับเข้า WIP IN 1.5 แล้ว N Lot ที่ยังไม่มีใครลงยอดผลิต`
- ค้นหาหลายเบอร์พร้อมกัน (วางจาก Excel) ได้ชิป `📥` เหมือนกัน · รวมสาย VSM3 ⇄ VSM4 ตามเดิม

### 🔗 ② "เริ่มผลิตแล้ว" ใน WIP IN 1.5 แต่หน้าค้นหา Process ว่างเปล่า

**ต้นเหตุ** — สถานะ "เริ่มผลิตแล้ว" ตัดสินจาก `/api/kpi/produced` = คู่ (valve, lot) ใน **`kpi_records`**
ส่วนหน้าค้นหา Process อ่าน **`production_records`** — คนละตาราง ถ้าบันทึกขึ้นแค่ตารางเดียว
(เช่น เน็ตหลุดกลางคัน KPI ขึ้นแล้ว แต่ `/api/records/bulk` ยังไม่ขึ้น) จะเห็นไม่ตรงกันแบบนี้

**แก้เป็น** — แยก "หลักฐาน" ให้เห็น แทนที่จะเงียบ
- แถวที่มีแต่ KPI ไม่มียอดผลิต → ป้าย `⚠️ มี KPI · ไม่มียอดผลิต` ในตาราง WIP IN 1.5 (พร้อมคำอธิบายใน tooltip)
- มีแถบสรุปด้านบนบอกจำนวน/ยอดรวม + สาเหตุที่พบบ่อย
- ตรวจเฉพาะตอนโหลดข้อมูลครบแล้ว (`_wipDataComplete`) — cache ไม่ครบจะฟ้องผิด

### ⌨️ ③ คีย์บอร์ดมือถือเด้งกลับตัวอักษรตอนพิมพ์เลขในตัวกรอง

**ต้นเหตุ** — `renderPlanFilterPop()` เขียน `innerHTML` ใหม่ทั้งก้อน **ทุกตัวอักษรที่พิมพ์**
→ `<input>` ถูกทำลายแล้วสร้างใหม่ · เบราว์เซอร์มือถือถือว่าเป็นช่องใหม่ เลยรีเซ็ตคีย์บอร์ด
กลับชั้นตัวอักษร (ต้องกด `?123` ใหม่ทุกตัว) แม้โค้ดจะ `.focus()` คืนให้ก็ตาม

**แก้เป็น** — แยกส่วนที่เปลี่ยนตามการพิมพ์/ติ๊กไว้ใน `.pcp-dyn` แล้วอัปเดตเฉพาะก้อนนั้น (`_pcpRenderList`)
ช่องค้นหาไม่เคยถูกสร้างใหม่ → โฟกัสไม่หลุด คีย์บอร์ดอยู่ชั้นเดิม
+ ใส่ `autocapitalize=off / autocorrect=off / spellcheck=false` ให้ช่องค้นหาที่เป็นรหัส (แผน · WIP)

### 📊 ④ ยอด FG บนหน้า IOT — แยกกลุ่มลูกค้า (เมื่อวาน / เดือนนี้)
- ใต้การ์ด **WIP OUT** มีชิป 4 กลุ่ม `MSI · SWE · OES&OEM · Other` แต่ละชิปบอก **ยอดเดือนนี้** และ **ยอดเมื่อวาน**
- คิดจาก `fgByDate` รายวันชุดเดียวกับตัวเลขบนการ์ด → ผลรวมทุกกลุ่ม = ยอดบนการ์ดเป๊ะเสมอ
- กดชิป = เปิดรายการ WIP OUT ที่กรองเฉพาะกลุ่มนั้น (ป๊อปอัปมีตัวเลือก "กลุ่มลูกค้า" เพิ่มเข้ามาด้วย)

### 🤖 ⑤ สถานะแผน — ระบบกำหนดเองทั้งหมด ไม่ให้เลือกเอง

**กติกาใหม่** (`planEffStatus`) — ผู้ใช้ไม่ต้องกดอะไรอีก
| สถานะ | เงื่อนไข |
|---|---|
| 📋 วางแผน | ยังไม่เข้า WIP IN 1.5 และยังไม่มียอดผลิต (เพิ่งลงวันที่ขายอย่างเดียว) |
| ⚙️ กำลังผลิต | **รับเข้า WIP IN 1.5 แล้ว** หรือมียอดผลิตบ้างแล้ว (ป้ายย่อยบอกว่าถึง Process ไหน) |
| ✅ ผลิตจบแล้ว | จบงานหลัง FG (DONE) หรือถึง Process สุดท้ายของวาล์ว |

- ถอด dropdown แก้สถานะออกจาก **ทุกที่**: ตารางแผน · คำสั่งกลุ่ม 🏷️ · ปุ่ม 🤖 สถานะอัตโนมัติ · ฟอร์มเพิ่มแผน · ป๊อปอัปแก้แผนใน WIP IN
- **ยกเลิกแผน** ยังเป็นของคนกด (ลูกค้ายกเลิก — คนละเรื่องกับความคืบหน้าการผลิต) ปุ่ม ❌ ท้ายแถวเหมือนเดิม
- ตัวนับ 4 ใบ · ตัวกรอง/เรียงคอลัมน์สถานะ · Export ใช้สถานะจริงหมดแล้ว (Export เพิ่มคอลัมน์ "ถึง Process")
- "แผนที่ยังนับ" (เทียบยอดรับเข้า · เตือนรับเกินแผน) เปลี่ยนจาก `planned/in_progress` เป็น **"ทุกแผนที่ไม่ถูกยกเลิก"**
  — ของเดิมผูกกับค่าที่คนกด ถ้าไม่มีใครกดก็หล่นหายทั้งที่ยอดสั่งยังอยู่

### 📵 ⑥ เน็ตหลุดตอนกดบันทึก — ต้องรู้ตัวทันที

**ต้นเหตุ** — เดิมมีแค่ toast สีส้ม 3 วิ (หน้าจอเด้งไป Dashboard ต่อทันที ยิ่งพลาดง่าย)
และการ **เข้ายอด WIP IN 1.5 ยิงขึ้น Cloud แบบไม่สนผล** (`.catch(()=>{})`) → ขึ้นเขียวว่า
"บันทึกแล้ว" ทั้งที่ของยังไม่ถึงเซิร์ฟเวอร์ เครื่องอื่นไม่เห็นเลย

**แก้เป็น 3 ชั้น**
1. **ป๊อปอัปที่ต้องกดปิดเอง** เมื่อส่งไม่สำเร็จ — บอกว่าข้อมูลอยู่ในเครื่องแล้วไม่หาย · สาเหตุ ·
   ปุ่ม **🔁 ลองส่งอีกครั้ง** · และเตือนชัด ๆ ว่า **ห้ามกรอกแล้วกดบันทึกซ้ำ** (จะกลายเป็นข้อมูล 2 ชุด)
2. **แถบส้มค้างหน้าจอ** `⚠️ ยังไม่ขึ้น Cloud N รายการ` ตราบใดที่ยังส่งไม่สำเร็จ (ปิดแอปเปิดใหม่ก็ยังขึ้น) — กดที่แถบ = ส่งใหม่
   · แถบบอกด้วยว่าค้างเป็น**ข้อมูลชนิดไหน** เช่น `แผนขาย 2 · เป้า (Targets) 1 · ต่อกะ 1`
3. ส่งซ้ำอัตโนมัติเมื่อเน็ตกลับมา (heartbeat + event `online`) แล้วแถบหายเอง
- `submitWipIn` **รอผลจริง** ก่อนบอกผล + `wipinApi`/`hoApi` มี timeout 20 วิ (เดิม fetch ค้างได้ไม่จำกัด)
- toast ตอนบันทึก KPI เปลี่ยนถ้อยคำเป็น "💾 บันทึกลงเครื่องแล้ว" (ผลขึ้น Cloud บอกอีกทีตอนส่งเสร็จ)

#### 📦 ครอบคลุม "ข้อมูลอื่น ๆ" ทุกชนิด — คิวงานเขียนที่ค้าง (`_pendW`)

KPI/ยอดผลิตมี `_unsynced` · WIP IN 1.5 กับต่อกะมีคิวของตัวเอง — แต่ที่เหลือเขียนแบบ
`try{ await api(...) }catch(e){}` คือ **เน็ตหลุดแล้วหายเงียบ** (หน้าจอขึ้นเขียวว่าบันทึกแล้ว
เพราะเก็บ local สำเร็จ แต่เครื่องอื่นไม่มีวันเห็น) เพิ่มคิวกลางเก็บใน localStorage:

| ข้อมูล | จุดที่แก้ |
|---|---|
| แผนขาย | พิมพ์แก้ในตาราง (debounce) · เพิ่ม/ลบแถว · ยกเลิก/กู้คืน · เลื่อนเดือน · คำสั่งกลุ่ม · ซ่อมวันที่ · ย้ายเดือนอัตโนมัติ · วางจาก Excel · เพิ่มจากแผงเตือน |
| เป้า (Targets) | บันทึก Targets · Process ของเครื่อง (mcproc) · migrate PDAC |
| Master Valve | บันทึก/ลบวาล์ว · Import (ก้อนใหญ่เกินคิว → ป๊อปอัปบอกให้ import ใหม่) |
| พนักงาน | เพิ่ม/แก้ VSM/ลบ |
| Valve IDs | บันทึก/ลบ · ลงทะเบียน Lot ทั้งชุด |
| เครื่องจักร | บันทึก/ลบเครื่อง |
| ต่อกะ | จดโน้ต/checklist — รอผลจริงแล้วเตือนถ้าไม่ขึ้น (กะถัดไปจะได้ไม่พลาดโน้ต) |
| แก้ Valve/Lot ทั้งชุด | หมาย `_unsynced` แทนที่จะ toast แล้วจบ |

- ส่งซ้ำได้ปลอดภัย เพราะทุก endpoint ที่เข้าคิวเป็น **upsert/ลบด้วย id** ไม่ใช่ insert ใหม่
- **กันคิวซ้ำ**: คีย์ = ชนิด+id → แก้แผนใบเดิม 10 ครั้งตอน offline เหลือคิวเดียว (ค่าล่าสุด)
- ส่งเรียงตามลำดับที่เข้าคิว (สำคัญเมื่อมีทั้ง "แก้" และ "ลบ" ของชิ้นเดียวกัน) · ติดตัวไหนหยุดรอไว้ ไม่ข้าม
- เพดาน 400 งาน / 60KB ต่อชิ้น — ก้อนใหญ่กว่านั้นไม่เข้าคิวแต่บอกผู้ใช้ให้ทำใหม่ ไม่เงียบ
- ทางที่ "รอ Worker ยืนยันก่อนค่อยแตะหน้าจอ" อยู่แล้ว (แก้ไข/ลบประวัติ · จบงานหลัง FG · แก้แผนจากป๊อปอัป WIP)
  ไม่ต้องเข้าคิว — ล้มเหลวแล้วข้อมูลในเครื่องยังเป็นค่าเดิม + มี toast แดงบอกอยู่แล้ว

## v3.45 — 2026-08-05 — PDAC คิดใหม่ให้ถูก · หน้าวางแผนกรองแบบ Excel + เลือกหลายรายการ + แบ่งหน้า · เก็บภาษาอังกฤษให้ครบ  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `2a33fcbc-a184-494a-b82a-93afb7c1d047` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `5457bf2` → https://topmaha.github.io/vsm4-aftermarket-record/

### 🐛 บั๊กใหญ่ — PDAC "รวม 2 กะ" ได้ค่าสูงกว่าทั้งกะกลางวันและกลางคืน

**อาการที่แจ้ง** (VSM3 · 2026-08-04): กะกลางวัน `12/14 = 85.71%` · กะกลางคืน `8/13 = 61.54%`
แต่พอเลือก **All shifts** กลับได้ `14/15 = 93.33%` — สูงกว่าทั้งสองกะ ซึ่งเป็นไปไม่ได้

**ต้นเหตุ** — `calcZonePdac()` เดิมตัดสินว่า "เครื่องนี้ผ่าน" ด้วย
```js
recs.some(r => (r.pdac_val||0) >= 0.883 || r.pdac_pass === 'PASS')
```
คือ **มี record ไหนผ่านสักตัวก็ถือว่าเครื่องนั้นผ่านทั้งช่วง**
พอดูรวม 2 กะ เครื่องที่ตกกลางวันแต่ผ่านกลางคืนจึงกลายเป็น "ผ่าน" ทันที
→ ตัวเศษ = **ยูเนียน**ของเครื่องที่เคยผ่าน (โตขึ้น) · ตัวหาร = เครื่องเดิม (เกือบเท่าเดิม)
ค่ารวมจึงพุ่งเกินทุกกะ **โดยโครงสร้างของสูตร** ไม่เกี่ยวกับข้อมูล

**แก้เป็น** — 1 หน่วยตัดสิน = **1 โซน × 1 เครื่อง × 1 วันผลิต × 1 กะ**
รวมยอดในหน่วยนั้นก่อน แล้วค่อยตัดสิน `ΣOPR ÷ Σ(Target × 85%) ≥ 88.3%`
- ดูกะเดียว = 1 หน่วยต่อเครื่อง → ตัวเลขคงเดิม (ZONE3: `12/14`, `8/13` เท่าเดิมเป๊ะ)
- ดูรวม = เอาหน่วยของทุกกะมากองรวม → `20/27 = 74.07%` ซึ่ง **อยู่ระหว่าง 2 กะ** ตามที่ควรเป็น
- หลายวัน = เฉลี่ยผลรายวันจริง ไม่ใช่ "เคยผ่านครั้งเดียวก็ผ่านตลอด" แบบเดิม
- ใส่ `zone` ในคีย์ด้วย — รหัสเครื่องซ้ำกันได้ระหว่างโซน ถ้าไม่ใส่ ตอนดู "รวมทุก Zone" จะยุบเป็นเครื่องเดียวกัน
- ป้ายใต้ค่าเปลี่ยนตามช่วงที่ดู: กะเดียว = `20/27 เครื่อง` · ดูคร่อมหลายกะ = `20/27 เครื่อง·กะ`

**⚠️ ผลข้างเคียงที่ต้องรู้ — โซนที่ลงงานหลายช่วงเวลาต่อกะ ตัวเลขจะลดลงชัดเจน**
ตัวอย่างจริง เครื่อง `G-2070` กะกลางวัน 04/08 (6 ช่วงเวลา):

| ช่วง | OPR | Target | PDAC ช่วงนั้น |
|---|---|---|---|
| 08:00–10:00 | 250 | 600 | 49.0% |
| 10:00–12:00 | 400 | 600 | 78.4% |
| 12:00–14:00 | 400 | 400 | **117.6%** ← ช่วงเดียวที่ผ่าน |
| 14:00–16:00 | 300 | 500 | 70.6% |
| 16:00–18:00 | 350 | 450 | 91.5% |
| 18:00–20:00 | 500 | 600 | 98.0% |
| **รวมทั้งกะ** | **2,200** | **3,150** | **82.2%** (ไม่ถึง 88.3%) |

เดิมนับว่า **ผ่าน** เพราะช่วง 12:00–14:00 ทำได้เกิน · ใหม่นับว่า **ไม่ผ่าน** เพราะทั้งกะทำได้ 2,200 จากเกณฑ์ 2,678
โซนที่กระทบมาก (04/08): `ZJ` กลางวัน 100% → 25.93% · `ZH` 100% → 36.84% · `ZI` 65% → 35.00%
ส่วนโซนที่ลงช่วงเดียวต่อกะ (เช่น ZONE2 / ZONE3) ตัวเลขเท่าเดิม
สูตรใหม่สอดคล้องกับ OAE ที่ใช้อยู่: `PDAC ต่อเครื่อง = OAE ต่อเครื่อง ÷ 0.85` → ผ่านเมื่อ OAE ≥ 75.06% (= เป้า OAE พอดี)

- การ์ดรายเครื่อง (`zoneMachineCard`) เดิมใช้ `avg(pdac_val)` = เฉลี่ยอัตราส่วนแบบไม่ถ่วงน้ำหนัก → เปลี่ยนเป็นยอดรวมเช่นกัน + โชว์ % จริงข้าง ✓/✗
- ตอนบันทึก (`_saveKpiImpl`) เปลี่ยนเลขดิบ `0.85` / `0.883` เป็นค่าคงที่ `PDAC_CAP` / `PDAC_PASS` (จุดเดียวจบ)

### 📅 หน้าวางแผน — ตัวกรองแบบ Excel ที่หัวคอลัมน์
- กด **▾** ที่หัวคอลัมน์ → เห็น **ค่าที่มีจริงทั้งหมด** ในคอลัมน์นั้น พร้อมจำนวนแถวของแต่ละค่า · ติ๊กได้หลายค่า
- ครบทุกคอลัมน์: วันที่ขาย · Valve No · Lot · Q'ty · สถานะ · หมายเหตุ
- รายการค่าที่โชว์ = ค่าที่ **เหลืออยู่หลังกรองคอลัมน์อื่น** (เหมือน Excel) ไม่โชว์ค่าที่กรองแล้วไม่มีแถว
- มีช่องค้นหาในลิสต์ + `(เลือกทั้งหมด)` + ปุ่มเรียง ▲▼ ในป๊อปอัปเดียวกัน
- ท่ามาตรฐานของ Excel ใช้ได้: เอา `(เลือกทั้งหมด)` ออกก่อน แล้วค่อยติ๊กเฉพาะตัวที่ต้องการ
- คอลัมน์ที่กรองอยู่ ปุ่ม ▾ เป็นสีน้ำเงินทึบ + มี **ชิปด้านบนตาราง** บอกว่ากรองด้วยอะไร กด ✕ ปลดทีละอัน
- ช่องค้นหาเดิม (Valve / Lot / สถานะ แยกช่อง) → เหลือ **ช่องเดียวค้นทุกคอลัมน์** (Valve / Lot / หมายเหตุ / วันที่)
- ป้ายใต้ตารางบอก `แสดง X จาก Y รายการ` · กรองแล้วไม่เจอ มีปุ่มล้าง Filter ให้ในแถวว่างเลย
- หัวคอลัมน์ยังกดเพื่อเรียงได้เหมือนเดิม (กดที่ **ชื่อ** = เรียง · กดที่ **▾** = ตัวกรอง)

### 🚀 ตามที่ขอ: แบ่งหน้าตารางแผน (แก้อาการโหลดช้า)

> 🩹 **hotfix หลัง deploy รอบแรก**: ค่าเริ่มต้นของ "แสดงหน้าละ" อ่านจาก localStorage ด้วย `+localStorage.getItem(...)`
> ผู้ใช้ใหม่ที่ยังไม่เคยตั้งค่าจะได้ `+null` = **0** ซึ่งบังเอิญเป็นค่าที่ใช้ได้จริง (= ทั้งหมด) → ทุกคนได้ "แสดงทั้งหมด" = ไม่ได้แบ่งหน้าเลย
> แก้เป็นเช็ก `raw === null` ก่อนแปลงเป็นตัวเลข (Worker version `2a33fcbc`) · เจอตอนเปิดเว็บจริงหลัง deploy ไม่ใช่ตอนทดสอบในเครื่อง (localStorage คนละ origin)
แผนสะสมหลายร้อยแถว · 1 แถวมี date input + 3 text input + `<select>` 4 ตัวเลือก + 3 ปุ่ม
วาดทีเดียวหมด = DOM หลายพัน node ต่อการกรอง/ติ๊ก **1 ครั้ง** → หน่วงชัดเจน โดยเฉพาะบนมือถือ

| แสดงหน้าละ | วาดจริง | เวลา render |
|---|---|---|
| ทั้งหมด | 269 แถว | 106 ms |
| 200 | 200 แถว | 81 ms |
| 100 | 100 แถว | 47 ms |
| **50 (ค่าเริ่มต้น)** | **50 แถว** | **30 ms** |
| 25 | 25 แถว | 23 ms |

- แถบใต้ตาราง: **แสดงหน้าละ 25 / 50 / 100 / 200 / ทั้งหมด** (จำค่าที่เลือกไว้ใน localStorage)
  เลือก "ทั้งหมด" ได้ แต่มีป้ายเตือนว่าจะช้าเมื่อข้อมูลเยอะ
- ปุ่มเปลี่ยนหน้า `‹ ก่อนหน้า · 1 2 … 6 · ถัดไป ›` + ป้าย `หน้า 1/6` · เปลี่ยนหน้าแล้วเลื่อนกลับแถวแรกให้
- ป้ายนับบอกครบ: `กรองได้ 27 จาก 269 รายการ · แสดง 1–27`
- **กรอง / เรียง / ค้นหา / เลือกเดือน = เด้งกลับหน้า 1 อัตโนมัติ** (ไม่ค้างอยู่หน้า 7 ของผลลัพธ์ที่เหลือ 3 แถว)
- **การเลือกยังข้ามหน้าได้**: ติ๊กหัวตาราง = เลือกทั้งหน้านี้ · มีปุ่ม **☑️ เลือกทั้งหมด N รายการ (ทุกหน้า)** ในแถบเครื่องมือ
  คำสั่งกลุ่มทำงานกับทุกแถวที่เลือกไว้ในชุดที่กรอง ไม่ใช่เฉพาะหน้าที่เปิดอยู่ (ทดสอบ: เลือกหน้า 1 + หน้า 2 → สั่งงานได้ครบ 100 รายการ)

### 🐛 ตามที่แจ้ง (3): "พอเลือกแล้วเหมือนระบบเลือกทั้งหมด แสดงข้อมูลมาทั้งหมด · กดปิดทั้งหมดก็ไม่มีอะไรเปลี่ยน"

**ต้นเหตุ** — ตรรกะ Excel ดิบ ๆ ที่ว่า "ยังไม่กรอง = ติ๊กครบทุกค่า" ชนกับวิธีที่คนใช้จริง
```
พิมพ์ค้น "2629" → เจอ VV2629M0 ซึ่งติ๊กอยู่แล้ว
  → กดเพราะคิดว่า "กดแล้วได้ตัวนี้"  แต่การกดคือ "เอาติ๊กออก"
  → ระบบเข้าใจว่า "เอาทุกค่า ยกเว้น VV2629M0" = 139 ค่า
  → ตารางโชว์ทุกอย่างยกเว้นตัวที่ต้องการ (ชิปขึ้น "Valve No: 139 ค่า")
```
และกด `(เลือกทั้งหมด)` ออกก็ไม่เปลี่ยน เพราะตอนค้นหาอยู่ มันปลดเฉพาะ 1 ค่าที่ค้นเจอ เหลืออีก 139 ค่าที่ยังติ๊กอยู่

**แก้: แยกกติกาตามเจตนาที่ชัดเจนของผู้ใช้**
- **ไม่ได้พิมพ์ค้นหา** = โหมด Excel เดิม (ติ๊กครบ → เอาตัวที่ไม่เอาออก) · ป้ายกำกับ `ติ๊ก = แสดง · เอาติ๊กออก = ซ่อน`
- **กำลังพิมพ์ค้นหา** = "กำลังตามหาของชิ้นนี้" → รายการเริ่มจาก **ยังไม่ติ๊กอะไรเลย** กดตัวไหน = เอาตัวนั้น
  ป้ายกำกับเปลี่ยนเป็น `ติ๊กตัวที่ต้องการ = เอาเฉพาะตัวนั้น` · หัวลิสต์เป็น `(เลือกผลค้นหาทั้งหมด)`
- เพิ่มปุ่มลัดสีเขียว **✅ เอาเฉพาะที่ค้นเจอ (N ค่า)** — พิมพ์แล้วกดทีเดียวจบ (ท่าที่เร็วที่สุดในการหาชิ้นงาน)
- ชิปบอกให้ครบว่า **เลือกกี่จากกี่ค่า** (`Valve No: เลือก 4 จาก 140 ค่า`) เดิมขึ้นแค่ "139 ค่า" ซึ่งดูไม่ออกว่าเกิดอะไรขึ้น

**ป๊อปอัปบนมือถือ = แผ่นเลื่อนขึ้นจากขอบล่าง**
เดิมลอยใต้หัวคอลัมน์ซึ่งอยู่ค่อนล่างของหน้า → ตกขอบจอ กดปุ่มล่างไม่ได้
(ซ้ำร้าย ตอนวางตำแหน่งยังวัดความสูงตอนที่ยังซ่อนอยู่ = 0 การพลิกขึ้นบนจึงคำนวณผิด)
- จอ ≤700px: เต็มความกว้าง ติดขอบล่าง มุมบนโค้ง เลื่อนในตัวได้ · แถวติ๊กสูงขึ้นให้นิ้วแตะง่าย
- จอใหญ่: วัดขนาดหลังเปิดจริง + วางตำแหน่งใหม่ทุกครั้งที่เนื้อหาเปลี่ยนความสูง

### 🐛 ตามที่แจ้ง (2): "เลือกเป็นรายการไม่ได้ มันแสดงข้อมูลทั้งหมดตลอดเวลา" — ตัวกรองตกหล่นทั้งหมดหลังจอเปลี่ยนขนาด

**ต้นเหตุ** — ตัวจัดการติ๊กทุกตัว (`planFilterPick` / `planFilterAll` / …) อ่านว่า "กำลังกรองคอลัมน์ไหน"
จากตัวแปรกลาง `_pcpCol` ซึ่ง `closePlanFilter()` ล้างเป็น `''` · และมี `window.addEventListener('resize', closePlanFilter)`

บนมือถือจึงพังทุกครั้ง:
```
กด ▾ → โค้ดสั่ง focus ช่องค้นหา → คีย์บอร์ดเด้งขึ้น → viewport เปลี่ยนขนาด
  → resize → closePlanFilter() → _pcpCol = ''
  → ติ๊กรายการ → เขียนลง _planColF['']  → filteredPlans() มองข้าม (ไม่มีใน PLAN_COLS)
  → ติ๊กเท่าไหร่ก็ไม่กรอง เห็นครบทุกแถวตลอด
```
ยืนยันแล้วด้วยการยิง `resize` 1 ครั้งขณะป๊อปอัปเปิด → `_pcpCol` กลายเป็น `""` และติ๊กแล้วได้ 269/269 แถวทันที

**แก้ที่ราก**
- **ส่งชื่อคอลัมน์ไปกับทุกปุ่มในป๊อปอัปโดยตรง** — `planFilterPick('valve', …)` ไม่พึ่ง `_pcpCol` อีก
  พ่วง `_pcpEnsure(col)` ที่ตั้งค่ากลับ + สร้างรายการค่าใหม่ให้เองถ้าชุดที่ค้างอยู่เป็นของคอลัมน์อื่น
- **`_planColF` จะไม่มีวันมีคีย์ที่ไม่มีจริง** (กันอาการ "กรองแล้วเงียบ" ซ้ำในอนาคต)
- **จอเปลี่ยนขนาด/เลื่อน = ย้ายตำแหน่งป๊อปอัปตามหัวคอลัมน์** ไม่ปิดทิ้งอีกแล้ว (ปิดเฉพาะตอนปุ่มต้นทางหายไปจากหน้า)
- **ไม่ focus ช่องค้นหาเองบนจอสัมผัส/จอแคบ** (`_isTouchUI()`) — ตัวการที่ทำให้คีย์บอร์ดเด้ง
- รับ `touchstart` คู่กับ `mousedown` สำหรับ "กดที่อื่นเพื่อปิด" (มือถือบางรุ่นไม่ยิง `mousedown`)

ทดสอบซ้ำบนจอมือถือ 375px ครบทั้ง 6 คอลัมน์ด้วยการกดจริง: ติ๊กแล้วกรองถูกทุกคอลัมน์ · ยิง `resize` ระหว่างเปิดก็ยังกรองได้

### 🐛 ตามที่แจ้ง (1): "กด filter แล้วข้อมูลไม่แสดงตาม filter" — ช่วงวันที่ที่มองไม่เห็นเป็นตัวตัด
ช่อง **วันที่ขาย ตั้งแต่–ถึง** ถูกตั้งเป็น `วันนี้ → +60 วัน` ตั้งแต่ตอนเปิดแอป (`init()`) และไม่มีอะไรบนจอบอก
ทุกอย่างบนหน้านี้ถูกตัดด้วยช่วงนี้ก่อนเสมอ — รวมถึง **รายการค่าในป๊อปอัปกรอง** ด้วย
ของจริงตอนเปิดแอป: ตัดทิ้ง **93 จาก 269 แผน** · เบอร์วาล์วในลิสต์เหลือ **140 จาก 220**
→ กดกรองแล้วหาของที่รู้ว่ามีไม่เจอ ดูเหมือนตัวกรองเสีย ทั้งที่มันทำงานถูกในขอบเขตที่ถูกจำกัดไว้

แก้ให้ "ตัวกรองที่มองไม่เห็น" กลายเป็นสิ่งที่เห็นและปลดได้ในคลิกเดียว:
- **ชิปสีเหลืองใต้ช่องค้นหา** `📅 05/08/2026 – 04/10/2026 ✕` — เห็นตลอดว่ากำลังถูกจำกัดช่วงวันอยู่ กด ✕ = ดูทุกวันที่
- **ในป๊อปอัปกรองทุกคอลัมน์** มีแถบเตือน "เห็นเฉพาะช่วงวันที่ขาย … · ของนอกช่วงยังไม่ถูกเอามาให้เลือก" + ปุ่ม **📅 ดูทุกวันที่**
  กดแล้วลิสต์เติมค่าที่ขาดให้ทันทีโดยไม่ต้องปิด-เปิดใหม่ (140 → 220 ค่า · 176 → 269 แถว)
- **กรองแล้วไม่เจอ** ตารางบอกเลยว่ามีของอยู่นอกช่วงกี่รายการ + ปุ่มกดดูทันที
  เช่น ค้นหา `VV4730A0` → "ไม่พบแผนที่ตรงกับตัวกรอง — 📅 ดูทุกวันที่ (มีอีก 2 รายการนอกช่วงวันที่ขาย)"
- **ล้างทั้งหมด / ↺ ล้าง Filter** คืนช่วงวันที่เป็น 2020–2099 ด้วย (เดิมล้างแต่ตัวกรองคอลัมน์ ช่วงวันยังค้าง)

### ☑️ หน้าวางแผน — เลือกหลายรายการ แล้วสั่งงานพร้อมกัน
- ช่องติ๊กหน้าแถว + ติ๊กบนหัวตาราง = เลือก **ทุกแถวที่กรองอยู่** (ติ๊กบางส่วน = ขีดกลาง)
- **Shift + คลิก** = เลือกเป็นช่วง
- แถบเครื่องมือโผล่เมื่อมีการเลือก (ติดขอบบน) บอกจำนวนรายการ + ยอดรวมชิ้น พร้อมปุ่ม:
  📅⁺ เดือนถัดไป · 📅⁻ เดือนก่อน · 🗓️ ย้ายไปเดือน/วันที่ที่ระบุ · 🏷️ ตั้งสถานะ (รวม 🤖 ตามการผลิตจริง) · ❌ ยกเลิก/↩️ กู้คืน · ⬇️ Export เฉพาะที่เลือก · 🗑️ ลบที่เลือก
- **สั่งงานได้เฉพาะแถวที่เลือก "และมองเห็นอยู่"** — กันสั่งโดนแถวที่ถูกกรองซ่อนไว้
- สิ่งที่เลือกไว้ **ไม่หลุดเมื่อกรอง/เรียงใหม่** (เก็บเป็น `plan_id` ไม่ผูกกับลำดับแถว)
- เลื่อนเดือนคิดวันสิ้นเดือนถูก: 31 ม.ค. +1 เดือน → 28 ก.พ. (ไม่เด้งไป 3 มี.ค.)
- ลบ/บันทึกเป็นชุดยิงทีละ 6 รายการพร้อมป้ายบอกความคืบหน้า (งาน ≤ 3 รายการเงียบสนิท)

### ⚡ ความเร็วหน้าวางแผน
- `_planAutoStatus()` (ไล่ดู kpiRecords/prodRecords ทุกครั้ง) ถูกเรียกซ้ำต่อแถว ต่อการกรอง ต่อการเรียง → ใส่ memo ต่อรอบข้อมูล (`_fvVer`)
- ติ๊กเลือกไม่วาดตารางใหม่ทั้งใบแล้ว — อัปเดตเฉพาะแถว/แถบเครื่องมือ: **70 ms → 1–6 ms ต่อคลิก**

### 🌐 ภาษาอังกฤษ — ช่องว่างหาย + ข้อความที่ยังเป็นไทย
- **ช่องว่างหัว/ท้ายหาย (ต้นเหตุที่ตัวอักษรไปกองติดกัน)**: `_i18nDoText()` เอาคำแปลเขียนทับ text node ทั้งก้อน
  แต่คีย์ในพจนานุกรมเก็บแบบ `trim()` → ช่องว่างที่คั่นกับ `<b>ตัวเลข</b>` หายหมด
  `Total OPR:36,359pcs` ← ที่ถูกคือ `Total OPR: 36,359 pcs` · ไทยอ่านได้เพราะคำติดกันเป็นปกติ แต่อังกฤษพัง
  → คืนช่องว่างเดิมกลับไปทุก node (แก้ทีเดียวหายทั้งแอป)
- **เดินสแกน DOM จริงทุกหน้าในโหมด EN** แล้วเก็บที่ยังเป็นไทย: **143 จุด → 0**
  (เหลือ 2 อย่างที่ไม่ควรแปลอยู่แล้ว = วันที่ในหมายเหตุ กับ Lot ที่พนักงานพิมพ์เป็นไทยเอง)
- เพิ่มพจนานุกรม ~120 คีย์ (หน้าติดตามงาน / ประวัติ / วางแผน / เครื่องมือแก้ข้อมูลในหน้าตั้งค่า) + 15 รูปแบบที่มีตัวแปรกลางประโยค
  (เดิมประโยคพวกนี้โดน `§ ชิ้น` จับทั้งก้อน แปลได้แค่ท้ายประโยค เช่น `รวมทั้งปี 324,915 pcs`)
- **ชื่อเดือนบนหน้าวางแผน** (`ม.ค. 26`) พจนานุกรมจับไม่ได้เพราะรวมกับปีเป็นก้อนเดียว → เลือกภาษาตั้งแต่ตอนวาด (`monName()`) + วาดหน้าวางแผนใหม่เมื่อสลับภาษา

---

## v3.44 — 2026-08-05 — เลิกให้ "แถบกำลังโหลด" ดันหน้าจอเด้งขึ้นลง  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `b21b325c-2fdf-4ef0-acc2-44b951859cd6` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `32a8120` → https://topmaha.github.io/vsm4-aftermarket-record/

### 🐛 อาการ
ระหว่างใช้งาน หน้าจอเลื่อนขึ้น-ลงเองเป็นระยะ กดอะไรพลาดตำแหน่ง
ต้นเหตุ: แถบสถานะ `.ldstrip` วางอยู่ **ในสายเนื้อหา** ที่หัวของหน้า ติดตามงาน / ประวัติ / ตั้งค่า
โผล่ที = การ์ดข้างล่างถูกดันลง · หายที = ดีดกลับขึ้น
งานสั้น ๆ (โหลดเสร็จใน 200–300 ms) ก็ยังดันจอครบรอบ — แวบเดียวแต่รำคาญที่สุด

### ✅ แก้: ย้ายทุกอย่างขึ้นชั้นลอย + เผยเป็นขั้น
ตัวบอกสถานะเป็น `position:fixed` ทั้งหมด → **ไม่กินที่ในสายเนื้อหา = layout ไม่ขยับเลย**
แล้วค่อย ๆ เผยตามความยาวงาน แทนที่จะโชว์ทันทีทุกครั้ง:

| งานใช้เวลา | เห็นอะไร |
|---|---|
| < 400 ms | **ไม่เห็นอะไรเลย** — งานส่วนใหญ่จบตรงนี้ |
| ≥ 400 ms | แถบบางวิ่งที่ขอบบนสุด (`#loadBar`) ไม่บังเนื้อหา |
| ≥ 1200 ms | ป้ายเล็กมุมล่างขวา (`#loadPill`) บอกว่ากำลังทำอะไร + ถึงขั้นไหน |

- ป้ายที่ขึ้นมาแล้วอยู่อย่างน้อย **600 ms** — กันกระพริบตอนงานจบพอดี
- `#loadBar` กับ `#topbar` (ความคืบหน้า API) อยู่ขอบบนเหมือนกัน → โชว์ทีละอัน ไม่ให้ทับกันดูรก
- `#loadPill` ย้ายจากกลางล่าง → **มุมล่างขวา** เล็กลง จางลง บังเนื้อหาน้อยกว่าเดิม

### 🔧 ของเดิมที่เลิกใช้
- `.ldstrip` / `.ldstrip-r` → `display:none!important` (กันโค้ดเก่าไปสั่งโชว์)
- `div#prodLoading` `#histLoading` `#setLoading` ยังอยู่ในหน้าแต่ไม่ถูกเขียนทับแล้ว — สูง 0
- `loadOn(key,msg,{at})` ยังรับ `at` ได้เหมือนเดิม (ผู้เรียกไม่ต้องแก้) แต่ไม่ใช้แล้ว
- `_LOAD_STRIPS` / `_stripShowing()` ถูกถอดออก
- `_loadJobs` เพิ่มฟิลด์ `t0` = เวลาเริ่มจริง · `loadStep()` เลื่อนแค่ `t` (กันหมดอายุ) ไม่แตะ `t0`
  → ขั้นการเผยจึงไม่ถูกรีเซ็ตกลางคัน และงานเดิมที่ถูกสั่งซ้ำไม่เริ่มนับใหม่

---

## v3.43 — 2026-08-04 — หน้าประวัติ ↔ หน้าติดตามงาน พูดตรงกันแล้ว (ผูก KPI ↔ ยอดผลิต เป็นข้อมูลชุดเดียว)  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `ec72431a-5b1f-4866-8483-da17e66ff96f` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `330b23a` → https://topmaha.github.io/vsm4-aftermarket-record/

### 🔀 จับคู่แบบให้คะแนน — เดิมจับสลับคู่กันเอง
`_rcPair` เดิมจับ exact → loose → **ที่เหลือไล่ตามลำดับดื้อ ๆ** ทำให้จับสลับคู่ในชุดเดียวกันได้ (พบจริง):
```
prod L-237 498 VV2629M0/2607S070244  ⟶ ผูกกับ  kpi L-237 175 VV2504A0/2606S060402
prod L-238 175 VV2504A0/2606S060402  ⟶ ผูกกับ  kpi L-238 498 VV2629A0/2607s070244
```
ที่ถูกคือ **ไขว้กัน** (ยอด+lot ตรงกันข้าม) — ถ้าเอาไปดัน valve/lot ต่อ ข้อมูลจะเละทั้งชุด **จับได้ก่อนกดดัน**

ใหม่: ให้คะแนนความเหมือนทุกคู่ที่เป็นไปได้ (เครื่อง 4 · ยอด 3 · valve 2 · lot 2 · process 1)
แล้วเลือกคู่คะแนนสูงสุดก่อน (greedy) · **ต่ำกว่า 5 = ไม่จับคู่** ดีกว่าจับมั่ว · ชุดที่มีอย่างละ 1 ตัวจับคู่ได้เลย
`reconcile` เขียนทับคู่ที่ผูกผิด (`wrong_link`) ให้ตรงตามการจับคู่ใหม่ — **ผูกใหม่ไป 358 แถว**

### 🔄 ดัน Valve/Lot จาก KPI ลงยอดผลิต
ยอดผลิตที่ผูกกับ KPI แล้วแต่เลข valve/lot ไม่ตรง (เกิดจากแก้ที่หน้าประวัติข้างเดียวก่อนมี cascade) — ยึด KPI เป็นหลัก
- ใช้ **คู่ที่เพิ่งคำนวณใหม่** ไม่ใช่ `kpi_record_id` ที่เก็บไว้ (ของเก่าอาจผูกผิด)
- เขียนทับเฉพาะคู่คะแนน **≥ 7** (สัญญาณแรง ≥2 อย่าง) · ต่ำกว่านั้นข้ามไว้ให้คนดูเอง (`fields_skipped`)
- ไม่แตะยอด/process — ยอดต่างเป็นคนละเรื่อง (KPI 1 แถวแตกหลาย process ยอดต้องต่างอยู่แล้ว)
- **ดันไป 54 แถว · ข้าม 12 แถว**

### 👻 แยกแถวผีเป็น 3 ชนิด — ป้องกันลบผิด
เกณฑ์จับคู่ที่เข้มขึ้นทำให้แถวที่จับคู่ไม่ได้กลายเป็น "ผี" ทั้งที่ชุดนั้นยังมี KPI อยู่ → **ห้ามลบ**
| ชนิด | ความหมาย | ลบได้ไหม |
|---|---|---|
| 🔗 `no-batch` | KPI หายไปทั้งชุดบันทึก | ✅ ลบได้ |
| ❓ `unmatched` | ชุดยังมี KPI แต่จับคู่แถวนี้ไม่ได้ | ❌ ต้องให้คนดู |
| ✍️ `standalone` | ลงตรงที่ยอดผลิต ไม่เคยมี KPI | ❌ ห้ามลบ |

`deleteGhosts` ลบเฉพาะ 🔗 เท่านั้น

### ✅ ซ่อม D1 + ลบแถวผีแล้ว (2026-08-04)
สำรองแถวผีไว้ก่อนลบที่ `backups/ghost-rows-before-delete-20260804.json` และ `.csv` (203 แถว · 260,853 ชิ้น)

| รายการ | ก่อน | หลัง |
|---|---|---|
| ยอดผลิตใน D1 | 11,719 | 11,647 |
| ⚠️ ไม่มีคีย์เชื่อมกับ KPI | 11,487 | **0** |
| 🔀 ผูกผิดคู่ | — | **0** (ผูกใหม่ 358) |
| 📅 แสดงคนละวันกับประวัติ | 5,304 | **0** |
| 🛠️ ไม่มีชื่อเครื่อง | 4,502 | **0** |
| ⚠️ valve/lot ขัดแย้งกับ KPI | 422 | **12** (ที่เหลือฟันธงไม่ได้ ต้องดูเอง) |
| 👻 แถวผี | 200 | **19** (ลบได้ 0 · ต้องดูเอง 18 · ไม่เคยมี KPI 1) |

เคสที่ผู้ใช้แจ้ง VV6031A1 / 2606S070074 / FG → หน้าติดตามงาน **1,552** = หน้าประวัติ **1,552** ✓

### ⚡ แก้ต่อ — query ช่วงกว้างตอบ 503
เอา `PROD_DATE_SQL` (มี `datetime()` + `COALESCE`) ไปใส่ `WHERE` → SQLite ต้องคำนวณทุกแถวก่อนกรอง ใช้ index ไม่ได้
→ `/api/records` ช่วงกว้างตอบ **503 (error 1102 = Worker เกินโควตา CPU)**
- เพิ่ม `PROD_DATE_COL` = `shift_date` เปล่า ๆ สำหรับกรองช่วงวัน (ใช้ `idx_prod_shift_date` ได้)
- `backfillShiftDates()` เติม `shift_date` ให้แถวที่ยังว่างครั้งเดียวจบ แทนการคิดใหม่ทุก query
- `reconcileCheck/Fix` ยิงทีละเดือนแล้วรวมผล — ช่วงกว้างทีเดียว Worker ไม่ไหว
- ผลหลังแก้: ทั้งปี 2026 → HTTP 200 · 3.7s (เดิม 503)

### 🕵️ หาต้นเหตุ "แถวผี" — เจอท่าที่ทำข้อมูลหาย 6 จุด
ไล่จากข้อมูลจริง 11,719 prod / 12,063 KPI: **79 ชุดบันทึก KPI หายทั้งชุด** (166 prod แถว) · **24 ชุดหายบางแถว** (61 แถว) ·
165/166 มี `note = auto-linked KPI` ⇒ เคยมี KPI จริงแล้วหายไปทีหลัง ไม่ใช่ข้อมูลที่ลงตรง

**ต้นเหตุ:** โค้ด 6 จุด "แก้ข้อมูล" ด้วยการ **ลบทิ้งจาก D1 ก่อนแล้วค่อย insert กลับ**
ถ้า insert ล้มเหลว (เน็ตสะดุด / batch ใหญ่เกิน) = ข้อมูลหายถาวร แต่ของอีกตารางยังอยู่ = แถวผี
บางจุดครอบ `.catch(() => {})` ไว้ที่ delete จึงไม่มีใครรู้ว่าเกิดอะไรขึ้น

| # | จุด | แตะตาราง |
|---|---|---|
| 1 | `_applyLotRewrite` — แก้ Lot ผิด | KPI + prod |
| 2 | `fixValveOne` — แก้ Valve ทีละตัว | KPI + prod |
| 3 | `fixValveBulk` — แก้ Valve หลายตัว | KPI + prod |
| 4 | `fixProcessBulk` — แก้ Process หลายแบบ | prod |
| 5 | `importProdEdited` — Import ยอดจาก Excel | prod |
| 6 | ปุ่มติ๊ก "ทำเครื่องหมายแก้ไขแล้ว" | KPI |

แก้ทั้ง 6 จุดเป็น **upsert ตรง ๆ** — `/api/kpi` และ `/api/records/bulk` เป็น `ON CONFLICT(record_id) DO UPDATE` มาตั้งแต่ 2026-07-16 แล้ว
คอมเมนต์เก่าที่เขียนว่า *"bulk = insert ล้วน (ไม่ upsert) → ต้อง delete ก่อน"* **ตกยุคไปแล้ว** · ข้อ 6 เปลี่ยนไปใช้ `/api/kpi/update` (แตะแค่ฟิลด์ `shifts`)

⚠️ **ข้อ 4–6 เป็น regression ที่เพิ่งสร้างเองจาก cascade เมื่อวาน** — การลบจะพา KPI/ยอดผลิตอีกฝั่งหายไปด้วย
เช่น Import ยอดจาก Excel จะทำ KPI พร้อม OAE/DLE หายเงียบทั้งชุด · แก้ทันก่อนมีคนใช้

**กันอีกชั้น:** `/api/records/delete` จะลบ KPI ตามก็ต่อเมื่อส่ง `cascade: true` เท่านั้น (เฉพาะหน้าประวัติที่ผู้ใช้กดลบเองและยืนยันแล้ว)
โค้ดอื่นที่เรียกโดยไม่ตั้งใจจะไม่ทำ KPI หาย — ทดสอบแล้วคืน `{deleted:0, kpi_deleted:0}`

**แก้ป้าย 👻 ให้ดู `note` ด้วย** — ยอดที่ลงตรงหน้าบันทึก Production (ไม่ผ่าน KPI) ไม่มีคู่เป็นเรื่องปกติ ห้ามติดป้าย
เดิมเช็กแค่ `kpi_record_id` ว่าง → จะติดธงยกแผงถ้ามีคนใช้หน้านั้น

### 🔁 รอบแก้เพิ่ม — วันผลิตถูกต้องทันทีโดยไม่ต้องรอกดซ่อม
ผู้ใช้แจ้งว่ายอด 580 หน้าประวัติบอก 29/07 แต่หน้าติดตามงานยังบอก 30/07
เหตุ: แถวเก่ายังไม่มี `shift_date` (ยังไม่กดซ่อม) → Worker fallback ไปแปลง `timestamp` เป็นวันไทย ซึ่ง**เดา**และเดาผิดทุกครั้งที่เป็นกะดึก
- `getRecords()` → `_resolveShiftDates()` อ่าน `shift_date` ตัวจริงจาก `kpi_records` ที่ `saved_at` ตรงกับ `timestamp`
  ⇒ หน้าจอถูกทันที · ปุ่มซ่อมเหลือหน้าที่ "เขียนลง D1 ให้ถาวร" อย่างเดียว
- จับคู่ **2 ระดับแยกกันชัดเจน** — exact (เครื่อง+ยอดตรง · consume ทีละตัว) ใส่ทั้งวันที่+`kpi_record_id` ·
  ทั้งชุดวันเดียวกัน ใส่**วันที่อย่างเดียว ไม่ผูก id**
  ❗รอบแรกใช้ fallback หยิบตัวแรกในชุดมาผูก id → prod G-2077/580 (แถวผี) ไปเกาะ KPI ของ G-2076/575 คนละแถว = ป้ายแถวผีหาย + cascade จะลบผิดตัว
- ป้ายเปลี่ยนจาก `⚠️ ไม่ผูก KPI` (ขึ้นทุกแถวที่ยังไม่ซ่อม = รบกวนเปล่า) เป็น `👻 ไม่มีใน KPI` สีแดง ขึ้นเฉพาะแถวที่หา KPI คู่ไม่เจอจริง ๆ
  + ป๊อปอัปสรุปให้ว่า "ยอดที่ตรงกับหน้าประวัติคือเท่าไร"
- **ปิดช่องโหว่ cascade** — ก่อนกดซ่อม คอลัมน์ `kpi_record_id` ใน D1 ยังว่าง Worker จึงหาไม่เจอ
  client ส่ง `prod_ids` ไปด้วยทั้งตอนลบและตอนแก้ · Worker เขียนคีย์เชื่อมให้ถาวรไปในตัว

ผลกับเคสที่แจ้ง (VV6031A1 / 2606S070074 / FG): `30/07 day 397` · `29/07 night 580` ✓ · `29/07 night 575` · `29/07 night 580 👻`
⇒ รวม 2,132 − แถวผี 580 = **1,552 = ยอดหน้าประวัติเป๊ะ**
(ตรวจของที่เสิร์ฟจริงแล้วทั้งคู่: หัวแอปขึ้น **v3.43** · `/api/records` คืน `shift_date`/`shift_type`/`kpi_record_id` · `/api/reconcile` ตอบปกติ)

⚠️ **ยังไม่ได้กดซ่อมข้อมูลเก่า** — schema พร้อมแล้ว แต่ D1 ยังไม่ถูกแตะ
รายงานทั้งปี 2026: ยอดผลิต 11,719 · KPI 12,063 · ยังไม่ผูก 11,487 · หน้าติดตามงานแสดงคนละวันกับประวัติ 5,304 ·
ไม่มีชื่อเครื่อง 4,502 · valve/lot ขัดแย้ง 422 · แถวผี 200 (เคยมี KPI แล้วถูกลบ 199 · ไม่เคยมี KPI 1)

### ⚠️ แก้ความเข้าใจผิดของรายงาน — "คนละวัน" ≠ "ข้อมูลผิด"
ผู้ใช้ทักว่า 5,304 อาจเป็นแค่**กติกากะกลางคืน** ไม่ใช่ข้อมูลเสีย — **ตรวจแล้วถูกต้อง**
กติกาวันทำงาน (`_resolveShift`): `08:00–19:59` กะกลางวัน = วันที่เริ่มงาน · `20:00–23:59` กะกลางคืน = วันที่เริ่มงาน ·
`00:00–07:59` กะกลางคืน = **วันก่อนหน้า** ⇒ กะดึกที่ลงตอนเช้า ต้องมีวันผลิต < วันที่กดบันทึกเสมอ

จำแนกทั้ง 5,304 แถวเทียบกติกา:

| จำนวน | กรณี | สรุป |
|---|---|---|
| 4,676 | กะดึก · บันทึก 00:00–07:59 = หางกะเมื่อวาน | ถูกตามกติกา |
| 376 | กะดึก · บันทึกทีหลัง (ลงสาย) | ถูกตามกติกา |
| 33 | กะกลางวัน · บันทึกหลังเที่ยงคืน (ลงสาย) | ถูกตามกติกา |
| 219 | ลงย้อนหลัง (Admin ปลดล็อกวันที่) | ถูกตามเจตนา |
| **0** | วันผลิตอยู่หลังวันกดบันทึก | **ผิดปกติจริง — ไม่มีเลย** |

**`shift_date` ฝั่ง KPI ถูกต้อง 100%** · ที่ผิดคือหน้าติดตามงานไม่มีวันทำงานเก็บไว้ เลยไปโชว์วันที่กดบันทึกแทน
**ปุ่มซ่อมแค่คัดลอกวันทำงานจาก KPI มาใส่ ไม่เปลี่ยนการจัดกะใด ๆ**
→ เปลี่ยนชื่อตัวนับเป็น `date_mismatch` + แยก `date_by_rule` / `date_odd` · ป้ายในแอปแก้ตาม + เพิ่มกล่องอธิบายกติกากะ

**คำขอผู้ใช้**: ค้น `G-2077` ที่หน้าติดตามงานเจอ 4 รายการ (575 · 580 · 580 · 397) แต่หน้าประวัติกรอง 30/07 เจอแค่ 1
→ "ข้อมูลที่แสดงเป็นข้อมูลเดียวกับฐานข้อมูลไหม · ระบบตรวจสอบความถูกต้องแล้วหรือยัง"

### ผลตรวจ (ดึงข้อมูลจริงจาก D1 มาเทียบทีละแถว — prod 9,813 · KPI 10,059 · 1 ก.ค.–4 ส.ค. 2026)
สองหน้าอ่าน **คนละตาราง** และ **ไม่เคยถูกตรวจสอบว่าตรงกัน**:
หน้าติดตามงาน = `/api/records` (`production_records`) · หน้าประวัติแท็บ KPI = `/api/kpi` (`kpi_records`)
ตอนบันทึกเขียนพร้อมกันทั้งคู่ แต่ผูกกันด้วย **ข้อความใน `note`** เท่านั้น (`auto-linked KPI <zone>/<mc>`) ซึ่งผู้ใช้แก้ทับได้

| # | ต้นเหตุ | ผลกระทบที่วัดได้ |
|---|---|---|
| ① | prod เก็บ `timestamp` = **เวลาที่กดบันทึก** · KPI เก็บ `shift_date` = **วันผลิตจริง** | 48.8% ของคู่ 1:1 วันไม่ตรงกัน (กะดึกลงตอนเช้า = คนละวันทันที) |
| ② | ลบ KPI ที่ประวัติ → prod ไม่ถูกลบตาม | 170 แถวผีที่ยังโชว์ยอดในหน้าติดตามงาน |
| ③ | แก้ valve/lot/ยอด ฝั่งใดฝั่งหนึ่ง อีกฝั่งไม่ตาม | valve ต่าง 0.7% · lot 0.5% · **ยอด 2.3%** · process 1.3% |
| ④ | ชื่อเครื่องอ่านจาก `note` ด้วย regex ที่บังคับรูป `ZONE1/2/3` | โซนจริงมี `ZH ZJ ZI ZK V1Z2 V2Z1` → **72%** โชว์ `—` ทั้งที่ 5,307 แถวมี `machine_id` ถูกอยู่แล้ว |

เคส G-2077 โดนพร้อมกัน 2 ข้อ: 580 (13:08) มีใน KPI แต่ `shift_date` = 29 ก.ค. · 580 (06:58) เป็นแถวผีจากข้อ ②

### แก้อะไร
**Worker** (`vsm4-api-worker/worker.js`)
- `production_records` + คอลัมน์ `shift_date` · `shift_type` · `kpi_record_id` (+ index) — คีย์เชื่อมจริงแทนข้อความใน note
- `getRecords()` กรอง from/to ด้วย **วันผลิตจริง** (`PROD_DATE_SQL`) ให้เกณฑ์เดียวกับ `/api/kpi` · แถวเก่าที่ยังไม่มี `shift_date` แปลงจาก timestamp เป็นเวลาไทย (`+7 hours`) ไม่ใช่ตัดดิบ ๆ
- cascade 2 ทาง: `updateKpiRecord`→`syncProdFromKpi()` · `deleteKpiRecords`→ลบ prod ที่ผูก · `updateRecord`→`syncKpiFromProd()` · `deleteRecord`→ลบ KPI **เฉพาะคู่ 1:1**
  (KPI 1 แถวที่แตกหลาย process จะไม่ถูกลบ/เขียนยอดทับ — ไม่งั้นยอดหายทั้งก้อนหรือนับซ้ำ)
- `GET/POST /api/reconcile` — ตรวจ/ซ่อมย้อนหลัง จับคู่ด้วย `prod.timestamp === kpi.saved_at` (ตอนบันทึก client ใช้ตัวแปร `now` ตัวเดียวกันเขียนทั้งสองตาราง จึงตรงกันระดับมิลลิวินาที) · แถวผี **รายงานอย่างเดียว ไม่ลบเอง**

**หน้าแอป** (`index.html`)
- ตอนบันทึกเขียน `shift_date` / `shift_type` / `kpi_record_id` ลง prod record
- `_spMachineOf()` ใหม่ — อ่าน `machine_id` ก่อน ค่อย fallback ไป note (regex รับโซนทุกรูปแบบ) · `_spProdDate()` ใหม่
- ป๊อปอัปหน้าติดตามงาน + ตารางประวัติแท็บ Production โชว์ **วันผลิต** (เวลาที่กดบันทึกย้ายไปเป็น tooltip) · เรียง/กรอง/Export ใช้วันผลิตทั้งหมด
- แถวที่ยังไม่ผูก KPI ติดป้าย `⚠️ ไม่ผูก KPI` — ผู้ใช้รู้ทันทีว่าตัวเลขนี้อาจไม่มีในหน้าประวัติ
- ลบ/แก้ ทั้งสองฝั่ง sync `state` ในเครื่องตามกัน (ไม่ต้องรอรีเฟรช) + กล่องยืนยันบอกล่วงหน้าว่าจะกระทบอีกฝั่งกี่รายการ
- หน้าตั้งค่า: การ์ด **🔗 ตรวจ/ซ่อมความสอดคล้อง** — เลือกช่วงวัน → 🔍 ตรวจสอบ (ไม่แก้อะไร) → 🔧 ซ่อม

### ทดสอบ
`node --check` ผ่านทั้ง worker.js และทุก script block ใน index.html ·
**dry-run ตรรกะ reconcile กับข้อมูลจริง 9,813 แถว**: ผูกได้ 9,658 (98.4%) · แถวผี 155 (1.6%) · วันที่ถูกแก้ 4,408 แถว
ซึ่ง **4,358 แถวเลื่อนพอดี 1 วัน** — ตรงกับรูปแบบ "กะดึกลงตอนเช้าอีกวัน" ยืนยันว่าจับคู่ถูกตัว

### แถวผีมาจาก 2 ทาง — ห้ามเหมารวมลบ
- 🔗 `note = auto-linked KPI` → เคยมี KPI แล้วถูกลบข้างเดียว = **ผีจริง ลบได้** (198)
- ✍️ `note` ว่าง/อื่น → ลงตรงที่ยอดผลิต ไม่เคยมี KPI = **ข้อมูลถูกต้องของมัน ห้ามลบ** (1 — แถวขยะ valve/lot ว่าง 2 ชิ้น)

`deleteGhosts` ลบเฉพาะกลุ่มแรกเสมอ แม้สั่งลบทั้งหมด · รายงานติดป้าย 🔗/✍️ แยกให้เห็นทีละแถว

### ยังไม่ได้ทำ (รอผู้ใช้ตัดสินใจ)
1. หน้าตั้งค่า → 🔗 ตรวจ/ซ่อมความสอดคล้อง → 🔧 ซ่อม (กดซ้ำได้ ปลอดภัย ไม่ลบอะไร)
2. ลบแถวผี 198 แถว — ต้องสั่งแยกอีกชั้น (`deleteGhosts`)

## v3.42 — 2026-08-04 — ป้าย "⚠ FG ไม่ตรงกับ WIP OUT" กดได้: บอกเลยว่าต่างวันไหน ล็อตไหน ทำยังไงต่อ  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `135a1c34-1bc7-4601-a9c6-3e96b1d40338` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `16373bb` → https://topmaha.github.io/vsm4-aftermarket-record/
(ตรวจแล้วทั้งคู่: ขนาดไฟล์เท่ากัน 1,342,680 bytes · หัวแอปขึ้น **v3.42** · มี `showFgReconcile` ในหน้าที่เสิร์ฟจริง)

**คำขอผู้ใช้**: เห็นป้าย `⚠ FG ไม่ตรงกับ WIP OUT (-1,920)` ที่หน้าติดตามงานแล้วไม่รู้ว่าคืออะไร ต้องทำยังไงต่อ
→ เอาคำอธิบาย + ขั้นตอนแก้ ใส่ลงในแอปเลย ไม่ต้องถามใคร

### เดิม
ป้ายบอกแค่ "ต่างเท่าไร" ที่เหลือผู้ใช้ต้องทำเองหมด: หดช่วงวันที่ทีละวันเพื่อหาว่าวันไหนเพี้ยน →
เปิดตาราง FG เทียบกับป๊อปอัป WIP OUT ทีละล็อต → เดาเองว่าเป็นเพราะข้อมูลยังโหลดไม่ครบหรือข้อมูลผิดจริง

### ใหม่ — แตะที่ป้าย ⚠ แล้วได้ครบในหน้าเดียว
1. **ตัวเลข 2 ฝั่ง + ส่วนต่าง** — FG (ตาราง Output ของ Process) เทียบ WIP OUT (ผ่าน FG) ช่วงเดียวกัน
2. **สถานะการโหลดข้อมูล** (สาเหตุอันดับ 1) — ยังโหลดย้อนหลังไม่ครบ → ขึ้นแถบส้ม "รอ ⏳ หายแล้วดูใหม่ ส่วนใหญ่หายเองตรงนี้"
   · โหลดครบแล้ว → แถบเขียว "ส่วนต่างมาจากใบบันทึก ไม่ใช่การโหลด"
3. **ส่วนต่างอยู่วันไหน** — ตารางรายวัน FG / WIP OUT / ต่าง เฉพาะวันที่ไม่ตรง (แทนการหดช่วงวันที่ทีละวันเอง)
4. **ล็อตไหน** — เจาะ 3 วันที่ต่างมากสุด ชี้เป็นราย (วาล์ว / Lot) ทั้งฝั่งที่ FG มีแต่ WIP OUT ไม่มี และฝั่งตรงข้าม
5. **ใบบันทึกที่ไม่ได้ระบุวันที่/กะ** — ติดกล่องแดงแยกพร้อมยอด (ต้นเหตุที่พบบ่อยสุด: ฝั่ง WIP OUT ตัดยอดตามวัน จึงนับให้ไม่ได้)
6. **ทำยังไงต่อ 4 ขั้น** + ปุ่ม `🔄 โหลดข้อมูลใหม่` และ `📋 คัดลอกสรุป` (ข้อความพร้อมส่งผู้ดูแล — มี fallback สำหรับ in-app browser ที่ไม่มี clipboard API)
7. **บอกด้วยว่าระหว่างนี้ใช้ตัวเลขไหนได้** — ยอด FG ในตารางใช้ได้ (ดึงสดตามช่วงทุกครั้ง) · WIP OUT อย่าเพิ่งเอาไปรายงานจนกว่าจะขึ้น ✓

### แก้ที่ไหน
- `computeProcessOutput()` — เฉพาะแถว FG เก็บ `days` = ยอดแยกรายวัน + รายล็อตเพิ่ม
  (สูตรหาวันใช้ชุดเดียวกับ `computePostFgAll` เป๊ะ ไม่งั้นเทียบกันไม่ได้) · คีย์วัน `''` = ใบบันทึกที่ไม่ระบุวันที่
- `_fgReconcileHtml()` — ป้าย ⚠ เปลี่ยนจาก `<span>` เป็นปุ่ม `.fgchk` (หน้าตาเหมือนเดิม + เส้นใต้จุด) · ป้าย ✓ ไม่เปลี่ยน
- `showFgReconcile()` / `_fgDiagCopy()` — ใหม่ · CSS `.fgchk` `.fgdiag-*`
- **ไม่แตะสูตรยอดใด ๆ** — ตัวเลขทุกตัวเท่าเดิม เพิ่มแค่การอธิบายส่วนต่าง

### ทดสอบ
เปิดไฟล์จริงในเบราว์เซอร์แล้วยิงเคส: วันที่ต่างทั้ง 2 ทิศ (FG มี/OUT ไม่มี และกลับกัน) · ใบบันทึกไม่ระบุวันที่ ·
วันที่ยอดตรง (ต้องไม่โผล่ในตาราง) · สถานะโหลดครบ/ไม่ครบ · ข้อความคัดลอก — ผ่านทั้งหมด · `node --check` ผ่าน

## v3.41 — 2026-08-04 — เลิก "ค้างเงียบ ๆ": ทุกงานที่ใช้เวลา ต้องบอกผู้ใช้ว่ากำลังทำอะไรอยู่  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `b6e30c60-99af-4cf2-ba13-12d075b0b11d` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `e05598a` → https://topmaha.github.io/vsm4-aftermarket-record/
(ตรวจแล้วทั้งคู่: ขนาดไฟล์เท่ากัน 1,328,193 bytes · หัวแอปขึ้น **v3.41** · มี `#loadPill` และ `yieldUI()` ในหน้าที่เสิร์ฟจริง)

**คำขอผู้ใช้**: "บางครั้งระบบโหลดข้อมูลอยู่ แต่ระบบกลับค้างไปสักพัก แล้วพอโหลดเสร็จก็ใช้งานได้ — แบบนี้ไม่ดี
ถ้าโหลดอยู่ก็ขึ้นว่ากำลังโหลดข้อมูล" · เจาะ 3 จุด: **1)** หน้าตั้งค่า (กดเข้า + เปลี่ยน VSM) **2)** หน้าประวัติ **3)** หน้าติดตามงาน

### สาเหตุ
งานหนักถูกทำ "รวดเดียวใน tick เดียว" — main thread ตัน เบราว์เซอร์วาดจอไม่ได้ รับการกดไม่ได้
จอเลยค้างนิ่งโดยไม่มีอะไรบอก (วัดจริงบนเครื่องที่มีข้อมูล 8.7k KPI / 8.8k ยอดผลิต / 11.5k Valve ID):

| จุด | ที่มาของการค้าง |
|---|---|
| หน้าตั้งค่า | `renderSettings()` วาดวาล์ว+ID+พนักงาน+เครื่อง+รายการค้าง+งานหลัง FG ต่อกันรวดเดียว (~300 ms) แล้วรอดึงงานหลัง FG ~10 MB เงียบ ๆ |
| เปลี่ยน VSM | `refreshVsmViews()` วาดใหม่ทุกมุมมอง (ฟอร์ม + แผน + ประวัติ + Dashboard + ตั้งค่า) ต่อกันรวดเดียว |
| หน้าประวัติ | กรอง+เรียงข้อมูลในเครื่อง แล้ว `await` API — ระหว่างนั้นเห็นแค่ตารางเก่าค้างอยู่ |
| หน้าติดตามงาน | `_paintHome()` คิด 3 ถัง WIP (~130 ms) + `ensureAllJobsLoaded()` ดึงย้อนหลังทั้งหมด ~10 MB (หลายสิบวินาที) |

### แก้: ระบบ "ป้ายสถานะ" กลาง + ซอยงานเป็นขั้น
เพิ่มกลไกกลาง `loadOn(key,msg,{at,sub})` / `loadStep()` / `loadOff()` / `withLoading()` และ **`yieldUI()`**
— คืนคิวให้เบราว์เซอร์ 1 เฟรมก่อนลงมือ เพื่อให้ป้ายที่เพิ่งสั่ง "ถูกวาดจริง" ก่อนงานหนักก้อนถัดไป

แสดงผล 2 ชั้น:
- **แถบในหน้า** (`#setLoading` `#histLoading` `#prodLoading`) — บอกตรงจุดที่ข้อมูลยังไม่พร้อม
- **ป้ายลอย** เหนือแถบเมนูล่าง — สำหรับงานที่หน้านั้นไม่ได้เปิดอยู่ / หน้าที่ไม่มีแถบ
  (โชว์เฉพาะงานที่ยังไม่มีแถบให้เห็น → ไม่บอกซ้ำสองที่ · ไม่บังหัวแอปและปุ่ม 🔄 ⚙️)

| จุด | ที่ผู้ใช้เห็นตอนนี้ |
|---|---|
| หน้าตั้งค่า | ซอยเป็น **6 ขั้น** — "⚙️ กำลังเตรียมหน้าตั้งค่า… 3/6 · พนักงาน + เครื่องจักร" (เลื่อน/กดจอได้ระหว่างวาด) |
| เปลี่ยน VSM | "🏭 กำลังสลับไป VSM 3…" → **4 ขั้น** "🔄 กำลังอัปเดตข้อมูลตามสายที่เลือก… 2/4 · แผนการผลิต" · ปุ่ม VSM ไฮไลต์ทันทีที่กด ไม่ต้องรอวาดเสร็จ |
| หน้าประวัติ | "🗂️ กำลังโหลดประวัติ KPI…" → *กำลังจัดข้อมูลในเครื่อง* → *กำลังดึงข้อมูลล่าสุดจาก Cloud…* → *กำลังจัดเรียง N รายการ…* · ดึงไม่สำเร็จ = เตือน + ใช้ข้อมูลในเครื่องต่อ (เดิมเงียบ) |
| หน้าติดตามงาน | "📋 กำลังเตรียมข้อมูลติดตามงาน…" + "⏳ กำลังโหลดข้อมูลย้อนหลังทั้งหมด (ใช้คิดยอด WIP)… — ยอด WIP ที่เห็นตอนนี้ยังไม่ครบ" |
| งานหลัง FG | กล่องขึ้น "กำลังดึงงานหลัง FG ทั้งหมดจาก Cloud…" + skeleton (เดิมเป็นกล่องว่าง) |
| ซิงก์ Cloud / แผนผลิต | `reload()` บอกเป็นขั้น (ข้อมูลหลัก → ยอดผลิต+KPI+แผน → อัปเดตหน้าจอ) · `pullPlans()` มีป้าย |

### กันปัญหาที่ตามมา
- **ป้ายค้าง** — watchdog ปลดงานที่เกิน 3 นาทีทิ้งเอง (ป้ายค้างถาวรแย่กว่าไม่มีป้าย เพราะผู้ใช้จะเลิกเชื่อถือ)
- **กดซ้อน/สลับ VSM รัว ๆ** — ทุกงานมีลำดับของตัวเอง (`_setSeq` `_vsmViewSeq` `_homeSeq` `_histLoadSeq`) รอบเก่าหยุดกลางคัน ไม่วาดซ้อนและไม่แย่งปิดป้ายของรอบใหม่
  (`_histLoadSeq` ต้องแยกจาก `_histReq` เพราะ auto-refresh เบื้องหลังก็บวก `_histReq` ด้วย — ถ้าใช้ตัวเดียวกันป้ายจะค้าง)
- **หน้าจอเด้งตอนรีเฟรชอัตโนมัติ** — รอบเบื้องหลัง (auto-sync 2 ชม. / กลับมาที่แท็บ) และ `ensureAllJobsLoaded` แบบไม่ force
  โชว์ **ป้ายลอยอย่างเดียว ไม่ใส่แถบในหน้า** เพราะแถบจะดันเนื้อหาลงขณะผู้ใช้กำลังอ่าน

**ไม่แตะตรรกะการคำนวณใด ๆ** — ตรวจแล้วยอด WIP IN / WIP / WIP OUT เท่าเดิมทุกตัว (314 · 1,157,596 · 18,113)

## v3.40 — 2026-08-03 — VSM2: เครื่องจักรตรงเอกสารทั้งชุด 71 เครื่อง (เพิ่ม 24 + ย้ายโซน 18) · ยังไม่แตะ IOT  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `9b7ee742-c25e-4965-a2a3-4387a077869a` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `8e52b91` → https://topmaha.github.io/vsm4-aftermarket-record/
(ตรวจแล้วทั้งคู่: ขนาดไฟล์เท่ากัน 1,302,451 bytes · หัวแอปขึ้น **v3.40** · มี `VSM2_ZONE_MOVE` ในหน้าที่เสิร์ฟจริง)

**คำขอผู้ใช้**: ตรวจรายการ Machine No ตามเอกสาร "Machine VSM2" ว่ารายการไหนยังไม่มีใน Zone → เพิ่มให้
**ส่วนของ IOT ยังไม่ต้องเพิ่ม** · (รอบสอง) 18 เครื่องที่อยู่คนละโซนกับเอกสาร → **แก้เลย**

`VSM12_MACHINES` เดิม VSM2 มี 47 เครื่อง · เอกสารมีเครื่องที่ยังใช้งานอยู่ **71 เครื่อง**

| Zone | เดิม | หลังแก้ | เปลี่ยนอะไร |
|---|---|---|---|
| V2Z1 | 4 | **17** | +13 : G-1106 · F-1150 F-1148 F-1149 F-1154 · L-1111 L-1112 L-240 L-1114 L-260 · G-1105 G-1115 G-1102 |
| V2Z2 | 19 | **31** | +6 : F-1143 F-1144 · Q-1101 · L-224 · C-217 C-1105 · รับเข้า 12 จาก V2Z3 · ส่งออก 6 ไป V2Z3 |
| V2Z3 | 24 | **23** | +5 : W-1139 · W-1136 · D-1119 D-1120 · C-204 · รับเข้า 6 จาก V2Z2 · ส่งออก 12 ไป V2Z2 |

รายการทั้ง 3 โซนเรียงตามลำดับกระบวนการในเอกสาร + มีคอมเมนต์ชื่อ Process กำกับทุกกลุ่ม (อ่านเทียบเอกสารได้ตรง ๆ)

- ทุกเครื่องใหม่ใช้ค่าเริ่มต้น `target:2000 · man_std:0.5` (แก้ทีหลังได้ในหน้าตั้งค่า)
- ตัวที่ **ไม่เพิ่ม** เพราะเอกสารระบุ *Non use*: `F-1151` `W-1137` `W-1138` `G-2049` `G-2095`
- `REMOVE EXCESS STELLITE` เอกสารเขียนว่า *Non have / 3x machine* — ยังไม่มีรหัสเครื่อง จึงยังไม่เพิ่ม
- **ไม่แตะผัง IOT (`FP2_MC`) ตามที่สั่ง** — ผังมีเครื่องพวกนี้อยู่แล้วเกือบครบ ยกเว้น `C-208` `C-1105` `Q-1101` ที่ผังยังไม่มี (ผังมี `Q-1108` แทน) ไว้รอบหน้า

### ย้ายโซน 18 เครื่อง — `VSM2_ZONE_MOVE`
| เครื่อง | จาก | ไป | กลุ่มงาน |
|---|---|---|---|
| W-1108 W-1109 W-1114 W-1118 W-1126 W-1135 | V2Z2 | **V2Z3** | Friction Weld P/P · Deposit Stellite seat |
| C-203 C-205 C-206 C-207 C-208 C-209 C-213 C-214 C-215 C-220 | V2Z3 | **V2Z2** | Cut valve |
| L-222 L-230 | V2Z3 | **V2Z2** | Top turn |

ทำ 3 ชั้น เหมือนแนวทาง MC_RENAME ของ VSM3 (2026-08-02):

1. **Machine Master** — `ensureVsmZones()` ขยาย logic "ลบตัวที่ย้ายโซนแล้ว" ของ VSM3 ให้ครอบ VSM1/2 ด้วย
   และเปลี่ยนจาก *ลบทิ้งแล้ว seed ใหม่* → **ยกทั้งก้อนไปโซนใหม่** จึงคง `target/man_std` ที่ผู้ใช้ตั้งไว้
   แผนที่ `id → โซน` แยกต่อ VSM (รหัสซ้ำข้าม VSM ได้) และ **ข้ามเครื่องที่ seed ตั้งใจให้อยู่ 2 โซน** (VSM3 · G-274 = ISUZU 1 + Mitsubishi 3)
   — ของเดิม G-274 โดนลบ-แล้ว-seed-ใหม่ทุกครั้งที่โหลด ทำให้ target ที่ตั้งเองเด้งกลับ 2000 (แก้ไปด้วยในรอบนี้)
2. **Record เดิม (KPI/ยอดผลิต)** — `mcZoneFixRecs()` แปลง `zone` ตอนอ่านทุกครั้ง เรียกต่อท้าย `applyMcRenameAll()`
   (ต้องรัน **หลัง** rename เสมอ เพราะ rename ใช้ zone เดิมเป็นกุญแจ) · idempotent · อัปเดต `zone_label` ให้ด้วย
3. **D1 จริง** — ปุ่ม 🔧 ในหน้าตั้งค่า (โผล่เมื่อดูสาย VSM2, Admin เท่านั้น) → `applyMcZoneMoveCloud()`
   upsert แถวโซนใหม่ + ลบแถวโซนเก่า ผ่าน endpoint เดิม (`/api/machines`, `/api/machines/delete`) — **ไม่ต้อง deploy Worker ใหม่**
   ยอด KPI/ยอดผลิตใน D1 ยังถือโซนเดิม แต่แอปแปลงให้ตอนอ่านอยู่แล้ว (ข้อ 2)

### ตรวจในเบราว์เซอร์
| ตรวจ | ผล |
|---|---|
| จำนวนต่อโซน | 17 / 31 / 23 = **71** ตรงเอกสาร · ไม่มีรหัสซ้ำข้ามโซนใน VSM เดียวกัน |
| จำลอง state เก่า (47 เครื่อง โซนเดิม) แล้วเรียก `ensureVsmZones()` | ย้ายครบ 6 + 12 · ไม่มีตัวตกค้างในโซนเก่า |
| target ที่ตั้งเอง (W-1114=3333 · C-203=4444 · F-1147=1111) | คงอยู่ครบหลังย้าย |
| เครื่องที่ admin เพิ่มเอง (ไม่อยู่ใน seed) | ไม่ถูกแตะ |
| G-274 (2 โซน) | ZI=7777 · ZJ=8888 คงค่าเดิมทั้งคู่ |
| เรียก `ensureVsmZones()` / `applyMcRenameAll()` ซ้ำ | ผลเท่าเดิม (idempotent · รอบสองแก้ 0 แถว) |
| record : `V2Z2/W-1114` · `V2Z3/C-220` · `V2Z3/L-222` · `ZK/G-2018` | → `V2Z3` · `V2Z2` · `V2Z2` · rename เป็น G-2010 ยังทำงานปกติ (ทั้ง `machine_id` และ `machineId`) |
| กล่อง 🔧 หน้าตั้งค่า | VSM2 = กล่องย้ายโซน · VSM3 = กล่องเปลี่ยนชื่อเดิม · VSM4 = ว่าง · console ไม่มี error |

## v3.39 — 2026-08-03 — บอร์ดงานขายเต็มจอ (ตัดต่อกะออก) + เครื่องมือหางาน + เตือน "ไม่มีในแผนขาย"  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `15364191-65b8-43f7-b730-2370dd7adff6` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages commit `6514476` → https://topmaha.github.io/vsm4-aftermarket-record/ (ตรวจแล้วทั้งคู่: หัวแอปขึ้น **v3.39**)

**คำขอผู้ใช้** (3 ข้อ): ① กดเต็มจอ IOT แล้วดูรายการงานขาย → ให้เป็นเหมือนรูปที่ส่งมา "เอาต่อกะออก"
② เพิ่มหัวข้อ sort จะได้หางานง่าย ๆ ③ ระบบวางแผน + WIP IN 1.5: รายการไหนไม่มีในแผน ให้แจ้งด้วย จะได้ตรวจว่าทำไมไม่มี

### ① IOT เต็มจอ + หน้า 2 = บอร์ดงานขายล้วน ๆ เต็มจอจริง
| ของเดิม | ตอนนี้ |
|---|---|
| เต็มจอยังแบ่งครึ่งจอให้แผงต่อกะ + แถบ OAE/WIP ค้างด้านบน → บอร์ด 130+ แถวเห็นจริงไม่ถึงครึ่งจอ | หน้า 2 ตัด **ต่อกะ · เส้นแบ่งลาก · OAE · WIP** ออกหมด · ตารางกินเต็มความกว้าง (`grid-column:1/-1`) |
| ปุ่ม 📝 ต่อกะ ค้างบนหัวทั้งที่ไม่เกี่ยวกับหน้านี้ | ซ่อนเมื่ออยู่หน้า 2 (กลับหน้า 1 ผัง → ทุกอย่างกลับมาเหมือนเดิมครบ) |
| ปุ่มซูม − ⊙ + กดแล้วไม่มีอะไรเกิดขึ้น (ไม่มีผังให้ซูม) | หน้า 2 = ปรับ **ขนาดตัวอักษรทั้งบอร์ด** (0.75–1.8×) จำค่าไว้ข้ามครั้ง (`vsm4_isl_zoom`) — จอติดผนังตั้งใหญ่ / นั่งดูใกล้ตั้งเล็กเห็นหลายแถว |

### ② เครื่องมือ "หางาน" บนบอร์ด (req #2)
- **ช่องค้นหา Part No / Lot** — พิมพ์แล้วกรองทันที + ไฮไลต์ตัวที่ตรงเป็นสีเหลือง · Esc/✕ ล้าง
  (จำโฟกัส+ตำแหน่งเคอร์เซอร์ไว้ก่อนวาดใหม่ ไม่งั้นพิมพ์ตัวที่ 2 ไม่ติด เพราะช่องถูกสร้างใหม่ทุกครั้ง)
- **หัวตารางกดเรียงได้ทุกคอลัมน์ + ลูกศรจาง ↕ บอกว่ากดได้** — ของเดิมโชว์ลูกศรเฉพาะคอลัมน์ที่เรียงอยู่ จึงไม่มีใครรู้ว่าคอลัมน์อื่นกดได้
  เพิ่ม `aria-sort` + `tabindex` + Enter/Space (คีย์บอร์ด/โปรแกรมอ่านหน้าจอ) · เพิ่มคีย์เรียงใหม่ **% ความคืบหน้า** และ **สายที่ผลิต**
- **dropdown "เรียงตาม" + ปุ่มสลับทิศ ▲▼** — จอสัมผัส/จอติดผนังกดหัวตารางยาก และมองปราดเดียวรู้ว่าเรียงตามอะไรอยู่
- **แผ่นตัวเลขสรุปกดได้ = ตัวกรองสถานะ** (เลยกำหนด / กำลังผลิต / ยังไม่เริ่ม / ผลิตเสร็จ) กดซ้ำ = ล้าง
  ตัวเลขบนแผ่นนับจากชุด "ก่อนกรอง" → กด "เลยกำหนด 12" แล้วได้ 12 แถวพอดีเสมอ · มีชิป `แสดง X/Y · ล้างตัวกรอง` กันเข้าใจผิดว่าข้อมูลหาย
- ตัวเรียงตัดสินรองคงที่ (valve → lot) → ลำดับแถวไม่สลับไปมาเองตอนรีเฟรช

### ③ เตือน "รายการนี้ไม่มีในแผนขาย" — 3 จุด (req #3)
บอร์ดติดตามงานขายสร้างจาก `state.plans` ล้วน ๆ → **ของที่รับเข้ามาจริงแต่ไม่มีใครวางแผนไว้ จะไม่โผล่ที่ไหนเลยสักที่**
| จุด | สิ่งที่เห็น |
|---|---|
| บอร์ดงานขาย (IOT หน้า 2) | แถบเหลืองบนสุด "รับเข้าเดือนนี้ แต่ไม่มีในแผนขาย — N รายการ · M ชิ้น" กางดูวันที่/เบอร์/Lot/ยอด/เหตุผล |
| ป๊อปอัป 📥 WIP IN 1.5 | แผงสรุปแยกเหตุผล + ปุ่ม "ดูเฉพาะรายการนี้" · ตัวเลือกสถานะใหม่ **📋 ไม่มีในแผนขาย** · ป้ายเล็กใต้เบอร์ในทุกแถวที่เข้าข่าย |
| หน้า 📅 วางแผน | แผงพับ "รับเข้าแล้ว แต่ไม่มีในแผนขาย" + ปุ่ม **➕ เพิ่มแผน** ต่อแถว (เติม Valve/Lot/จำนวนให้ เหลือใส่วันที่ขาย) |

**เหตุผลที่แยกออกมา 4 แบบ** (ตอบคำถาม "ทำไมถึงไม่มี" ตรง ๆ ไม่ต้องเดา):
`ไม่มีในแผนขาย` (ไม่มีแผนของเบอร์นี้เลย) · `มีเบอร์นี้ แต่คนละ Lot` · `แผนถูกยกเลิกไปแล้ว` · `แผนอยู่ที่สายอื่น`
กฎจับคู่ = กฎเดียวกับตารางเทียบยอดเดิม (`_wipPlanRows`): เบอร์ตรง + แผนที่ไม่ระบุ Lot ถือว่าตรงทุก Lot · การจับคู่ไม่ดูเดือน (รับเข้า ก.ค. → ขาย ส.ค. ยังจับคู่ได้)

**กติกากันเตือนมั่ว** (พบจากการตรวจข้อมูลจริง): เตือนเฉพาะของที่รับเข้าใน "เดือนที่มีการวางแผนจริง" เท่านั้น
ตารางแผนเก็บย้อนหลังไม่กี่เดือน (ตอนตรวจมีแค่ ก.ค.–ส.ค.) ถ้าไม่กัน ของที่รับเข้า พ.ค./มิ.ย. จะโดนติดธงยกเข่งทั้งที่ไม่มีอะไรให้เทียบ
— รายการที่ถูกข้ามจะบอกจำนวนไว้ท้ายแผง ไม่ใช่เงียบหาย

### ตรวจกับข้อมูลจริงในเบราว์เซอร์ (266 รายการรับเข้า · 230 แผน · สาย VSM4)
| ตรวจ | ผล |
|---|---|
| เดือนที่มีแผน | 2026-07 (96) · 2026-08 (134) |
| ส.ค. (เดือนปัจจุบัน) | เตือน **2** รายการ (คนละ Lot 1 · ไม่มีในแผน 1) — ตรงกับที่ตรวจมือ |
| ก.ค. | เตือน **126** (ไม่มีในแผน 120 · คนละ Lot 6) — ของจริง: รับเข้า 211 ล็อต แต่มีแผน 96 |
| มิ.ย. | เตือน **0** · ข้าม 50 (เดือนนั้นไม่มีแผนให้เทียบ) ✔ กติกากันเตือนมั่วทำงาน |
| ค้นหา + เรียง + กรองสถานะ | พิมพ์ "V775" รวดเดียว 4 ตัวอักษรไม่หลุดโฟกัส → เหลือ 1 แถว ไฮไลต์ตรงตัว · กด "1 กำลังผลิต" → เหลือ 1 แถวพอดี · เรียง Order ▲/▼ + `aria-sort` ถูกต้อง |
| เต็มจอ + หน้า 2 | `#iotFsSide` / `#iotSplit` / OAE / WIP / ปุ่มต่อกะ = `display:none` · บอร์ด `grid-column:1/-1` · กลับหน้า 1 ต่อกะกลับมาครบ |

**สำรองไฟล์ก่อนแก้**: `backups/index.before-iotsales-ux-20260803.html`

## v3.38 — 2026-08-02 — รีเฟรชอัตโนมัติ 30 วิ → 2 ชม. + หน้าจอไม่เด้งกลับบนสุด  ✅ deployed

**Deploy แล้วทั้ง 2 ที่** · Worker `vsm4-api` version `808e39c5-6a92-460e-a6d9-e2b209f432ac` → https://vsm4-api.wiphawas-sketchup.workers.dev
· GitHub Pages → https://topmaha.github.io/vsm4-aftermarket-record/ (ตรวจแล้ว: หัวแอปขึ้น v3.38 · มี `AUTO_REFRESH_MS` และ signature guard ครบทั้งสองที่)

**อาการที่ผู้ใช้แจ้ง**: กำลังไล่ดู "งานขายเดือนนี้" (IOT หน้า 2) อยู่ พอเวลาผ่านไปสักพัก ระบบรีเฟรชเอง
ทำให้ตารางเด้งกลับขึ้นบนสุด + เครื่องที่หน้างานช้า · "มีปุ่มรีเฟรชให้กดอยู่แล้ว อัตโนมัติไม่จำเป็นขนาดนั้น"

### ① ยืดรอบรีเฟรชอัตโนมัติ (ค่าคาบเวลารวมไว้ที่เดียว เหนือ `startAutoSync()`)
| ค่า | เดิม | ใหม่ | ครอบคลุม |
|---|---|---|---|
| `AUTO_REFRESH_MS` | 30 วิ | **2 ชม.** | dashboard / ประวัติ / แผน / WIP IN 1.5 (`_wipinPoll` เดิม 15 วิ) |
| `HEARTBEAT_MS` | (ปนอยู่ในรอบ 30 วิ) | **1 นาที** | ping เช็กเน็ต + ดัน record ที่ค้างตอนเน็ตหลุดขึ้น cloud |
| `LIVE_SYNC_MS` | 15–60 วิ | **5 นาที** | บอร์ดรวม 4 VSM (`_iotAllTimer`) + แผงต่อกะ (`_hoPoll`) |

- **แยก heartbeat ออกจากรอบรีเฟรช** — เดิมอยู่ในลูปเดียวกัน ถ้ายืดตามเป็น 2 ชม. งานที่พนักงานเซฟตอนเน็ตสะดุด
  จะค้างในเครื่องนานถึง 2 ชม. กว่าจะขึ้น cloud · heartbeat ไม่วาด DOM ใหม่ = ไม่ใช่ต้นเหตุที่เครื่องช้า
- **ต่อกะ + บอร์ดรวม 4 VSM ไม่ยืดเป็น 2 ชม.** — สองอันนี้คือของที่ต้องเห็นความเคลื่อนไหวของคนอื่น (จอเฝ้าดู / ข้อความข้ามกะ)
- ทุกตัวเพิ่ม `document.hidden` guard — สลับไปแท็บอื่น/ย่อจอ = ไม่ยิง API ทิ้ง
- เลิกใช้ `_prodSetTick` (เดิมทำ produced-set ทุก 4 รอบ) → รอบใหม่ห่างพอที่จะทำครบทุกอย่างในรอบเดียว

### ② `renderIotSales()` — วาดใหม่แบบไม่รบกวนคนที่กำลังนั่งดู
ต่อให้รีเฟรชนาน ๆ ที ถ้าดันมาตรงที่กำลังดูอยู่ก็ยังกวน — ตัวเลื่อนจริงคือ `.isl-sc` ของแต่ละกลุ่ม (+ `#iotSalesPage` ตอนเต็มจอ)
- **ข้อมูลไม่เปลี่ยน → ไม่แตะ DOM เลย** (signature guard เทียบ html ก่อนวาด) ไม่กระพริบ ไม่เด้ง ไม่เปลืองแรงเครื่อง
- **เปลี่ยนจริง → จำตำแหน่งเลื่อนแล้วคืนหลังวาด** ผูกกับ `data-g` ของกลุ่ม (MSISWE / OESOEM / OTHER) ไม่ใช่ลำดับ → สลับกลุ่มก็คืนถูกตัว
- ผู้ใช้กดเปลี่ยนเดือน/กลุ่มลูกค้าเอง → `renderIotSales(true)` = เริ่มดูจากบนสุด (ถูกต้องตามที่ควรเป็น)
- ตรวจแล้วในเบราว์เซอร์: เลื่อนค้างไว้ที่ 700 → รอบข้อมูลไม่เปลี่ยน **700 (DOM node เดิม ไม่ถูกสร้างใหม่)** · รอบข้อมูลเปลี่ยนจริง **700** · กดเปลี่ยนเดือน **0**

### หมายเหตุ
- เวอร์ชันบนหัวแอปค้างที่ v3.36 (v3.37 ลืมบัมป์) → แก้เป็น **v3.38** ทั้งข้อความไทยและคู่แปลอังกฤษ

## v3.37 — 2026-08-02  ✅ deployed

**Deploy แล้ว** `vsm4-api` version `bca72f26-0f09-459b-81f5-666a302e08b8` → https://vsm4-api.wiphawas-sketchup.workers.dev

### ตรวจข้อมูลบนของจริงหลัง deploy
| ตรวจ | ผล |
|---|---|
| พนักงานรายสาย | VSM1 **60** · VSM2 **73** · VSM3 71 · VSM4 55 (เดิมกรอง VSM1-2 เจอ 1 คน) |
| คอลัมน์ `employees.vsm` ใน D1 | มีแล้ว (`id, name, vsm`) — migration ผ่าน |
| เครื่อง M4 / R2 | Machine Master: ZK=`G-2010` · ZI=`G-270` · ZJ ยังเป็น `G-2018`,`G-2070` ตามเดิม |
| ยอด KPI ต่อเครื่อง | `G-2010@ZK` 72 · `G-2018@ZJ` 40 · `G-270@ZI` 63 · `G-2070@ZJ` 65 · ตกค้างผิดโซน **0** |
| แผนขาย ส.ค. 2026 | 134 รายการ · ชิปกรอง MSI 75 + SWE 57 + OES&OEM 2 = **134 ตรงพอดี** |
| ล็อกหัวตาราง | แถบหัว / ชื่อกลุ่ม / หัวคอลัมน์ ค้างครบทั้งโหมดปกติและเต็มจอ |

### ซ่อมชื่อเครื่องใน D1 (รันจริงแล้ว)
`POST /api/machines/rename` แบบระบุโซน — **ผลลัพธ์**
- `G-2018` (M4·ZK) → `G-2010`: KPI **72** · ยอดผลิต **54** แถว
- `G-2070` (R2·ZI) → `G-270`: KPI **63** · ยอดผลิต **46** แถว
- ยอดรวมก่อน/หลังเท่าเดิม (112 และ 128) = ไม่มีแถวหาย/ซ้ำ · รันซ้ำได้ผล 0 (idempotent)
- ⚠️ ถ้าไม่ระบุโซน จะเปลี่ยนผิดตัวไป **105 แถว** (G-2018@ZJ 40 + G-2070@ZJ 65) — ดู [[machine-id-rename-needs-zone]]

### 🔴 เหตุการณ์ระหว่างตรวจ: ทดสอบ endpoint บน production แล้วข้อมูลเสีย (กู้คืนครบแล้ว)
- **เกิดอะไร**: ทดสอบว่าด่านกัน rename ทำงานไหม โดยยิง `/api/machines/rename` **ไม่ใส่ zone** กับค่าปลอม `to:"XXX-TEST"` บนฐานข้อมูลจริง
- **ทำไมด่านไม่จับ**: ด่านรุ่นแรกปฏิเสธเฉพาะตอนรหัส "ยังอยู่มากกว่า 1 โซน" — แต่ตอนนั้นเปลี่ยนชื่อฝั่ง ZK ไปแล้ว `G-2018` เหลือโซนเดียว ด่านจึงปล่อยผ่าน
- **ความเสียหาย**: `G-2018@ZJ` 40 KPI + 17 ยอดผลิต · `G-2070@ZJ` 65 KPI + 42 ยอดผลิต ถูกเปลี่ยนเป็น `XXX-TEST` (รวม 164 แถว) + แถว machines ของ 2 เครื่องถูกยุบเหลือแถวเดียว
- **กู้คืน**: ใช้ `state.kpiRecords`/`state.prodRecords` ที่ค้างในแท็บเบราว์เซอร์ (โหลดมาก่อนเกิดเหตุ) ดึง `record_id` ครบทั้ง 164 แถว แล้ว `update` กลับทีละแถว — สำเร็จ 164/164 ล้มเหลว 0
- **ยืนยันหลังกู้**: `G-2010@ZK` 72/54 · `G-2018@ZJ` 40/17 · `G-270@ZI` 63/46 · `G-2070@ZJ` 65/42 (KPI/ยอดผลิต) ตรงกับก่อนเกิดเหตุทุกตัว · **record_id ซ้ำ 0 แถว** จาก 10,861 KPI + 10,498 ยอดผลิต · `XXX-TEST` เหลือ 0 · machines ZJ ได้ `G-2018`/`G-2070` กลับพร้อม `man_std` เดิม (0.214285 / 0.33333)

### ปิดช่องโหว่ `/api/machines/rename` ให้เด็ดขาด
- **`zone` = required เสมอ** (เดิม optional → ไม่ใส่ = กวาดทุกโซน) · บทเรียน: ด่านที่ขึ้นกับ "สภาพข้อมูลตอนนั้น" พังทันทีที่ข้อมูลเปลี่ยน จึงเปลี่ยนเป็นข้อบังคับไม่มีเงื่อนไข
- ตรวจเพิ่ม: ถ้าโซนที่ส่งมาไม่มีเครื่องนั้นจริง → ปฏิเสธพร้อมบอกโซนที่พบ
- เพิ่ม **`{preview:true}`** — ดูจำนวนแถวที่จะกระทบ (machines/kpi/prod) โดยไม่แก้อะไรเลย ใช้ตรวจก่อนลงมือได้ทุกครั้ง
- ทดสอบซ้ำเคสเดิม: ไม่ใส่ zone → ถูกปฏิเสธ · โซนผิด → ถูกปฏิเสธ · preview → คืนตัวเลขถูก ข้อมูลไม่ขยับ

### แก้เพิ่มจากการตรวจข้อมูลรอบสุดท้าย
- **ชิปกรอง "อื่น ๆ"** — เดิม MSI+SWE+OES&OEM ไม่เท่า "ทั้งหมด" ถ้ามีวาล์วที่ไม่มีใน Master (ตกกลุ่ม Other) แล้วไม่มีปุ่มให้กดดู · ตอนนี้ชิปโผล่เฉพาะเมื่อมีจริง + เป็นสัญญาณเตือนว่าเบอร์นั้นไม่มีใน Master Valve
- **ข้อความตอนไม่มีข้อมูล** — เดิมกรอง SWE แล้วไม่เจอ ขึ้นว่า "ไม่มีรายการขายในเดือนนี้" ซึ่งผิด (เดือนนั้นมี 6 รายการ แค่ไม่มี SWE) · แยกเป็น 2 ข้อความ: ไม่มีทั้งเดือน vs ไม่มีเฉพาะกลุ่มที่กรอง

## v3.37 รายละเอียดการแก้ — 2026-08-02

### ① พนักงาน VSM1 / VSM2 ไม่แสดง + คนที่เพิ่มใหม่ตกไป VSM4
- **ต้นเหตุที่ 1 (รายชื่อไม่แสดง) = เวอร์ชันที่ deploy อยู่เก่า** — ตัวเลือกกรองบนเครื่องที่ใช้งานยังเป็น `value="vsm12"` ("VSM 1-2") ซึ่งไม่ใช่คีย์ที่ระบบรู้จัก (`VSM_KEYS = vsm1..vsm4`) → กรองแล้วไม่เจอใครเลย ยกเว้นคนที่ถูกเพิ่มด้วยตัวเลือกนั้น (จึงเห็น "รวม 1 คน")
  - โค้ดใน repo แก้ไปแล้ว (`normVsmKey` + ตัวเลือกคีย์จริง + migrate ค่า `vsm12` เก่า) แต่ **ยังไม่ได้ deploy** — Worker เสิร์ฟ `vsm4-api-worker/index.html` ซึ่งเป็นสำเนาเก่า
  - **ทำแล้ว**: copy `index.html` ล่าสุดเข้า `vsm4-api-worker/` — เหลือแค่ `npx wrangler deploy`
  - ตรวจแล้วในหน้าเว็บ: กรอง VSM1 = **60 คน** · VSM2 = **73 คน** · VSM3 = 71 · VSM4 = 54
- **ต้นเหตุที่ 2 (คนใหม่ตกไป VSM4) = D1 ไม่มีคอลัมน์ `vsm`** — VSM ที่ admin ตั้งถูกเก็บใน localStorage เท่านั้น → เครื่องอื่นเห็นเป็น VSM4
  - Worker: `ALTER TABLE employees ADD COLUMN vsm TEXT` + `getEmployees`/`upsertEmployee` อ่าน-เขียนคอลัมน์นี้ (มี fallback ถ้า ALTER ยังไม่ผ่าน · ไม่ส่ง vsm มา = ไม่ล้างค่าเดิม)
  - แอป: `_empVsmCloud` เป็นชั้นใหม่ในลำดับ `VSM_EMP_MAP (โค้ด) > override เครื่องนี้ > ค่าจาก D1 > vsm4` ใช้ทั้ง `resolveEmpVsm` / `vsmOf` / `_hoVsm` / `_wipinVsm`
  - ตรวจแล้ว: เพิ่ม `ZZ-001` เลือก VSM2 → resolve = `vsm2` ทั้งใน state และแผนที่ที่ส่งขึ้น D1

### ② ค้นหา Process จาก Valve No — มองข้ามสาย VSM3 ⇄ VSM4 (แก้ WIP ค้าง)
- งานจริงวิ่งข้ามสาย (เช่น `VF1349 Lot 12345` ลง IND/COT ที่ VSM4 แล้วไปต่อ SSG/TOP&HOD ที่ VSM3) แต่ทุกอย่างกรองแยกสายเด็ดขาด → ① ตารางค้นหาเห็น process ไม่ครบ ② งานเดียวถูกนับเป็น WIP **ทั้งสองสาย** และไม่มีวันหลุด WIP เพราะ FG ไปตกอีกสาย
- เพิ่มแนวคิด **"สายพี่น้อง"**: `vsmPeers()` / `filterVsmPeer()` — vsm3 ↔ vsm4 เท่านั้น · **VSM1 / VSM2 พฤติกรรมเดิมทุกอย่าง**
- **ค้นหา Process** (ทั้งโหมดเดี่ยวและวางหลาย Valve) ใช้ `filterVsmPeer` + ช่องที่ผลิตอีกสายติดป้าย `VSM3`/`VSM4` + หมายเหตุบนหัวตารางว่ารวม 2 สาย (หน้าอื่นยังแยกสายตามเดิม)
- **WIP**: ตัดงานที่ถึง FG "ที่สายไหนก็ได้" (`reachedFgAny`) และล็อกเจ้าของงานไว้ที่ **สายที่ทำ process ไกลสุด** สายเดียว → ไม่ค้าง ไม่นับซ้ำ
- **ยอด WIP OUT ยังยึดสายตัวเองเหมือนเดิม** — กฎ "Output ของ Process ที่ FG = WIP OUT" ไม่ถูกแตะ
- **MSI / SWE = ยอดจบของ VSM4 เสมอ** ไม่ว่าจะผลิต/จบที่สายไหน (`_jobVsmOwner`)
- ตรวจแล้ว: FG ที่ VSM3 → VSM3 มี WIP OUT 570 · VSM4 ไม่ค้าง WIP ล็อตนั้นอีก · งานที่ยังไม่ถึง FG โผล่เป็น WIP สายเดียว (สายที่ทำ IND)

### ③ หน้าวางแผน — UX
- **Lot / Q'ty แสดงไม่หมด**: กำหนดความกว้างตามเนื้อหาจริง (Lot 136px · Q'ty 82px) + ซ่อน spinner ของ `input[type=number]` · วัดแล้วพอดีทั้ง desktop และมือถือ (lot 11 ตัวอักษร = 78px ในช่องว่าง 91px)
- **หมายเหตุกว้างเกิน**: เดิม `min-width:120px` ทำให้ดูดพื้นที่ที่เหลือทั้งแถว → เปลี่ยนเป็นความกว้างคงที่ 150px
- **การ์ดยอดขายรายเดือน**: เดิม fix 6 คอลัมน์ → `auto-fit minmax(84px,1fr)` + `max-width:1120px` = 12 เดือนแถวเดียวบนจอใหญ่ (การ์ด 88px) ย่อเป็น 3 คอลัมน์เองบนมือถือ
- **เรียงตารางได้**: กดหัวคอลัมน์ วันที่ / Valve / Lot / Q'ty / สถานะ (สถานะเรียงตามของจริงจากการผลิต)

### ④ เปลี่ยนชื่อเครื่อง VSM3
- `G-2018` **เฉพาะสาย M4 (โซน ZK)** → `G-2010` · `G-2070` **เฉพาะสาย R2 (โซน ZI)** → `G-270`
  - ⚠️ รหัสเดิมซ้ำอยู่ 2 ไลน์คนละโซน — **G-2018 ของ N1 (ZJ) และ G-2070 ของ I1 (ZJ) คงเดิม** จึงต้องระบุโซนคู่กันเสมอ
- แก้ครบทุกที่: `VSM3_LINES` · Machine Master (`SEED_VSM_MACHINES` + ของที่มีอยู่ในเครื่อง คง target/man_std เดิม) · **ผัง IOT VSM3 (`FP3_MC`)** · **ผัง IOT รวมทุก VSM (`FPA_MC`)** · ค่า process ของเครื่อง (`mcproc`)
- **ข้อมูลเดิม**: `mcRenameId()/mcRenameRecs()` normalize ตอนอ่านทุกทาง (เปิดแอป · reload · `mergeRecords` · ชุดเต็ม `_allKpi/_allProd`) → ทุกจอเห็นชื่อใหม่ทันที
- **ฐานข้อมูลจริง**: Worker endpoint ใหม่ `POST /api/machines/rename {from,to,zone}` แก้ `machines` + `kpi_records` + `production_records` (idempotent) · ปุ่ม 🔧 ในหน้าตั้งค่า → เครื่องจักร (Admin + สาย VSM3)

### ⑤ อัปโหลด IDs — เตือนของซ้ำ + ใช้ข้อมูลใหม่แทนของเก่า
- ตรวจ 3 แบบ: **ซ้ำในไฟล์เดียวกัน** (ใช้บรรทัดล่าสุด — เดิมทิ้งบรรทัดใหม่) · **ID เดิมมีอยู่แล้วแต่ Valve/Lot ต่าง** (แสดงของเดิม→ของใหม่) · **Valve+Lot ซ้ำคนละ ID**
- เตือนแบบ inline ในกล่องเดิม แล้วต้องกด **"✅ ยืนยัน — ใช้ข้อมูลใหม่แทนของเก่า"** อีกครั้ง (ไม่ทับเงียบ ๆ)
- ตารางรายการ IDs กดหัวคอลัมน์เรียงได้ (ID / Valve / Lot)

### ⑥ + ⑧ หน้า IOT "หน้า 2" — รายการติดตามงานขายของเดือนนั้น
- ปุ่มสลับ **🏭 ผัง ⇄ 📋 งานขายเดือนนี้** ในหัวการ์ด IOT (ใช้ได้ทั้งโหมดปกติและเต็มจอ — เต็มจอใช้พื้นที่เดียวกับผัง)
- ข้อมูลมาจาก **การวางแผนขาย** ของ VSM ที่กำลังดู · เลือกเดือนได้ ◀ ▶ · แยกกลุ่มลูกค้าตามฟอร์มที่ส่งมา **[MSI and SWE] | [OES&OEM]**
- กรอง ทั้งหมด / MSI / SWE / OES&OEM — **กลุ่มที่ไม่มีข้อมูลจะไม่ถูกวาด และเหลือกลุ่มเดียวตารางขยายเต็มความกว้าง** (ไม่เว้นช่องว่างทิ้งไว้)
- คอลัมน์ตามเอกสาร: `No · Part No · Lot No. · Order · Due date · Q'ty · Status · Remark` — กดหัวคอลัมน์เรียงได้ทุกช่อง
  - `Order` = ยอดตามแผนขาย · `Q'ty` = ยอดที่ผลิตได้จริงถึงตอนนี้ · `Status` = process ล่าสุดจากการบันทึกผลิต · `Remark` = สายที่ผลิตจริง (งานข้ามสายโชว์ทั้ง 2 สาย)

#### ดีไซน์: "บอร์ดติดตามงานหน้าไลน์" (shop-floor board)
ออกแบบจากการใช้งานจริง 2 แบบ — ขึ้นจอใหญ่ติดผนัง (IOT เต็มจอ) ต้องอ่านออกจากระยะไกล · เปิดบนมือถือหัวหน้าไลน์ ต้องกวาดตาหาแถวที่มีปัญหาได้เร็ว
- **แถบสถานะสีข้างแถว (ภาษา Andon)** — เทา ยังไม่เริ่ม · ส้ม กำลังผลิต · เขียว เสร็จ · **แดง เลยกำหนด** (แถวเลยกำหนดพื้นหลังแดงอ่อนทั้งแถว) เห็นแถวที่ต้องรีบโดยไม่ต้องอ่านตัวหนังสือ
- **แถบความคืบหน้าในช่อง Q'ty** — คำถามจริงคือ "ทำได้กี่ % ของยอดสั่ง" ไม่ใช่ตัวเลข 2 ตัวให้คิดเอง · แถบเปลี่ยนสีตามสถานะแถว · หัวบอร์ดแต่ละกลุ่มมีแถบรวมของกลุ่ม
- **นับวันถึงกำหนดส่ง** — "อีก 3 วัน" / "พรุ่งนี้" / "เลยมา 2 วัน" ใต้วันที่ (สีส้มเมื่อเหลือ ≤3 วัน · แดงเมื่อเลย) แทนการโชว์แค่วันที่
- **แผ่นตัวเลขสรุป** Order / Q'ty+% / กำลังผลิต / ผลิตเสร็จ / เลยกำหนด — ไทล์ที่ไม่มีค่าจะไม่แสดง ไม่รกจอ
- **ตัวกรองลูกค้าเป็น segmented control** พร้อมตัวนับในปุ่ม · หัวบอร์ดเป็นแถบไล่สีน้ำเงินเข้ม
- Part No / Lot เป็น monospace (ภาษาเดียวกับรหัสเครื่องในแอป) · ตัวเลขทั้งหมด tabular · แถบสถานะ "กำลังผลิต" มีจุดเต้นเบา ๆ · แถวไล่ปรากฏตอนวาดใหม่ (เคารพ `prefers-reduced-motion`)
- มือถือ: บอร์ดเรียงคอลัมน์เดียว · ไทล์สรุปแบ่งเท่ากันเต็มแถว · ตารางกว้างเลื่อนในกล่องตัวเอง (หน้าเว็บไม่เลื่อนแนวนอน)

#### รอบแก้ตามที่ผู้ใช้แจ้ง (หลังลองใช้จริง 134 แถว)
- **① หัวตารางไม่ล็อก** — ต้นเหตุ: โหมดเต็มจอตั้ง `.isl-sc{max-height:none}` กล่องนี้เลยไม่ได้เป็นตัวเลื่อนเอง
  ตัวที่เลื่อนจริงคือ `#iotSalesPage` แต่ `position:sticky` ของ `th` ผูกกับ `.isl-sc` → หัวตารางเลื่อนหายไปพร้อมทั้งบอร์ด
  - แก้เป็น flex column: `#iotSalesPage` → `overflow:hidden` · `.isl-wrap`/`.isl-grp` → `flex:1;min-height:0` · `.isl-sc` → กินความสูงที่เหลือแล้วเลื่อนในตัวเอง
  - ผล: **ล็อกครบ 3 ชั้น** — แถบหัว (เดือน/ตัวกรอง/ตัวเลขสรุป) · ชื่อกลุ่มลูกค้า · แถวหัวคอลัมน์ · เลื่อนเฉพาะตัวข้อมูล + เงาใต้หัวบอกว่ามีเนื้อหาต่อ
  - **มือถือก็แก้ด้วย** — เดิม `max-height:none` เหมือนกัน (หัวตารางไม่ค้างจริง) เปลี่ยนเป็น `64vh`
- **② ตัวอักษรใหญ่เกินไป** — ของจริงมี 130+ แถว ความหนาแน่นสำคัญกว่าการอ่านไกล ๆ
  - ลดตัวคูณจอผนัง: เต็มจอ 1.12→**1.0** · เต็มจอ ≥1600px 1.24→**1.06** (โหมดปกติคง 1.0 ไม่ให้เล็กเกินบนมือถือ)
  - ลดขนาดฐานทุกตัว ~6-10% + บีบระยะห่างแถว `7px`→`4px`
  - ผลที่เต็มจอ ≥1600px: Part No 15.5→**12.5px** · ตัวเลขสรุป 19.8→**15.4px** · สถานะ 11.1→**10.4px** · ความสูงแถว 41→**35px** (เห็นแถวต่อจอเพิ่ม ~17%)
- ตรวจแล้วที่ 375 / 1440 / 1600px ทั้งโหมดปกติและเต็มจอ: หัวตารางค้างจริงตอนเลื่อน · ไม่มี overflow แนวนอน · 4 VSM × 6 หน้า ไม่มี error

### ⑦ ตรวจงานที่ถูกย้ายไปขายเดือนถัดไป
- เจอเมื่อ **Valve No + Lot + Q'ty เดียวกัน** อยู่ทั้งเดือนเก่าและเดือนใหม่ (เช่น 30/07 กับ 30/08) → แถวเดือนเก่าคือของค้าง
- แผงเตือนบนหน้าวางแผน + ปุ่มเดียวจบ **"🔀 ย้ายงานทั้งหมดไปเดือนถัดไป"**: ลบแถวเดือนเก่า · ยกหมายเหตุมาให้แถวใหม่ถ้ายังว่าง · **ตั้งสถานะให้อัตโนมัติจากการผลิตจริง**
- `_planAutoStatus` เดิมดูแค่ KPI → ถ้ามีแต่ยอดผลิตจะขึ้น "ยังไม่เริ่ม" ทั้งที่กำลังผลิตอยู่ · ตอนนี้ดู `prodRecords` (รวมสายพี่น้อง) ด้วย และถึง process สุดท้ายของวาล์ว = ผลิตเสร็จ
- ปุ่ม **🤖 สถานะอัตโนมัติ** ตั้งสถานะให้ทุกแถวที่กรองอยู่
- ตรวจแล้ว: แถว 07/2026 ถูกลบ · แถว 08/2026 เหลือแถวเดียวพร้อมหมายเหตุเดิม · แผงเตือนหายไปเอง

## v3.36 — 2026-07-30

### แก้ข้อมูลจริงบน Cloud: "VV6446A0 vv7161mo" → VV6446A0 (ครบ 3 ที่)
- **หลักฐานก่อนแก้** (ไม่ได้เดา): lot `2603S040236` ถูกใช้โดย **VV6446A0 เท่านั้น 15 records** · VV7161M0 ใช้ lot `2602s020016` คนละตัว · รายการนี้เป็นงานเดียว lot เดียว เครื่องเดียว (G-1109) qty เดียว → `vv7161mo` คือข้อความหลงมา ไม่ใช่งานที่ 2
  - เครื่อง G-1109 วันนั้นเดินทั้ง VV6446A0 และ VV7161M0 จริง (คนละ record) → อธิบายได้ว่าทำไมพิมพ์ปนกัน
- **แก้แล้ว 3 ที่**: KPI `K-mriwa96a-12-818i` (รวม valve ใน shifts JSON) · prod `R-mqti11gc-25thul` · WIP IN `wi_mrobmxwr9rbm1`
- **ตรวจหลังแก้**: ทั้ง 3 ที่เป็น `VV6446A0` · KPI วันนั้นเครื่องนั้นยังมี 3 record เท่าเดิม (opr/oae ไม่เปลี่ยน) · WIP IN ยังมี 254 รายการเท่าเดิม (ไม่เกิดของซ้ำ) · **เบอร์ผิดรูปใน WIP IN เหลือ 0**
- สำรองค่าเดิมไว้ก่อนเขียนทุกครั้ง

### กันข้อมูลผิดรูป "ไหลต่อ" เข้า WIP IN 1.5 อัตโนมัติ
- **ที่มาของแถวเสีย**: note ของรายการนั้นระบุว่ามาจากปุ่ม **"ลงบันทึก WIP IN ทั้งหมด"** ในแผง "ลืมลงบันทึก WIP IN 1.5" → KPI ต้นทางลง valve ผิด 1 ที่ **กลายเป็นผิด 2 ที่**
- **แก้**: `_wipinMissingSplit()` แยกรายการที่ Valve/Lot ผิดรูปออกจากชุดที่ลงอัตโนมัติได้
  - แผงขึ้นกล่องแดง **"⛔ ลงอัตโนมัติไม่ได้ n รายการ — Valve/Lot ต้นทางผิดรูป"** พร้อมบอกเหตุผลรายแถว + ปุ่ม ✏️ แก้ต้นทาง
  - ปุ่ม "ลงบันทึกทั้งหมด" นับเฉพาะรายการที่ลงได้ และแจ้งจำนวนที่ข้ามในกล่องยืนยัน
- **ผลตรวจ**: ป้อน 4 รายการ (2 ดี · 2 ผิดรูป) → ลงอัตโนมัติได้ 2 · บล็อก 2 พร้อมเหตุผลถูกต้องทั้งคู่

### ยังต้องรอข้อมูลจากหน้างาน (แก้เองไม่ได้ — เดาแล้วข้อมูลเสีย)
- **VV2629A0 lot `"1"`** (1,000 ชิ้น · 2026-07-29 · G-1108 · S-057) — มีทั้งใน WIP IN และ prod 3 แถว (SSG/TOP&HOD/CB) · lot จริงคืออะไรระบุไม่ได้จากข้อมูล
- **อีก 7 คู่ที่พิมพ์ 2 เบอร์ + 2 lot รวมในแถวเดียว** (prod 8 · KPI 7 แถว) — ต้องรู้ว่าแบ่งจำนวนกันเท่าไรถึงจะแยกได้:
  `VV4763A0 VV4674A0` 708 · `VV2249A0 VV7252M0` 1,049 · `VV2629MO V78416M1` 1,180 · `VV7324M2 V78416M1` 620 · `VV1159AO VV2629MO` 840 · `VV4674A0 2629` 1,520 · `VV6214A0 2629` 1,500 · `V77805 / V78302` 3,820

## v3.35 — 2026-07-30

### 🔴 ต้นเหตุใหญ่: ชิป SWE / OES&OEM / MSI ขึ้น 0 — เพราะ Master Valve **ถูกลบทิ้ง** ตอนพื้นที่เต็ม
- **อาการที่แจ้ง**: WIP IN 1.5 เดือนนี้ ชิป **SWE = 0** ทั้งที่ต้องมีข้อมูล
- **ตรวจข้อมูลจริงบน Cloud แล้ว — ข้อมูลครบ 100% ไม่ได้หาย**:
  | กลุ่ม | รายการ | ชิ้น (2026-07) |
  |---|---|---|
  | MSI | 75 | 66,561 |
  | **SWE** | **33** | **51,576** |
  | OES&OEM | 94 | 219,575 |
  | Other | 0 | 0 |
  Master Valve มี 1,213 เบอร์ · เป็นกลุ่ม SWE **364 เบอร์** · customer ดิบสะอาด (MSI 737 · SWE 364 · OES&OEM 98 · OEM 9 · OES 1 · Other 4) → **ไม่มีปัญหาที่ข้อมูล**
- **ต้นเหตุจริงอยู่ที่ตัวจัดการ localStorage เต็ม** (`makeSafe.setItem`) — บรรทัดสุดท้ายเดิมคือ `ls.removeItem(k)` ใส่ไว้ "กันค่าครึ่ง ๆ ค้าง" แต่ `setItem` ของ localStorage เป็น **atomic** อยู่แล้ว (เขียนไม่สำเร็จ = ค่าเดิมอยู่ครบ ไม่เคยมีค่าครึ่ง ๆ) → บรรทัดนี้จึง **ลบ cache ทิ้งฟรี ๆ**
- **ลูกโซ่ที่เกิดขึ้นจริง** (โยงกับข้อ ④ + ⑤ ของ v3.34 พอดี):
  1. เขียน cache รวมกันเกินโควตา (วัดจริง: prod_records **3.3 MB** + valve_ids 557 KB + valves 181 KB + targets 77 KB > โควตา ~5 MB)
  2. `vsm4_valves` เขียนไม่ลง → **ถูกลบทิ้ง**
  3. เปิดแอปครั้งหน้า `state.valves` ตกไปใช้ `DEFAULT_VALVES` = **3 เบอร์ตัวอย่าง**
  4. Sync ก็ล้มเหลว (badge Local) → ไม่มีอะไรมาเติมกลับ
  5. วาล์ว 1,213 เบอร์กลายเป็น "ไม่รู้จักลูกค้า" → ตกกลุ่ม **Other ทั้งหมด** → **MSI / SWE / OES&OEM ขึ้น 0**
- **ทำซ้ำอาการได้เป๊ะในหน้าเว็บ**: ตั้ง `state.valves = DEFAULT_VALVES` (3 เบอร์) → ชิปออกมา `MSI 0 · SWE 0 · OES&OEM 0 · Other 337,712` — **ตรงกับที่เจอหน้างาน**
- **แก้ 5 จุด**:
  1. **พื้นที่เต็มแล้วห้ามลบคีย์นั้นทิ้ง** — คงค่าเดิมไว้ (ทดสอบแล้ว: เขียนไม่ลง → ค่าเดิมยังอยู่ครบ)
  2. **ทิ้ง cache ทีละก้อนแล้วลองใหม่** แทนล้างทั้ง 4 ก้อนรวดเดียว · เรียงจากก้อนใหญ่สุดก่อน (`prod_records` 3.3 MB มาก่อน)
  3. **`/api/init` ที่คืน array ว่างจะไม่ทับข้อมูลเดิม** — เดิม `res.valves || state.valves` ยอมรับ `[]` ด้วย (array ว่างเป็น truthy) → Worker สะดุดครั้งเดียว Master หายทั้งก้อน
  4. **heartbeat ดึง Master กลับมาให้เอง** เมื่อเน็ตกลับมาแล้วพบว่า Master ยังไม่ครบ — ไม่ต้องรอผู้ใช้กดปุ่ม
  5. **เตือนแทนที่จะโชว์เลขผิด** — เพิ่ม `masterValveLoaded()` เป็นด่านกลาง · ถ้า Master ยังไม่ครบ ใต้การ์ด WIP IN ขึ้นแถบแดง "Master Valve ยังไม่โหลด (n เบอร์) — ยอดแยกกลุ่มลูกค้าจะตกไปอยู่ Other ทั้งหมด" + ปุ่ม 🔄 โหลดใหม่
- **กันธงเตือนรัวด้วย**: ธง "เบอร์ไม่มีใน Master Valve" (v3.34 ข้อ ①) จะ **ไม่ทำงานเลย** ถ้า Master ยังไม่ครบ — ไม่งั้นจะติดธงทั้ง 254 แถวโดยไม่มีความหมาย
- **ผลตรวจหลังแก้**: Master ครบ → `MSI 66,561 · SWE 51,576 · OES&OEM 219,575 · Other 0` **ตรงกับข้อมูลบน Cloud เป๊ะ** · Master ไม่ครบ → ขึ้นแถบเตือน + ไม่มีธงเตือนปลอม

## v3.34 — 2026-07-30

### ① WIP IN 1.5 มีรายการที่ตามไปหาในหน้าบันทึกข้อมูลไม่เจอ (เคส VV2629A0)
- **ไล่จากข้อมูลจริงบน Cloud** (`/api/init` 1,213 วาล์ว · `/wipin/list` 254 รายการ · `/api/records` ทั้งหมด) แทนการเดา — สรุปผล:
  - VV2629A0 **ยังอยู่ใน Master Valve** และ **มียอดผลิตจริง** (37 prod records) → ไม่ใช่เบอร์ที่ถูกลบ
  - ยอด "รอผลิต" บนการ์ด **2,447 ชิ้น ถูกต้องแล้ว** (มาจาก 2 รายการ: VV2102A0 314 + VV4729A0 2,133) — VV2629A0 **ไม่ได้อยู่ในนั้น**
  - **ตัวปัญหาจริงคือแถวนี้**: `VV2629A0 · lot "1" · 1,000 ชิ้น · 2026-07-29 · by S-057` — **lot เป็นเลข "1"** (พิมพ์ไม่ครบ) จึงตามไปหาในหน้าบันทึกข้อมูล/ค้นหา Process ไม่เจอ แต่ยอด 1,000 ชิ้นถูกนับรวมในสรุป "รับเข้าเดือนนี้ MSI" ไปแล้ว (เป็น 1 ใน 4 ครั้งของ 6,609 ชิ้นที่กดดู)
  - **สแกนเจออีก 1 รายการแบบเดียวกัน**: `VV6446A0 VV7161MO · lot 2603s040236 · 2,002 ชิ้น` — **พิมพ์ 2 เบอร์ติดกันในช่องเดียว** (และ `VV7161MO` ใช้ตัว O แทนเลข 0)
- **สาเหตุเชิงระบบ**: ฟอร์ม WIP IN 1.5 **รับทุกอย่างที่พิมพ์เข้ามาโดยไม่ตรวจอะไรเลย** ต่างจากถังอื่น (WIP ข้าม `!isKnownValve()` ทิ้ง · WIP OUT ติดธง `unknown`) → ข้อมูลผิดรูปเข้าไปแล้วไม่มีใครเห็น
- **แก้ (ขยายผลทุกรายการ ไม่เจาะจง VV2629A0)** — เพิ่ม `_wipinIssues()` ตรวจทุกแถว 3 ข้อ:
  1. เบอร์ไม่มีใน Master Valve · 2. เบอร์มีช่องว่าง (พิมพ์ 2 เบอร์ติดกัน) · 3. Lot สั้นผิดปกติ (<8 ตัว)
  - **ตรวจเกณฑ์กับ lot จริงทั้งระบบก่อนใช้**: 250/254 รายการยาว 11 ตัว · 10-12 ตัวอีก 3 · **เหลือ "1" ตัวเดียวที่หลุดเกณฑ์ → false positive = 0**
  - แถวที่ผิดขึ้น **พื้นแดง + ⚠ + บอกเหตุผลใต้เบอร์** ทั้งในตาราง WIP IN 1.5 และป๊อปอัป "รับเข้าเดือนนี้"
  - แถบ 🔎 ตรวจสอบ เพิ่มบรรทัดสรุป + ปุ่ม **"ดูเฉพาะรายการนี้"** (กรองข้ามเดือน/ลูกค้า/สถานะให้เลย)
  - **ปุ่ม ✏️/🗑 โผล่ให้แม้รายการจะ "เริ่มผลิตแล้ว"** — เดิมซ่อนไว้ ทำให้แถว VV2629A0 lot "1" แก้ไม่ได้เลย
  - **ยอดยังนับให้เหมือนเดิม** ไม่ตัดทิ้งเงียบ ๆ (ยึดกฎเดิมของระบบ: ตัวเลขต้อง reconcile ได้)
- **ปิดช่องที่ทำให้เข้าใจผิดด้วย**: ป๊อปอัป "รับเข้าเดือนนี้" เดิม**ไม่บอกเลยว่าผลิตไปหรือยัง** → เบอร์ที่ผลิตจบแล้วดูเหมือนยังค้างอยู่ใน WIP IN 1.5 · เพิ่มคอลัมน์ **"สถานะผลิต"** (เริ่มผลิตแล้ว / รอผลิต n ชิ้น) ทุกแถว

### ② รายชื่อพนักงาน VSM 1-2 ไม่ขึ้น + คนใหม่ตกไป VSM4 เสมอ
- **สาเหตุเดียวกันทั้งสองอาการ**: dropdown ในหน้าตั้งค่าใช้ `value="vsm12"` ซึ่ง**ไม่ใช่คีย์ที่ระบบรู้จัก** (`VSM_KEYS = vsm1..vsm4`)
  - กรอง "VSM 1-2" → เทียบ `e.vsm === 'vsm12'` กับข้อมูลจริงที่เป็น `vsm1`/`vsm2` → **ไม่เจอใครเลย**
  - เพิ่มคนใหม่ด้วยตัวเลือกนั้น → เก็บ override เป็น `vsm12` → `resolveEmpVsm()` แปลไม่ออก → **ตกไป vsm4**
  - แถวที่ค้างค่า `vsm12` ไว้ dropdown ในตารางไม่มี option ตรง → เบราว์เซอร์โชว์ตัวแรก ("VSM 1") ทั้งที่ค่าจริงไม่ใช่
- **แก้**: dropdown ทั้งสองช่องเป็น VSM 1 / 2 / 3 / 4 แยกกันจริง · เพิ่ม `normVsmKey()` เป็นด่านเดียวของทั้งแอป (ใช้ใน `resolveEmpVsm` `vsmOf` `_hoVsm` `_wipinVsm` `saveEmpVsmOv` `setEmployeeVsm`) · migrate ค่าเก่า `vsm12 → vsm1` อัตโนมัติตอนโหลด
- **ค่าเริ่มต้นของช่อง "เพิ่มพนักงาน" = สายที่กำลังดู** (เดิม fix ไว้ที่ VSM4) และเปลี่ยนตามเมื่อสลับสาย
- **เตือนเมื่อรหัสถูกล็อกไว้ในโค้ด**: ถ้ารหัสนั้นอยู่ใน `VSM_EMP_MAP` แล้ว ค่าที่เลือกไม่มีผล — เดิมเงียบ ตอนนี้ toast สีส้มบอกชัด
- **เพิ่มตัวนับหัวรายสาย** ข้างช่องกรอง — **ผลตรวจ: VSM1 60 · VSM2 73 · VSM3 71 · VSM4 53 · รวม 257 คน** (เดิมกรอง "VSM 1-2" ได้ 1 คน)

### ③ "จบงาน" ในกล่องรับงานเข้า Process ถัดไป (หลัง FG) — ใส่เกณฑ์ตามหน้างานแล้ว
- **กฎใหม่**: จะจบได้ **หลัง FG ต้องไม่เหลือ process อื่นนอกจาก `P/T` · `T/T` · `QC` · `POL` · `BUFF`**
- งานที่ไม่เข้าเกณฑ์ **ไม่หายไป** — ย้ายไปกล่องพับ **"⛔ ยังไม่เข้าข่ายจบ"** พร้อมบอกว่า**ติดที่ process ไหน** เพื่อไปแก้ลำดับที่ Master Valve
- ปิดทุกทางที่จะจบผิด: ปุ่ม "→ จบงาน" รายตัวเช็กซ้ำก่อนบันทึก · **"✅ จบงานทั้งหมด" ข้ามรายการที่ยังจบไม่ได้** และบอกจำนวนที่ข้ามในกล่องยืนยัน
- เทียบชื่อ process แบบไม่สนเครื่องหมาย (`P/T` = `PT`, `T/T` = `TT`) · วาล์วที่ยังไม่มี Process Master ถือว่าเข้าข่าย (ไม่งั้นงานหายโดยไม่มีเหตุผล) · VSM1/2 ใช้ Process Master ของสายตัวเอง
- **ผลตรวจ 9/9 ตรงตามที่สั่ง**: `SFG FG QC` ✓ · `SFG FG P/T POL QC` ✓ · `SFG FG COT IND T/T QC` ✗ (ติด COT, IND) · `FG SFG QC` ✗ (ติด SFG) · `SFG FG T/T QC` ✓ · `FG QC` ✓ · `FG BUFF P/T QC` ✓ · `SFG T/T IND TG FG QC` ✓ · `FG SFG2 QC` ✗ (ติด SFG2)

### ④ "Sync ไม่สำเร็จ / ดึงข้อมูลไม่สำเร็จ" — บอกไม่ได้ว่าเกิดจากอะไร
- **บั๊กที่เจอ**: `renderHome()/renderZone()/renderSettings()` อยู่**ใน try ก้อนเดียวกับการ sync** → ถ้า render พังหลังดึงข้อมูลสำเร็จแล้ว จะขึ้น "Sync ไม่สำเร็จ" + badge เด้งกลับเป็น **Local** ทั้งที่ข้อมูลลงมาครบ — วินิจฉัยผิดทาง
- **แก้**: แยกส่วนวาดหน้าจอออกมานอก try ของการ sync · แต่ละส่วนรายงานของตัวเอง
- **บอกสาเหตุจริง** ใน toast: "ไม่มีสัญญาณอินเทอร์เน็ต" / "ต่อเซิร์ฟเวอร์ไม่ได้ (เน็ต/Worker)" / ข้อความ error จริง + log ลง console
- **เพิ่ม timeout ให้ `api()`** (ปกติ 45 วิ · ก้อนใหญ่ 120–150 วิ) — เดิม `fetch` ไม่มี timeout เน็ตหลุดกลางทางแล้วค้างเงียบไม่มี error ให้เห็น

### ⑤ "พื้นที่เก็บข้อมูลในเบราว์เซอร์เต็ม" ที่เด้งบ่อย — ไม่ใช่ข้อมูลเต็ม
- **คำตอบ**: เต็มเฉพาะ **cache ในเครื่อง** ไม่ใช่ข้อมูลบนเซิร์ฟเวอร์ · เกิดจากเขียน cache 4 ก้อนลง localStorage **เต็มจำนวน** (KPI/prod 10,000 แถว + Valve IDs 50,000 แถว ≈ หลาย MB) แต่ quota ของ in-app browser (LINE/iOS) มีแค่ ~5 MB
- **แก้**: `_lsFlush()` เขียนลง disk แค่ชุดล่าสุดตามเพดาน (KPI/prod 4,000 · IDs 8,000 · plans 3,000) — **ของใน memory ยังครบเหมือนเดิม** และเปิดแอปครั้งหน้า `reload()`/`ensureAllJobsLoaded()` ดึงของจริงจาก Cloud อยู่แล้ว
- **record ที่ยังไม่ขึ้น Cloud (`_unsynced`) เก็บไว้ครบเสมอ ไม่ถูกตัด** — เป็นข้อมูลที่มีอยู่ที่เดียว
- ข้อความเตือนเขียนใหม่ให้เข้าใจตรง: "พื้นที่ cache เต็ม (ไม่ใช่ข้อมูลหาย) — ระบบทิ้ง cache เก่าให้อัตโนมัติแล้ว งานทั้งหมดอยู่บนเซิร์ฟเวอร์ครบ"

## v3.33 — 2026-07-30

### กล่อง "งานหลัง FG" + "ผลิตเสร็จ" แยกตาม VSM แล้ว (เดิมปนทุกสาย)
- **อาการ**: กล่องรับงานหลัง FG แสดงงานของ**ทุกสายรวมกัน** ต่างจากทุกที่ในแอปที่ล็อกทีละสาย → ดู VSM1/VSM2 ก็เห็น 34 งานที่เป็นของ VSM3/VSM4 ทั้งหมด · ยอด "ผลิตเสร็จ" ก็รวมงานจบของสายอื่นมาด้วย (`computeDoneFromStore()` คืนทุกสาย)
- **อันตรายที่แก้ไปด้วย**: ปุ่ม **"✅ จบงานทั้งหมด"** เดิมจบงาน**ข้ามสาย** — admin ดู VSM4 อยู่แล้วกด งานของ VSM3 จะถูกตั้ง DONE ไปเงียบ ๆ · ตอนนี้จบเฉพาะสายที่กำลังดู และข้อความยืนยันบอกชื่อสายชัด ๆ
- **โจทย์ยาก**: `targets` store เก็บงานจบเป็นคีย์ `jobproc.<VALVE::LOT>` ซึ่ง **ไม่มี VSM** อยู่ในคีย์ จึงต้องสืบสายจาก record
  - ยึด "record ใดก็ได้" ไม่ได้ — valve+lot เดียวกันวิ่งข้ามสายได้ **145 งานกำกวม** ทำให้ผลรวมทุกสาย 760 > ของจริง 589 (นับซ้ำ)
  - **ยึด "สายที่งานนั้นผ่าน FG"** (record ที่เครื่องตั้ง process = FG) → ชี้สายเดียวชัดเจน **581/589 งาน**
  - เหลือ 2 งานที่ผ่าน FG ทั้ง 2 สาย → ยึด **FG ล่าสุด** (กฎเดียว ไม่นับซ้ำ) · อีก 6 งานเก่าที่ไม่มี record เลย → ยกให้สายหลัก เพื่อให้ยังเห็นอยู่ ไม่หายและไม่โผล่ซ้ำทุกสาย
- **เพิ่มป้ายบนกล่อง** `🔒 เฉพาะ VSM n` เห็นชัดว่ากำลังดูสายไหน
- **ป้ายแผนกไม่ผิดสายแล้ว**: `PROC_DEPT` ฝัง FG/POL = 'VSM4' ตายตัว → ดู VSM3 ขึ้น "FG · VSM4" สับสน · เพิ่ม `procDept()` ให้ FG/POL ใช้ชื่อสายที่กำลังดู (P/T = PC, QC = QC เป็นแผนกอื่นจริง คงเดิม)
- **ผลตรวจ**: **32/32 ผ่าน** ทั้ง 4 VSM · งานค้าง VSM4 = 12, VSM3 = 24, VSM1/2 = 0 · **ผลรวมงานจบทุกสาย = 589 = ที่มีจริงใน store** (เดิม 760) · ไม่มีงานโผล่ซ้ำมากกว่า 1 สาย · ไม่มีของสายอื่นหลุดมาทั้งงานค้างและงานจบ · VSM3 แสดง "FG · VSM 3" ถูกต้อง · WIP OUT ยังตรงกับ FG เป๊ะเหมือนเดิม

## v3.32 — 2026-07-29

### ตรวจทั้งระบบ "ตัวเลขจากแหล่งเดียวกันต้องเท่ากัน" — 144/144 ผ่าน
- ไล่ตรวจทุกคู่ตัวเลขที่ใช้ข้อมูลชุดเดียวกัน ครบทั้ง 4 VSM × 2 สถานการณ์ cache: WIP OUT (การ์ด/IOT/ป๊อปอัป) = FG ใน Output ของ Process (เดือนนี้/วันนี้/เมื่อวาน) · WIP = ตารางลูกค้า = ป๊อปอัป · Output ของ Process หัวข้อ = ผลรวมชิป = drill-down วาล์ว = OPR ดิบ · Dashboard การ์ด ALL = ผลรวมราย Zone · กะกลางวัน+กลางคืน = รวมทุกกะ · 3 ถังไม่นับซ้ำกัน · งานหลัง FG ⊆ WIP OUT และยอดตรงกัน · byCust = ผลรวม items — **ผ่านหมด ไม่มีข้อไหนเพี้ยน**

### แก้ WIP / WIP IN คิดจาก cache ที่บังเอิญมี → คิดจากข้อมูลครบทุกช่วง (โรคเดียวกับ FG)
- **อาการ**: WIP กำลังผลิต และ WIP IN รอผลิต เป็น "สถานะปัจจุบัน" แต่คำนวณจาก `state.kpiRecords`/`state.prodRecords` ที่โหลดมาแค่ 30 วัน → **ตัวเลขขยับตามว่าเครื่องนั้นบังเอิญเปิดดูย้อนหลังแค่ไหน**
  | cache ที่มี | WIP กำลังผลิต | WIP IN รอผลิต |
  |---|---|---|
  | เดือนนี้ | 502,077 | 8,809 |
  | 40 วัน (ที่แอปใช้จริง) | 749,013 | 8,069 |
  | ครบทุกช่วง = ของจริง | **1,163,120** | **6,409** |
- **สาเหตุ**: งานที่ถึง FG ไปนานแล้วแต่หลุดช่วง cache → หา record ไม่เจอ เลยถูกนับเป็น "กำลังผลิต" ค้างตลอด · ส่วน WIP IN ก็ตรงข้าม คือ งานที่เริ่มผลิตไปแล้วแต่ record อยู่นอก cache → ยังโชว์เป็น "รอผลิต"
- **แก้**: เพิ่ม `ensureAllJobsLoaded()` ดึง KPI + prod records **ครบทุกช่วง** (`from=2020-01-01`) มาเก็บใน memory (`_allKpi`/`_allProd`) แล้วให้ `computeWipBuckets()` ใช้ชุดนี้แทน cache — ตรวจแล้วข้อมูลทั้งระบบมีแค่ ~9,400 KPI + ~9,000 prod (**881 KB + 304 KB บีบอัด**) โหลดครบได้สบาย · ไม่ลง localStorage (ก้อนดิบ ~14 MB เกินโควตา) · TTL 10 นาที โหลดตอนเข้าหน้าติดตามงาน
- **นิยาม WIP (ตามที่หน้างานเลือก)**: นับ**ทุกงานที่ยังไม่ถึง FG** ไม่ตัดตามอายุ — ในยอด 1,163,120 มี 630,779 ชิ้น (263 งาน) ที่ไม่มีการลงผลิตเกิน 30 วัน ส่วนใหญ่ค้างที่ IND / SSG / TOP&HOD
- **ระหว่างโหลดยังไม่เสร็จ** การ์ด WIP ขึ้น ⏳ กำกับ (ยอดชั่วคราว ยังไม่ครบ) — ไม่ปล่อยให้เข้าใจผิดว่าเป็นยอดจริง
- **ผลตรวจ**: ตัวเลข **นิ่งเท่ากันทุกสถานการณ์ cache** ทั้ง 4 VSM · WIP OUT ยังตรงกับ FG เป๊ะเหมือนเดิม · ไม่มี render error
- ผลพลอยได้: กล่อง "งานหลัง FG" ใช้ชุดข้อมูลเดียวกัน (`_postFgRecs = _allKpi`) ไม่ต้องดึงก้อน ~10 MB ซ้ำอีกรอบ

### หมายเหตุจากการตรวจ (ไม่ใช่บั๊ก — แจ้งให้ทราบ)
- **กล่อง "งานหลัง FG" ตั้งใจแสดงทุก VSM** (ตามคอมเมนต์เดิม "มองเห็นทุก VSM") ต่างจากที่อื่นที่ล็อกทีละสาย → ตอนดู VSM1/VSM2 จะเห็น 34 งานที่เป็นของ VSM3/VSM4 ทั้งหมด · ถ้าอยากให้ล็อกตามสายเหมือนที่อื่น แจ้งได้
- **โค้ดตาย**: `computeLocalStats()` ไม่ถูกเรียกใช้แล้ว · `hWip` `hCompleted` `hProdToday` `hKpiToday` อ้างถึง element ที่ถูกลบไปแล้ว (มี `if` กันไว้ ไม่ error)
- **การสะกด process ในข้อมูลดิบ**: มี 95 แบบ ในนั้นมี 16 กลุ่มที่ต่างกันแค่ตัวพิมพ์ (IND/Ind/ind/iND · FG/Fg · COT/Cot/CoT …) — ตรวจแล้ว**ไม่ทำให้ยอดเพี้ยน** (0 งาน) แต่ควรเก็บกวาดที่ต้นทาง · อีก 74 งาน (159,993 ชิ้น) ลง process ที่ไม่อยู่ใน flow ของวาล์วนั้นเลย เช่น `Cutter` `Tip lapping` `Remove`

## v3.31 — 2026-07-29

### แก้ยอด FG ไม่ตรงกัน: "Output ของ Process → FG" ≠ "WIP OUT เดือนนี้ (ผ่าน FG)"
- **อาการ**: การ์ด Output ของ Process โชว์ FG = **230,231** แต่การ์ด WIP OUT เดือนนี้ = **245,647** — ต่างกัน **15,416 ชิ้น** (ตรวจกับข้อมูลจริงวันที่แก้ ได้ 237,740 vs 253,156 = ต่าง 15,416 เท่ากันเป๊ะ)
- **สาเหตุที่ 1 — เหมายอดทั้งล็อตลงเดือนเดียว (ตัวใหญ่สุด)**: `computePostFgAll` เก็บยอด FG ของงานเป็น "ก้อนเดียว + วันที่เดียว" แต่ล็อตหนึ่งมักผ่าน FG **หลายวันและคร่อมเดือน** → ชิ้นที่ผ่าน FG ตั้งแต่ **มิถุนายน** ถูกนับเป็นยอดกรกฎาคมทั้งก้อน (**+16,116 ชิ้น** จาก 14 ล็อต)
- **สาเหตุที่ 2 — ใช้วันที่ผิดความหมาย**: วันที่ที่ใช้ตัดเดือนคือ **วันที่ Admin กดขยับงาน** (`jobprocd`) ไม่ใช่วันที่ผ่าน FG จริง → 12 ล็อตที่ผ่าน FG 29–30 มิ.ย. ถูกกด "ขยับ" วันที่ 10 ก.ค. เลยตกไปอยู่เดือนกรกฎาคมทั้งหมด
- **สาเหตุที่ 3 — ยอดหายเงียบ ๆ**: วาล์วที่ไม่มีใน Master (เช่น `6637` = 700 ชิ้น) และ record ที่ **ไม่ได้ระบุเบอร์วาล์ว** (มิ.ย. = 29,234 ชิ้น จาก 26 record) ถูก `continue` ทิ้งทั้งก้อน ทั้งที่ Output ของ Process นับให้
- **สาเหตุที่ 4 — ยอดแกว่งตามสิ่งที่บังเอิญมีใน cache** (ตรงกับที่หน้างานสงสัยว่า "แก้ข้อมูลแล้วระบบใช้ข้อมูลเดิม"): WIP OUT คิดจาก `state.kpiRecords` ที่โหลด 30 วันตอนเปิดแอป แล้ว `mergeRecords` **เพิ่มอย่างเดียว ไม่เคยตัดออก** → เปิดดูย้อนหลังเดือนก่อน record เดือนก่อนก็ค้างใน cache แล้วดันยอด "เดือนนี้" ให้เพี้ยน (จำลองแล้ว: ยอดเดียวกันได้ 237,040 / 253,156 / 401,141 แล้วแต่ cache) · ส่วน record ที่ถูก **แก้ไข/ลบจากเครื่องอื่น** เครื่องนี้ยังถือของเก่าไว้คำนวณต่อ

#### วิธีแก้ — ทำให้ "ตรงกันโดยโครงสร้าง" ไม่ใช่แก้ตัวเลขให้พอดี
- **`fgByDate` — ตัดยอด FG ราย "วันที่ผลิตจริง"**: `computePostFgAll` เก็บยอดแยกรายวัน → รวมช่วงไหนก็เท่ากับ Σ OPR ที่ process FG ช่วงเดียวกัน **เป๊ะเสมอ** ล็อตที่คร่อม 2 เดือนถูกแบ่งยอดตามจริง
- **ทุกตัวเลข WIP OUT มาจากแหล่งเดียว** `_wipOutRange(b, from, to)` — วันนี้ / เมื่อวาน / เดือนนี้ / ป๊อปอัป / แถบ IOT ใช้ฟังก์ชันเดียวกันหมด (เดิมต่างคนต่างกรอง และ "วันที่ว่าง" ถูกเหมาเป็นเดือนปัจจุบัน)
- **ใช้วันที่ผ่าน FG จริง** (`fgDate`) แทนวันที่ Admin กดขยับ (เก็บไว้เป็น `moveDate` สำหรับอ้างอิง)
- **ไม่ทิ้งยอดอีกต่อไป**: วาล์วนอก Master / ไม่ระบุเบอร์ → ติดธง `unknown` แล้ว **นับเข้า WIP OUT ครบ** พร้อมป้าย ⚠ ในป๊อปอัปให้ตามไปแก้ · กล่องจัดการงานหลัง FG ยังกรองออกเหมือนเดิม (ไม่รก)
- **`ensureFgMonthLoaded()` — ข้อมูลเดือนนี้ครบและใหม่เสมอ**: ดึงช่วง 1 → วันนี้ (ทุกกะ) แล้วให้ **Cloud เป็นแหล่งความจริงของช่วงนั้น** ผ่าน `_syncKpiFromApi` (record ที่ยังไม่ขึ้น Cloud `_unsynced` ยังเก็บไว้) — เรียกตอนเข้าหน้าติดตามงาน + ทุก 60 วิ → record ที่ถูกแก้/ลบจากเครื่องอื่นหายตามทันที
- **การ์ดสองใบใช้ข้อมูลชุดเดียวกัน**: `refreshProcOutput` เดิมดึง record มาแสดงแล้วทิ้ง ตอนนี้ยัดกลับเข้า `state` ด้วย แล้ววาดการ์ด 3 ถังซ้ำในรอบเดียว → ไม่มีอาการ "การ์ดหนึ่งอัปเดตแล้ว อีกการ์ดยังใช้ของเดิม"
- **ยามเฝ้ายอด (กันเกิดซ้ำ)**: ใต้ช่วงวันที่ของ Output ของ Process มีป้ายเทียบยอดอัตโนมัติ — ตรงกันขึ้น **`✓ FG 237,740 ตรงกับ WIP OUT`** · ไม่ตรงขึ้น **`⚠ FG ไม่ตรงกับ WIP OUT (+n)`** สีแดง เห็นทันทีในวันแรก ไม่ต้องรอจับได้เอง

#### ผลตรวจกับข้อมูลจริง (2026-07-29)
- **VSM4 กรกฎาคม**: FG 237,740 = WIP OUT 237,740 (**ต่าง 0** · เดิมต่าง 15,416) · **VSM3**: 908,462 = 908,462 · VSM1/VSM2 ตรงกันทั้งคู่
- **20/20 เคสผ่าน** — 4 VSM × 5 ช่วง (วันนี้ / เมื่อวาน / 7 วัน / เดือนนี้ / เดือนก่อน) ทุกเคส WIP OUT = Output ของ Process FG
- **ยอดนิ่งไม่แกว่งตาม cache แล้ว**: จำลองเปิดดูย้อนหลัง มิ.ย. และ พ.ค. เพิ่ม (7,097 → 8,952 → 9,373 แถว) ยอดเดือนนี้คงที่ **237,740 ทั้ง 3 รอบ**
- **ทดสอบข้อมูลเก่าค้าง**: ปลอม record ที่ถูกแก้จากเครื่องอื่น (+99,999) และ record ที่ถูกลบไปแล้ว (+55,555) → ยอดเพี้ยนเป็น 393,294 แล้ว `ensureFgMonthLoaded()` **ดึงกลับมาถูกต้องเป็น 237,740 เอง** (record ผีหาย · ค่า OPR กลับเป็นของจริง)
- ป๊อปอัป WIP OUT รวมได้ = ตัวเลขบนการ์ด = 237,740 · เมนูเลือกเดือน (Admin) อิงเดือนที่ผ่าน FG จริง · ยามเฝ้ายอดทดสอบทั้งกรณีตรงและไม่ตรงแล้ว

## v3.30 — 2026-07-28

### IOT — "งานที่กำลังผลิต" มองปราดเดียวรู้เรื่อง ไม่ต้องคลิกทีละเครื่อง
- **ปัญหาเดิม**: บนผังเครื่องจักรเห็นแค่ "รหัสเครื่อง + สีสถานะ OAE" — จะรู้ว่าเครื่องไหนกำลังผลิต **Valve No / Lot** อะไร ต้องแตะเข้าไปดูทีละเครื่อง (เต็มจอเท่านั้น) ตรวจงานทั้งสายจึงช้ามาก
- **หลักการ (หลังลองใช้จริงแล้วปรับใหม่ — ดู v3.30.1 ด้านล่าง): "ผังบอกที่ · กระดานบอกรายละเอียด"**
  - **ผังคงความสะอาดเดิม** — รหัสเครื่องตัวใหญ่กลางกล่องเหมือนเดิมทุกประการ + **แถบสีบาง ~2.5px บนหัวกล่อง = เบอร์วาล์วที่กำลังผลิต** (เบอร์เดียวกันได้สีเดียวกันทั้งผัง · hash คงที่ + golden angle → สีห่างกันมากที่สุด) กวาดตาเห็นทันทีว่าเบอร์ไหนเดินอยู่ตรงไหน กระจายกี่เครื่อง โดยไม่มีตัวเลขเพิ่มสักตัว
  - **กระดานงาน (Job Board) เหนือผัง = ที่อ่าน Valve No / Lot จริง** ตัวหนังสือขนาดปกติ อ่านสบาย · 1 บรรทัด = 1 สาย/โซน · **VSM3 จัดกลุ่มตาม Line จริง** (FORD 2 / NISSAN 1 / Polish D-203 …) ตาม `line_key` ที่บันทึกไว้ แล้ว fallback ไป map เครื่อง→สาย · **VSM1/2/4 จัดตาม Zone**
  - ชิปเครื่องในกระดาน = จุดสถานะ OAE + รหัสเครื่อง + **Valve** + Lot + จำนวนชิ้น (ขอบซ้าย = สีเดียวกับแถบบนผัง) · มีบรรทัด "อ่านว่า: เครื่อง · Valve No · Lot · ชิ้น" กันสับสนว่าเลขไหนคืออะไร · หัวกระดานสรุป "กี่เครื่อง · กี่เบอร์ · กี่สาย" + ชิปเบอร์ยอดฮิตพร้อมจำนวนเครื่องที่เดินอยู่
- **เรียงให้อ่านง่าย**: ในแต่ละสาย เบอร์ที่เดินหลายเครื่องขึ้นก่อน → รู้ทันทีว่าสายนี้กำลังรันเบอร์อะไรเป็นหลัก · สายที่มีเครื่องเดินเยอะขึ้นก่อน
- **"กำลังผลิตอยู่" คิดจากอะไร**: งานล่าสุดของเครื่อง = วันกะ + เวลาสิ้นสุดของ record (ใช้ `_iahRecTimes` ตัวเดียวกับแผง KPI รายชั่วโมง รองรับกะข้ามเที่ยงคืน) — ตรงกับช่วงวันที่/กะที่เลือกบน Dashboard เสมอ
- **เอาเมาส์ชี้ก็เห็น** — เพิ่ม `<title>` ทุกกล่องเครื่อง (Valve/Lot เต็ม ๆ + OAE) และ `aria-label` มีข้อมูลงานด้วย
- **กดชิปในกระดาน** = เลื่อนผังไปที่เครื่องนั้น + กะพริบไฮไลต์ (กลไกเดียวกับ TOP ปัญหา)
- **ปุ่มสลับ 3 ปุ่มบนแถบผัง** (จำค่าใน localStorage): 📋 **กระดานงาน** (`vsm4_iot_jobboard`, เริ่มต้น=เปิด) · 🎨 **สีเบอร์วาล์ว** (`vsm4_iot_jobdot`, เริ่มต้น=เปิด) · 🏷️ **เบอร์ในกล่อง** (`vsm4_iot_joblbl`, **เริ่มต้น=ปิด**) — ปิดทั้ง 3 = ผังกลับไปเหมือนเดิมทุกประการ
- สีสถานะ OAE / เส้นขอบ / badge TOP ปัญหา / 🆘 SOS **ไม่ถูกแตะเลย**

### v3.30.2 — แก้ด่วน: "ทำไมบางอันมี บางอันไม่มี ต้องเหมือนกันสิ"
- **สิ่งที่พลาด**: v3.30.1 ตั้งเกณฑ์ "กล่องต้องสูง ≥30 กว้าง ≥38 ถึงจะใส่ป้าย" + คำนวณฟอนต์**แยกทีละกล่อง**
  → บนผังเดียวกัน **บางเครื่องมีเบอร์ บางเครื่องไม่มี** และตัวหนังสือใหญ่ไม่เท่ากัน (VSM4 ได้ป้ายแค่ 4 จาก 20 เครื่อง) ดูแล้วสับสนว่าทำไมไม่เหมือนกัน
- **แก้ — บังคับให้เหมือนกันทั้งผัง** (`_jobFitPlan`):
  - **ตัดเกณฑ์ขนาดกล่องทิ้งทั้งหมด** → **ทุกเครื่องที่เดินอยู่ได้ป้ายครบ 100%** ไม่มีเครื่องไหนถูกข้าม
  - คำนวณ **ขนาดชุดเดียวจาก "กล่องที่คับที่สุดของผังนั้น"** แล้วใช้กับทุกกล่อง → ทั้งผังมี **font-size ของรหัสเครื่อง 1 ค่า และของ Valve 1 ค่า** เท่านั้น (ตรวจแล้วทั้ง 4 ผัง)
  - ตัดตัวอักษรที่ **จำนวนตัวเท่ากันทุกกล่อง** (ไม่ใช่ตามความกว้างของกล่องตัวเอง) → ป้ายหน้าตาเหมือนกันหมด
  - **Valve ตัวใหญ่กว่ารหัสเครื่องเสมอ** (รหัสเครื่อง ≤ 85% ของ Valve) เพราะเบอร์วาล์วคือข้อมูลที่ต้องอ่าน
  - เลือก **"เบอร์ครบ ตัวเล็กลงหน่อย" มากกว่า "ตัวโต แต่ตัด"** — เบอร์ที่ถูกตัดเสี่ยงอ่านผิดเครื่อง (ผลตรวจ: ทั้ง 4 ผังโชว์เบอร์ครบ ไม่มีการตัดเลย · VSM4 ตัวเล็กสุดเพราะกล่องแคบสุดกว้าง 31px บังคับทั้งผัง — ใช้ปุ่มซูมช่วยได้)
- **เครื่องที่เดินอยู่แต่ record ไม่ได้ระบุเบอร์วาล์ว** ก็ได้ชั้นข้อมูลเหมือนกัน — แถบเทา + `—` บนผัง · ในกระดานงานขึ้นชิปเส้นประ "ไม่ระบุเบอร์" (เดิมหายไปเฉย ๆ เลยดูเหมือน "บางอันไม่มี")
- **ป้ายในกล่องกลับมาเป็นค่าเริ่มต้น = เปิด** (flag `fv_iot_lbl_v3` ล้างค่าเก่าครั้งเดียวให้ทุกเครื่องเริ่มเหมือนกัน) เพราะตอนนี้สม่ำเสมอแล้ว
- ตรวจครบทั้ง 4 ผัง: เครื่องที่เดิน = จำนวนแถบสี = จำนวนป้าย ทุกผัง · font-size ไม่ซ้ำซ้อน · ตัวหนังสือล้นกล่อง **0**

### v3.30.3 / v3.30.4 — ตัดของที่ไม่จำเป็นออก + ทำให้ป้าย "เหมือนกันทุกกล่อง และอ่านออกจริง"
- **ตัดทิ้งตามที่หน้างานสั่ง**: 🎨 แถบสีเบอร์วาล์ว และ 📋 กระดานงาน (Job Board) — ไม่ได้ใช้ รกจอเปล่า ๆ · ลบทั้ง JS (`jobColor`, `renderIotJobBoard`, `iotJobLocate`, สวิตช์ 2 ตัว) และ CSS ทั้งชุด (~60 บรรทัด) ไม่เหลือโค้ดตาย · เก็บกวาดค่าเก่าใน localStorage ให้ด้วย
- เหลือปุ่มเดียวบนแถบผัง: **🏷️ เบอร์ในกล่อง** (ค่าเริ่มต้น = เปิด) · ปิดแล้วผังกลับไปเหมือนเดิมเป๊ะ (ตรวจแล้ว: text ต่อกล่องเหลือรหัสเครื่องอย่างเดียว)
- **"ทำไมบางอันมี บางอันไม่มี" — แก้ครบ 3 สาเหตุ**:
  1. เดิมมีเกณฑ์ "กล่องใหญ่พอถึงจะใส่ป้าย" → **ตัดเกณฑ์ทิ้ง** ทุกเครื่องที่เดินอยู่ได้ป้ายหมด
  2. record ที่ไม่ได้ระบุเบอร์วาล์วเลย → ยังขึ้นป้าย แต่โชว์ `—` สีเทาจาง (รู้ว่า "ข้อมูลขาด" ไม่ใช่ "ไม่มีป้าย")
  3. ความสูงบล็อกข้อความ **จองตามจำนวนบรรทัดของทั้งผัง** → เบอร์สั้น/ยาวอยู่ตำแหน่งเดียวกัน ไม่มีอันลอยขึ้นอันลอยลง
  ขนาดฟอนต์/ระยะ คิด "ชุดเดียวจากกล่องคับที่สุด" แล้วใช้กับทุกกล่อง — ตรวจแล้วทั้ง 4 ผัง: ป้ายครบ 20/20 เครื่อง, ฟอนต์มีแค่ 2 ค่า (รหัสเครื่อง+เบอร์), ตัวหนังสือล้นกล่อง 0
- **"ตัวเล็กจนอ่านไม่ออก" — แก้ 2 ทาง**:
  1. **เบอร์ยาวขึ้นบรรทัดที่ 2** แทนการบีบฟอนต์ — ตัดที่ `-` ที่ใกล้กึ่งกลางที่สุด (`24560-` / `1180`) เห็นเลขครบและตัวโตขึ้นเกือบเท่าตัว · เลือก 1 หรือ 2 บรรทัดให้ทั้งผังเหมือนกัน
  2. **ซูมอัตโนมัติ** — เดิมย่อผังให้พอดีกรอบทำให้กล่องจริงเหลือสูง ~20px (ผัง VSM1 ถูกย่อเหลือ 0.61 เท่า) ใส่ตัวหนังสือยังไงก็อ่านไม่ออก → คำนวณระดับซูมเริ่มต้นให้ตัวเบอร์บนจอถึง ~9px แล้วเลื่อนดูในกรอบ (เพดาน 2.2 เท่า)
  - ผลวัดจริง: เบอร์ 5 หลัก (แบบที่ใช้จริง) ได้ **9.1–12.5px ที่ซูม 1.0** (ไม่ต้องเลื่อนเลย) · เบอร์ยาว 10 ตัว ได้ 9.8–13.3px ที่ซูม 1.13–1.54
  - ปุ่ม **+/−** = ผู้ใช้คุมเอง (ระบบหยุดปรับให้) · ปุ่ม **⊙** = คืนค่าให้ปรับอัตโนมัติอีกครั้ง
- **ผังรวม 4 VSM**: ป้ายชุดเดียวกัน (รหัสเครื่อง + เบอร์) · ปุ่มบนแถบหัวเปลี่ยนเป็น 🏷️ เบอร์ในกล่อง · ไม่ซูมอัตโนมัติ (หน้านั้นมีไว้ดูภาพรวมทั้ง 4 สายพร้อมกัน — ซูมเองได้)

### v3.30.1 — แก้ด่วน: "ตัวเลขมั่วไปหมด" (จากการใช้งานจริง)
- **สิ่งที่พลาด**: รุ่นแรกยัด `รหัสเครื่อง + Valve + Lot` เป็น **3 บรรทัดในกล่องเครื่อง** ทุกกล่องที่มีงาน — แต่กล่องบนผังเล็กมาก (เตี้ยสุด 26px) ตัวหนังสือจึงจิ๋วเท่ากันหมด **แยกไม่ออกว่าเลขไหนคืออะไร** ผังที่เคยสะอาดกลายเป็นตัวเลขเต็มไปหมด
- **แก้**:
  - **ป้ายในกล่องเปลี่ยนเป็นค่าเริ่มต้น = ปิด** → เปิดหน้ามาเจอผังเดิมสะอาด ๆ (รหัสเครื่องขนาดเดิม fs 10) · ล้างค่าเก่าจากรุ่นทดลองครั้งเดียวด้วย flag `fv_iot_lbl_v2` ให้ทุกเครื่องเริ่มเหมือนกัน
  - **เปิดป้ายแล้วเหลือ 2 บรรทัด: รหัสเครื่อง + Valve เท่านั้น** (ไม่ยัด Lot ไม่มีป้าย `+N`) ตัวโตขึ้น (Valve ≥6px, ≤11.5px) และ**เกณฑ์ขนาดกล่องเข้มขึ้นมาก** — ต้องสูง ≥30 และกว้าง ≥38 ถึงจะใส่ป้าย (VSM4 เหลือ 4 เครื่องจาก 20 ที่ผ่านเกณฑ์) กล่องที่เล็กกว่านั้นคงผังเดิมแล้วไปอ่านที่กระดานงาน
  - **แถบสีบางลง** จาก ~6px เหลือ ~2.5px คงที่ (ไม่เบียดตัวหนังสือ) และ**แยกปุ่มเปิด/ปิดเป็นของตัวเอง** (🎨 สีเบอร์วาล์ว)
  - **กระดานงานเป็นตัวหลัก** — ตัวใหญ่ขึ้น (Valve 13px · Lot 11px), สูงได้ 280px, เพิ่มบรรทัดอธิบายลำดับฟิลด์
- **ผังรวม 4 VSM** (252+ เครื่อง กล่องเล็กกว่านี้อีก): ใช้ **สีบอกเบอร์อย่างเดียว ไม่เขียนตัวเลขลงกล่อง** · ปุ่มบนแถบหัวเปลี่ยนเป็น 🎨 สีเบอร์วาล์ว · ชี้ที่เครื่องอ่าน Valve/Lot เต็มจาก tooltip
- **ตรวจแล้วทั้ง 4 ผัง**: ค่าเริ่มต้น = ตัวหนังสือเพิ่มบนกล่อง **0 ตัว**, รหัสเครื่องขนาดเดิม, ตัวหนังสือล้นกล่อง **0** ทั้งตอนเปิดและปิดป้าย
- โหมดเต็มจอ: กระดานงานตั้ง `flex:0 0 auto` + จำกัดสูง 18vh → **ไม่แย่งพื้นที่ผัง**

## v3.29 — 2026-07-28

### Dashboard — VSM3 กด Zone แล้ว drill 2 ชั้น (ราย Line → รายเครื่อง)
- **เฉพาะ VSM3**: กดการ์ด Zone บน Dashboard (`showZoneDetail`) เดิมเด้งไป **รายเครื่อง** ทันที — ใหม่แยกเป็น **2 ชั้น** ให้ตรงกับโครงสร้างสายการผลิตของ VSM3
  - **ชั้นที่ 1** — สรุปราย **Line** (สายการผลิต): การ์ดต่อสาย (`zoneLineCard`) โชว์ OAE/DLE/PPLH/OPR/PDAC + Scrap/Target + 🆘 ที่ค้าง · สายที่มีข้อมูลขึ้นก่อน · เครื่องที่ไม่อยู่สายใดรวมไว้ท้าย "🏭 เครื่องนอกสาย"
  - **ชั้นที่ 2** — กดที่สาย → `showLineDetail(zone, lineKey)` แสดง **รายเครื่องในสายนั้น** (การ์ดเดิม `zoneMachineCard` ครบทุกฟิลด์ + รายการ record) + ปุ่ม **← กลับไปราย Line**
- **VSM1/2/4 คงเดิม** — กด Zone เด้งไปรายเครื่องตรง ๆ (ชั้นเดียว)
- การจับ record เข้าสายใช้ `line_key` ที่บันทึกไว้ก่อน ถ้าไม่มี (record เก่า/ลงรายเครื่อง) map จาก `machine_id → line` อัตโนมัติ (`VSM3_ZONE_LINES`/`VSM3_LINES`) → ตัวเลขชั้น 1/ชั้น 2 ตรงกันเสมอ
- fetch records ครั้งเดียวตอนกด Zone แล้ว cache ไว้ (`_zoneDetailCache`) → drill เข้าสายไม่ยิง API ซ้ำ · ป้ายบอกทางบน Dashboard เปลี่ยนเป็น "กดเพื่อดูราย Line →" เฉพาะโซน VSM3

### รวม 4 VSM (IOT) — drill ราย Line สำหรับ VSM3 ด้วย
- popup **"รวม 4 VSM"** เดิมกดการ์ด Zone → รายเครื่องทันที (`iotAllZonePop`) — ใหม่ **โซน VSM3 เห็นราย Line ก่อน** (ชั้นที่ 1) เหมือน Dashboard หลัก · กดสาย → `iotAllLinePop` ดูรายเครื่องในสาย (ชั้นที่ 2) แบบ popup ซ้อน + ปุ่ม **← กลับไปราย Line**
- สายที่มีข้อมูลเป็นการ์ดกดได้ (สรุป KPI + จำนวนเครื่อง/record) · สายว่างแสดงแบบจาง · เครื่องนอกสายรวมท้าย
- แยกโค้ดการ์ดรายเครื่องเป็น `_iotAllMcCards()` ใช้ร่วมทั้งระดับ Zone (VSM อื่น) และ Line (VSM3) · VSM1/2/4 คงเดิม (กด Zone → รายเครื่องตรง ๆ) · ป้ายบนการ์ด Zone ของ VSM3 = "กดเพื่อดูราย Line →"

## v3.28 — 2026-07-27

### หน้าประวัติ — โหมดตรวจสอบ (Verify Mode) ช่วย admin เทียบเอกสารเร็วขึ้น
- **ปัญหา**: พนักงานลง KPI เป็นหลายช่วงเวลา → Valve+Lot เดียวกันบนเครื่อง/วัน/กะเดียวกันโผล่หลายแถวคละกัน admin ต้องไล่สายตาว่า "200/200" เป็นของอันไหน เสียเวลา
- **แก้**: ปุ่ม **🔍 โหมดตรวจสอบ** (แท็บ KPI) ยุบ record เป็น **1 กลุ่ม = 1 บรรทัด + ยอดรวม OPR** ตาม `วันที่ | กะ | เครื่อง | Valve | Lot` (ตรง granularity กระดาษ 1 แผ่น) — ทดสอบจริง 648 record → 495 กลุ่ม
  - กด ▸ กางดูช่วงเวลาย่อยแต่ละอัน (เวลา/OPR/OAE/Process/ผู้บันทึก + ปุ่ม ✏️/🗑️ เดิม)
  - ติ๊ก **✓ ตรวจแล้ว** รายกลุ่ม → จำใน localStorage (`vsm4_hist_verified`, prune > 60 วัน) · จางลง + แถบความคืบหน้า "ตรวจแล้ว X/Y กลุ่ม"
  - **⚠️ เตือนช่วงเวลาซ้อนทับ** (เสี่ยงนับซ้ำ) · เรียงตาม Valve/Lot หรือ เครื่อง · ปุ่มล้างติ๊ก
  - `renderKpiHist` แตกเป็น `_renderKpiVerify` เมื่อเปิดโหมด — auto-refresh/paging/signature-guard ครบเหมือนเดิม

### IOT รวมทุก VSM — กด Zone เข้าไปดูรายเครื่อง (popup ซ้อน)
- การ์ด Zone ใน `iotAllVsmPop` (และแผง KPI ซ้าย) **กดได้** → เปิด `iotAllZonePop(v,zone)` แสดงรายเครื่องใน Zone นั้น
- จัดกลุ่ม record ตามเครื่อง · จุดสีสถานะ (ถึงเป้า/ใกล้/ต่ำกว่า OAE) · แต่ละเครื่องกาง (accordion) ดูงาน Valve/Lot/เวลา/OPR/OAE/Process
- สรุป KPI ของ Zone อยู่บนสุดของ popup เป็นบริบท

### หน้าประวัติ — ค้นหาแยกฟิลด์ + วันที่ default
- **ค้นหาแยก Valve / เครื่อง / Lot** (dropdown "ค้นด้วย") — เดิมค้นได้แค่เครื่อง (KPI ใช้ backend LIKE ที่มีอยู่, partial match, กรอง client ซ้ำให้ครบทุกกรณี)
- **วันที่ default** จาก `−30 วัน` → `−1 วัน` (เมื่อวาน→วันนี้) ดึงข้อมูลเท่าที่จำเป็น เร็วขึ้น

### หน้าวางแผน — แก้ import วันที่เพี้ยน (ต้นตอที่แท้จริง)
- **ต้นเหตุ**: `parseDate` เดิมสมมติ `D/M/YYYY` เสมอ → ไฟล์ Excel รูปแบบ **US `MM/DD/YYYY`** (เช่น `07/30/2026`) ถูกอ่านเป็น วัน=07 เดือน=30 → เก็บเป็น `2026-30-07` (เดือน 30 ไม่มีจริง) → `<input type=date>` ว่าง + วันที่กองรวมผิด
- **แก้**: `parseDate(s, fmt)` เดา D/M กับ M/D อัตโนมัติ (field ที่ >12 = วัน) · เดือน/วันเกินช่วง → คืน '' (โชว์ ⚠️) · เพิ่มตัวเลือก **📅 รูปแบบวันที่** ในกล่องวาง Excel (auto / วัน-เดือน-ปี / เดือน-วัน-ปี) สำหรับวันที่กำกวม (≤12 ทั้งคู่) · preview เตือนจำนวนแถวที่อ่านวันที่ไม่ได้

### หน้าวางแผน — ซ่อมวันที่เดิม / สถานะจริง / ปุ่มจัดการ / ช่องพอดี
- **ข้อมูลเดิมที่เพี้ยนไปแล้ว**: เพิ่ม `_planDateISO()` normalize ทั้ง **แสดง/กรอง/เรียง/export** (`2026-30-07`→`2026-07-30`, 145/146 กลับมาแสดงถูก) + ปุ่ม **🔧 ซ่อมวันที่ (N)** ซ่อมค่าดิบใน D1 ให้ถูก (user กดเอง) + ป้าย ⚠️ เตือนแถวที่ค่าเดิมผิดรูป
- **สถานะผูกกับการผลิตจริง** (`_planAutoStatus`): แสดงสถานะจาก record ที่บันทึกจริง — 📋 ยังไม่เริ่ม / ⚙️ กำลังผลิต **· ถึง process ไหน** / ✅ ผลิตเสร็จ (อ่านจาก kpiRecords + `getJobProc`=DONE) · คง dropdown แก้เอง (override) ไว้
- **ปุ่มช่วยจัดการ**: 📅⁺ เลื่อนไปขายเดือนหน้า (+1 เดือน) · ❌ ยกเลิก / ↩️ กู้คืน (ลูกค้ายกเลิก→เลื่อนเดือน) · 🗑️ ลบ
- **ช่องพอดีขึ้น**: ปรับความกว้างคอลัมน์ + ตาราง `min-width:760px` → จอแคบเลื่อนแนวนอนในกรอบแทนการบีบช่อง (Valve/Lot/Qty อ่านออก)

### หน้าวางแผน — มุมมองรายเดือน (ใหม่)
- แผง **📆 ยอดขายรายเดือน**: เลือกปี (◀ ▶) + ตาราง 12 เดือน (ม.ค.–ธ.ค.) โชว์ยอดรวม/เดือน · กดเดือนเพื่อกรองตาราง · ปุ่ม "ทั้งปี" · ไม่นับรายการที่ยกเลิก

## v3.27 — 2026-07-25

### แก้บั๊กใหญ่ — ยอดรวมทั้งระบบต่ำกว่าจริง (Output ราย Process / Dashboard) + WIP IN 1.5 พองเกินจริง
- **ต้นตอ**: API `/api/kpi` เดิม default `LIMIT 500` (เพดาน 5000) + `ORDER BY saved_at DESC` → query ตามช่วงวันกว้าง (เช่น "เดือนนี้") ถูกตัดเหลือ **500 แถวล่าสุด ≈ 1.5 วันสุดท้าย** เงียบ ๆ → ยอด OPR ราย process / dashboard ต่ำกว่าจริง ~20-25 เท่า (ยอดจริงทั้งเดือน VSM4 >2.3 ล้านชิ้น แต่การ์ดโชว์ ~6 หมื่น)
- **แก้ฝั่ง Worker** (`getKpiRecords`): ถ้ามี `from`+`to` → คืน**ครบทั้งช่วง** (default สูง · เพดานใหม่ 50,000) · ไม่มีช่วง (ดึงล่าสุด) คง 500 เท่าเดิม → client เดิมทุกตัวได้ยอดถูกทันทีเมื่อดึงข้อมูลรอบใหม่
- **แก้ฝั่ง Client**: ใส่ `limit=50000` ทุกจุดที่ดึงตามช่วงวัน (การ์ด Output ราย Process, report, dashboard auto-refresh, IOT sync, home background sync)
- **WIP IN 1.5 พองเกินจริง (ต้นตอเดียวกัน)**: `computeWipBuckets` เดิมเช็ค "เริ่มผลิตหรือยัง" จาก cache ที่ถูกตัด (~14 วันล่าสุด) → งานที่ผลิตไปนานแล้ว (พบย้อนถึง พ.ค.) หา record ไม่เจอ ค้างเป็น "รอผลิต" ตลอด → ยอดพองถึง ~2.7 แสน ทั้งที่จริง ~5 พัน
  - เพิ่ม endpoint เบา `GET /api/kpi/produced` → คืนเซ็ต (valve, lot) ที่เคยผลิตแล้ว **ครบทุกช่วงเวลา** (2 คอลัมน์) · client ดึงมา match "เริ่มผลิตแล้ว" ให้ครบ (poll 60 วิ) → งานเก่าหลุดออกจาก "รอผลิต" ถูกต้อง

### เก็บงานฝั่ง server (ตามหลัง v3.27 · ไม่กระทบหน้าจอ)
- **`/api/records`**: แก้เพดานแบบเดียวกับ `/api/kpi` — มี `from`+`to` → คืนครบทั้งช่วง (เดิม cap 1000/5000 ตัดงานเก่า)
- **`/api/kpi/produced`**: cache ที่ edge (`caches.default`) 60 วิ — หลายเครื่อง poll พร้อมกันได้ผลจาก cache ไม่ยิง DISTINCT สแกนทั้งตาราง D1 ซ้ำ (ลดภาระ/โควตา D1)

## v3.26 — 2026-07-23

### ปรับเกณฑ์ PDAC — เปลี่ยนเกณฑ์ "ผ่าน" จาก 90% → 88.3%
- **เกณฑ์ตัดสินผ่านต่อเครื่อง/ช่วง**: เดิม `pdac_val ≥ 0.90` → ใหม่ `≥ 0.883` ทั้งตอนบันทึก `pdac_pass` (`_saveKpiImpl`) และตอนนับเครื่องผ่านบนการ์ด KPI ราย Zone (`calcZonePdac`)
- **เป้า PDAC ราย Zone**: ค่า default เปลี่ยน 0.9 → 0.883 ทุก Zone (Zone1/2/3 + flat fallback ใน `DEFAULT_TARGETS`)
- **Migration ครั้งเดียว** (`migratePdacTarget`): ผู้ใช้เก่าที่มีเป้า PDAC = 0.9 ค้างใน localStorage/cloud จะถูกดันเป็น 0.883 อัตโนมัติตอนโหลด — เฉพาะค่าที่ยังเป็น 0.9 เป๊ะ (ไม่ทับค่าที่ตั้งเอง) · flag `fv_pdac_tgt_0883` กันรันซ้ำ · รันหลัง cloud sync เพื่อใช้ค่าจริง
- record เก่าที่บันทึกไว้แล้วจะถูกคำนวณผ่าน/ไม่ผ่านใหม่ตามเกณฑ์ 0.883 ทันทีตอนแสดงผล (อ่านจาก `pdac_val` ที่เก็บไว้ ไม่ต้อง save ใหม่)

## v3.25 — 2026-07-21

### IOT รวม 4 VSM — เพิ่มแผง "KPI รายชั่วโมง" (แทนการ์ดสรุปฝั่งซ้าย)
- **คอลัมน์ซ้ายใหม่ = ตาราง KPI รายชั่วโมง** ราย Zone ของแต่ละ VSM (แทนการ์ดสรุปเดิม) — จอเต็มเหลือ 2 ส่วน `[ตารางรายชั่วโมง | ผังเครื่องจักร]` ลากเส้นแบ่งปรับขนาดได้
- **เลือกดู KPI ได้ 5 ตัว**: OAE / DLE / PPLH / PDAC / SCRAP (ปุ่มสลับด้านบน)
- **record ที่คร่อมหลายชั่วโมง** (เช่น 08:00–10:00 OAE 60%) โชว์ค่าเดียวกันทุกช่วงที่คร่อม (08–09 = 09–10 = 60%) · คอลัมน์ "รวม" = ทั้งวัน
- **หน้าต่าง 5 ช่วง/หน้า** ยึด "ช่วงชั่วโมงที่มีข้อมูลจริง" (LIVE) — กด `‹ ›` เลื่อนเอง หรือปุ่ม "ตอนนี้" กลับมา LIVE · ปิดปุ่มอัตโนมัติเมื่อสุดช่วงข้อมูล
- **กดหัว VSM / แถว Zone = เปิดรายละเอียด** (ป๊อปอัปเดิม) · ป้าย 🆘 SOS ต่อ VSM · แถบอธิบายสี (ถึงเป้า/ใกล้/ต่ำกว่า)
- ป๊อปอัปรายละเอียด: การ์ด **กะกลางวัน/กลางคืน** ทำหัวแถบสีเข้ม (ทอง/คราม) + กล่องไอคอน อ่านง่ายบนจอ Production

### แก้บั๊ก — รายชั่วโมงไม่แสดงค่า (สำคัญ)
- record จาก Cloud ไม่มีคอลัมน์ `time_start`/`time_end` และเก็บ `shifts` เป็น JSON string → เดิมอ่านเวลาไม่ได้ ช่องรายชั่วโมงว่างหมด
- แก้เป็นอ่านเวลาผ่าน `_shiftsArr()` (แปลง string→array) แล้ว fallback ไป `shift_detail` · cache ด้วย WeakMap
- แก้หน้าต่าง LIVE ที่เดิมวิ่งตามนาฬิกาเครื่องไปช่องว่าง → ยึดช่วงที่มีข้อมูลจริงแทน

## v3.24 — 2026-07-18

### 1) WIP IN 1.5 — ยอด "รอผลิต" ตรงขึ้น (เดิมโชว์เกินจริง เช่น 1.2 หมื่น ทั้งที่จริง ~5 พัน)
- **สาเหตุหลัก**: รายการรับเข้าที่ลง **"ไม่มี Lot"** ไม่เคย match กับยอดผลิต (ยอดผลิตมี Lot เสมอ) → ค้างเป็น "รอผลิต" ตลอดกาล
- **แก้**: ถ้ารายการรับเข้าไม่มี Lot → เทียบ "ระดับเบอร์วาล์ว" แทน (มีการผลิตเบอร์นี้แล้ว = ถือว่าเริ่มผลิต ย้ายออกจากรอผลิตอัตโนมัติ) · ถ้ามี Lot ยังเทียบเป๊ะเบอร์+Lot เหมือนเดิม
- **แผงตรวจสอบใหม่** ในป๊อปอัป "รอผลิต" (📥 WIP IN 1.5): บอกชัดว่ายอดมาจากอะไร — **ซ้ำกี่กลุ่ม** (Valve/Lot เดียวกันลงหลายครั้ง + ส่วนเกิน) และ **ค้างเกิน 45 วันกี่รายการ** (มักลง Valve/Lot ผิด)
- เพิ่มตัวกรองสถานะ **"⏳ รอผลิต · ค้างนาน (>45 วัน)"** — กดดูเฉพาะรายการค้างนานทุกเดือนแล้วกด ✏️ แก้ได้ทันที

### 2) WIP IN 1.5 — ค้นหาด้วย Valve No / Lot
- ป๊อปอัป "รอผลิต" (ตาราง WIP IN): ช่องค้นหาเดิมค้นได้ทั้ง **Valve No และ Lot** แล้ว
- แผง "ลืมลงบันทึก WIP IN 1.5" (หน้าตั้งค่า): เพิ่มช่องค้นหา Valve No / Lot (กรองสดขณะพิมพ์)

### 3) WIP IN 1.5 — ปุ่มแก้ไข (กันพนักงานลง Valve No ผิด)
- **ตาราง WIP IN (ป๊อปอัป "รอผลิต")**: เพิ่มปุ่ม **✏️** ต่อรายการที่ยังรอผลิต → แก้ Valve No / Lot / จำนวน แล้วอัปเดตขึ้น Cloud (LWW) — รายการที่เคยลงผิดแล้วค้าง จะ match ยอดผลิตและหลุดออกจาก "รอผลิต"
- **แผง "ลืมลงบันทึก WIP IN 1.5"**: เพิ่มปุ่ม **✏️ แก้** → แก้ Valve No / Lot ที่พนักงานลงผิด "ตอนบันทึกผลิต" ที่ต้นทาง (อัปเดตทุก KPI + Production record ของ Valve/Lot นั้น ทั้ง Local + Cloud) แล้วรายการหายจากลิสต์

### 4) การบันทึก — เพิ่มช่วงเวลาแล้ว "บันทึกไม่ได้/ไม่หมด" ให้ชัดขึ้น
- ยังคงกฎ **บันทึกทุกช่วงพร้อมกัน** (ครบทุกช่วงถึงบันทึกได้ — กันข้อมูลตกหล่นเงียบ) แต่หน้าต่างแจ้งเตือนอธิบายชัดขึ้นว่าทำไมยังไม่บันทึก + ต้องทำอะไร
- เพิ่มปุ่ม **🗑 ลบช่วงนี้** ในหน้าต่างแจ้งเตือน — สำหรับ "ช่วงที่กดเพิ่มเข้ามา" (ช่วงหลักลบไม่ได้) กดลบช่วงที่ไม่ใช้/กรอกไม่ครบได้เลยแล้วกดบันทึกอีกครั้ง
- ปุ่ม "✏️ กลับไปแก้ไข" เลื่อนจอไปที่เครื่อง/ช่วงแรกที่ยังไม่ครบให้อัตโนมัติ

## v3.23 — 2026-07-18

### 1) หน้า IOT ราย VSM — ชิป "รับเข้าเดือนนี้" กดดูรายละเอียดรายกลุ่มลูกค้าได้
- ชิปกลุ่มลูกค้าใต้การ์ด WIP IN 1.5 (MSI / SWE / OES&OEM / Other) เปลี่ยนเป็นปุ่มกดได้ — กดที่กลุ่มไหนก็เปิดป๊อปอัปของกลุ่มนั้น
- **📥 รับเข้าแล้วเดือนนี้**: รายการ Valve No ที่รับเข้ามาแล้ว รวมยอดต่อเบอร์ (ครั้งที่รับ · Lot · รวมชิ้น) เรียงจากยอดมากไปน้อย + สรุปหัวป๊อปอัป "กี่เบอร์ · กี่ชิ้น"
- **⏳ ยังขาดจ่าย**: เทียบกับแผนขายที่ยังไม่ปิด (สถานะ planned/in_progress จากเมนูวางแผน) — แสดง Valve/Lot ที่รับเข้ายังไม่ครบ พร้อมยอด แผน/รับเข้า/ยังขาด และสรุป "ขาดกี่เบอร์ · กี่ชิ้น" (ถ้าครบทุกแผนขึ้นป้ายเขียว ✓)
- ใช้ได้ทั้งบนหน้า IOT ปกติ (modal) และ IOT เต็มจอ (ป๊อปอัปในจอเต็ม) · ข้อมูลกรองตาม VSM ที่ดูอยู่เหมือนตัวเลขบนชิป

## v3.20 — 2026-07-18

### 1) WIP IN 1.5 — ปุ่มอัตโนมัติลงบันทึกตาม "วันที่ผลิตที่ SSG" ไม่ใช่วันที่กด
- ปุ่มในแผง "ลืมลงบันทึก WIP IN 1.5" (หน้าตั้งค่า) — ทั้ง **➕ ลงบันทึก WIP IN ทั้งหมด** และปุ่มลงรายตัว — บันทึกด้วย**วันที่ผลิตจริงที่ SSG** (วันกะ `shift_date`) ของแต่ละ Valve/Lot
- ยอดจะไปตกวัน "เมื่อวาน / รวมทั้งเดือน" ในการ์ด IOT และคอลัมน์วันที่ในตาราง WIP IN ตามวันผลิตจริง — ถ้า Lot ผลิตหลายวัน ใช้วันผลิตล่าสุด
- หน้าต่างยืนยันบอกชัด "📅 ลงตามวันที่ผลิตที่ SSG (ไม่ใช่วันที่กด)" + รายการ preview มีวันที่กำกับ · ป้ายในแผงเปลี่ยนจาก "ล่าสุด" เป็น "วันที่ผลิต"
- ฟอร์มกรอกเอง "เข้ายอด WIP IN 1.5" ยังใช้วันที่ปัจจุบันตามเดิม · ระบบ sync ข้ามเครื่องไม่กระทบ (`updatedAt` ยังเป็นเวลาจริง)

### 2) Lot ยังไม่ลงทะเบียน — ปุ่ม "➕ ลงทะเบียนทั้งหมด" (ออก ID ให้อัตโนมัติ)
- แผง "Lot ยังไม่ลงทะเบียน" (หน้าตั้งค่า · Admin) เพิ่มปุ่มเขียวลงทะเบียน Lot ที่พนักงานพิมพ์เองทั้งหมดในคลิกเดียว
- มีหน้าตรวจทานก่อนยืนยัน (ติ๊กเลือก/ออกได้ · ปุ่มเลือกทั้งหมด/ล้างเลือก) — รายการที่ระบบเดาว่า**พิมพ์ผิด** (เช่น `$`→`S`) จะ**ไม่ถูกติ๊กไว้** พร้อมป้าย ⚠️ ให้กดแก้ Lot ก่อน
- ID Code ออกให้อัตโนมัติ `A000001…` (ตัวอักษรนำหน้า ไม่ชนบาร์โค้ดจริงที่เป็นเลขล้วน) + หมายเหตุ "ลงทะเบียนอัตโนมัติ…" ทุกแถว — ค้น/แก้/ลบทีหลังได้ในตาราง Valve IDs
- ขึ้น Cloud อัตโนมัติทีละ 8 รายการพร้อมแถบสถานะ · ลงทะเบียนแล้ว Lot หายจากแผงค้างและโผล่ในตัวเลือก Lot ของฟอร์มบันทึกทันที

## v3.19 — 2026-07-17

### 1) ประวัติ — Export Excel กรองเฉพาะ VSM ที่ดูอยู่
- เดิม API คืนข้อมูลทุก VSM ปนกัน → ไฟล์ Excel มี Zone ของ VSM อื่น (ZJ/ZK/ZI ฯลฯ) โผล่ตอนดู VSM4
- ตอนนี้ export ผ่าน `filterVsm` เหมือนตารางบนจอ — เห็นอะไรบนจอ ไฟล์ได้อย่างนั้น (ทั้ง KPI และ Production)

### 2) ค้นหา Process จาก Valve No — วางหลาย Valve จาก Excel ได้เลย
- copy คอลัมน์ Valve No จาก Excel มาวางในช่องค้นหาได้ทันที (คั่นด้วยขึ้นบรรทัด/แท็บ/ช่องว่าง/คอมมา สูงสุด 100 เบอร์)
- ผลลัพธ์แบบสรุป: แต่ละ Valve/Lot แสดง Process เป็น "ชิปสี" — เขียว=มียอดแล้ว · เหลือง=ขั้นถัดไป/ค้าง · ขาว=ยังไม่ถึง (ไม่โชว์ตัวเลขให้รก)
- กดชื่อ Process สีเขียว → popup จำนวน/เครื่อง/วันที่/พนักงาน + ปุ่ม ✏️ แก้ไข (โครงเดียวกับโหมดเดี่ยว)
- ค้นหา Valve เดียวยังได้ตารางละเอียดแบบเดิมทุกอย่าง

### 3) IOT รวม 4 VSM — ผังย่อขยายตามการปรับหน้าจอ + KPI พอดีจอ
- ลากเส้นแบ่ง KPI ⇄ ผัง → ผังคำนวณขนาด fit ใหม่ทันทีระหว่างลาก (เดิมคำนวณครั้งเดียวตอนเปิด ต้องกดซูมเอง)
- ย่อ/ขยายหน้าต่าง หรือเข้า-ออก fullscreen → refit อัตโนมัติทั้งผังและแผง KPI
- แผง KPI ซ้ายคอมแพ็กต์ลง (การ์ด/ช่องตัวเลขเล็กลง ซ่อนแถบ bar) + ถ้ายังเกินจอ ระบบย่อทั้งแผงอัตโนมัติ (zoom) → การ์ดสรุปทั้ง 4 VSM เห็นครบไม่ต้องเลื่อนขึ้นลง (ตอนกาง "KPI ราย Zone" ค่อยเลื่อนตามปกติ)

### 4) ตรวจสอบข้อมูลซ้ำ (จากที่ค้นหา Process โชว์ 2 รายการแต่ประวัติมี 1)
- สาเหตุ: เศษ record ค้างจากบั๊กก่อน v3.14 (แก้ไข KPI แล้ว Worker สร้าง prod record ใหม่แทนที่จะทับตัวเดิม) — รอบล้าง v3.15 จับเฉพาะคู่ที่ timestamp เท่ากันเป๊ะ เลยหลุดคู่ที่ห่างกัน ~30 นาที (เช่น G-255 FG 1,065×2)
- วิเคราะห์ทั้งฐาน: พบซ้ำจริง 21 แถว (copies > KPI slots) — เตรียม backup + SQL ไว้ที่ `VSM1-4 KPI/_dup_cleanup_2026-07-17/` (รอยืนยันก่อนลบจาก D1)
- กลุ่มที่ "ดูซ้ำแต่ไม่ซ้ำ": 1 การบันทึกของ VSM1/2 สร้าง KPI หลาย record (เครื่องละ process ยอดเท่ากัน) → prod record ต่อ process ตามจริง — ไม่ลบ

## v3.13 — 2026-07-12

### เพิ่มข้อมูลจริง VSM1 / VSM2 (ตามเอกสาร VSM1-2.xlsx) — เน้น KPI ใช้งานได้
- **พนักงาน**: VSM1 = 59 คน · VSM2 = 72 คน (รหัส → VSM ใน `VSM_EMP_MAP` + ชื่อใน seed) — ใช้เลือกผู้บันทึก/ต่อกะ/autocomplete ได้ทันที
- **เครื่องจักร (Machine No)**: รวม 81 เครื่อง แบ่งตามโซนจริง
  - VSM1 → `Zone 2 VSM1` (12) · `Zone 3 VSM1` (22)
  - VSM2 → `Zone 1 VSM2` (4) · `Zone 2 VSM2` (19) · `Zone 3 VSM2` (24)
  - โซนเดิม (Zone A–F ทดสอบ) ถูกแทนด้วยโซนจริง · bucket เครื่องทดสอบเก่าถูกล้างอัตโนมัติ
- **Process**: ยังไม่กำหนด (target/man_std ใช้ค่า default) — KPI ระดับเครื่องบันทึกได้เลย ไม่ต้องมี process
- หมายเหตุข้อมูล: รหัส `PST789` ชนกัน (มีทั้ง VSM3 'ณัฐพงษ์ ศรีปราชญ์' และ VSM2 'ภานุพงษ์ การงาน') → **คงเป็น VSM3 ตามเดิม** ข้าม VSM2 (รอรหัสที่ถูกต้อง) · D-1122 ในผัง VSM1 ซ้ำ 2 แถว → ตัดเหลือ 1

## v3.12 — 2026-07-12

### 1) WIP IN 1.5 — โชว์ "เครื่องจักรที่ผลิต"
- กดเข้าดูรายละเอียด WIP IN 1.5 → มีคอลัมน์ **เครื่องจักร** บอกว่าผลิตที่เครื่องไหน (🏭 ตามเลขเครื่อง)
- เครื่องจักรดึงจาก prod record ที่ link มาจาก KPI (`machine_id`) — งานที่ยัง "รอผลิต" จะเป็น `-`
- ป๊อปอัปประวัติ (🕘) ก็เพิ่มคอลัมน์ **เครื่องจักร** ราย Process ด้วย

### 2) WIP IN 1.5 — เพิ่มตัวกรอง/เรียงตาม "สถานะ"
- เพิ่ม dropdown **สถานะ**: ทั้งหมด · 🟠 รอผลิต · 🟢 เริ่มผลิตแล้ว
- ค่าเริ่มต้นเรียงแบบ **รอผลิตขึ้นก่อน** แล้วค่อยเรียงวันที่ล่าสุดในแต่ละกลุ่ม

### 3) ต่อกะ — ขยายช่องกรอก "ข้อมูลทั่วไป"
- ช่องกรอกแต่ละข้อในหมวด **ข้อมูลทั่วไป** ใหญ่ขึ้น (min-height 46px → 88px, rows 1 → 3) พิมพ์ยาว ๆ ได้สะดวก
- checkbox/ปุ่มลบจัดชิดบนของช่อง (align-items: flex-start) ให้ตรงกับบรรทัดแรก

## v3.11 — 2026-07-12

### 1) Process Master แยกเฉพาะ VSM1 / VSM2
- **VSM3 + VSM4** ใช้ Process Master ชุดเดียวกันเหมือนเดิม (`valve.processes`)
- **VSM1** และ **VSM2** แต่ละสายมี Process Master **ของตัวเอง** — Valve No เดียวกันมี process คนละชุดได้ (เพราะ VSM1/2 เป็น process **ก่อนเข้า** VSM3/4)
- ตัวแก้ Valve (หน้าตั้งค่า) มี **3 แท็บ**: `VSM 3/4` · `VSM 1` · `VSM 2`
  - แท็บ VSM3/4 = เลือกจากลิสต์มาตรฐานเหมือนเดิม
  - แท็บ VSM1/VSM2 = **พิมพ์ชื่อ process เองได้อิสระ** (พิมพ์แล้วกด ➕ เพิ่ม)
  - รายการวาล์วโชว์ป้าย `VSM1`/`VSM2` เมื่อมีการตั้งค่าไว้
- เก็บแยกในเครื่อง (localStorage) — cloud reload ไม่ทับหาย · รวมอยู่ใน Backup/Restore/Reset
- การบันทึกหน้างาน: ช่องเลือก Process จะโชว์ชุดตาม VSM ของโซนนั้นอัตโนมัติ · VSM3/VSM4 **ไม่เปลี่ยนพฤติกรรมเดิม**

### 2) พิมพ์ด้วยเสียง (Speech-to-Text ภาษาไทย) — ช่วยลงสาเหตุ OAE ต่ำ
- เพิ่มปุ่ม 🎤 ใน **กล่องเดิม** "🆘 OAE ต่ำกว่า 75% — ต้องการให้ช่วยอะไรไหม?" (ในหน้าสรุปก่อนบันทึก)
- กดพูดภาษาไทย → ข้อความขึ้นในช่องเลย (ต่อท้ายของเดิมได้ · กดซ้ำ = หยุด · ปุ่มกะพริบตอนอัด)
- ฟอร์มบันทึก**เหมือนเดิมทุกอย่าง** — แค่เพิ่มปุ่มไมค์เข้าไป

### 3) แก้ข้อความ/การขอสิทธิ์ไมโครโฟน
- ไมค์ทำงานเฉพาะเมื่อเปิดผ่าน **https หรือ localhost** (เปิดไฟล์ตรงๆ `file://` ใช้ไม่ได้) → แจ้งสาเหตุที่ถูกต้องแทนคำว่า "ไม่อนุญาต" เฉยๆ
- ขอสิทธิ์ไมค์ให้เด้ง prompt ชัดเจน + บอกวิธีเปิดสิทธิ์ (แตะ 🔒 ข้างที่อยู่เว็บ → ไมโครโฟน → อนุญาต → โหลดใหม่)
- เบราว์เซอร์ที่ไม่รองรับ (Safari/Firefox บางรุ่น) → แจ้งให้พิมพ์แทน ไม่พัง

### หมายเหตุ
- ยังต้องใส่ข้อมูลจริงของ VSM1/VSM2 เพิ่ม: รายชื่อเครื่องต่อโซน · รหัสพนักงาน (ใส่ใน `VSM_EMP_MAP` เพื่อ lock in) · ชื่อโซนจริง · Process Master ต่อวาล์ว (ใส่ผ่านตัวแก้ Valve แท็บ VSM1/VSM2)
- แก้ทั้ง `index.html` และ `indexV1.0.html` (เหมือนกันเป๊ะ)
